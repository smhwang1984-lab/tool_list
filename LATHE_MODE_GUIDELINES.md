# CNC 선반(Lathe) 모드 지침

Last updated: 2026-09-07 (v1.6.4에서 최초 작성, v1.6.8에서 §8/§9 갱신,
v1.7.1에서 §3/§8/§9(신설)/§10 갱신, v1.7.2에서 §7/§8/§10 갱신, v1.7.3에서
§8 갱신, v1.7.4에서 §8/§10 갱신)

이 문서는 사용자가 v1.6.4에서 지시한 **CNC 선반 모드**의 설계 원칙을 못박아 둔 것이다.
이후 어떤 작업에서도 이 지침을 먼저 읽고, 이 지침에 어긋나는 변경을 하지 않는다.

---

## 0. 최우선 원칙 — 완전 분리 (절대 규칙)

> **선반 모드는 지금까지의 밀링/MCT 장비와 완전히 별개로 취급한다.
> 선반 작업이 기존 장비(3축 MCT, 4축 MCT, 5축 밀링 A-to-C / B-to-C)의
> 툴패스 계산·표시에 영향을 주는 일은 절대로 없어야 한다.**

구현 규칙:

1. 선반 전용 로직은 **`is_lathe`(= 장비명에 "선반" 포함) 분기 안에서만** 동작한다.
2. 밀링 경로가 지나가는 코드(회전행렬, `G17/G18/G19` 평면 원호, `G43` 유효성,
   고정 사이클 등)는 **선반 작업을 이유로 수정하지 않는다.**
   선반에 필요한 계산은 밀링 코드를 고쳐서 재사용하지 말고 **선반 전용 함수를 새로 만든다.**
3. 밀링 회귀를 막기 위해, 선반 관련 변경을 할 때마다 기존 밀링 테스트가
   전부 통과하는지 확인한다(`pytest tests/test_nc_tool_list.py`).
4. 공용 상수/헬퍼를 건드려야 한다면, 기본값을 바꾸지 말고 **선반 전용 인자/분기를 추가**한다.

---

## 1. 좌표계 — X는 수직, Z는 수평

선반은 밀링과 축 배치가 다르다. 밀링 뷰 그대로 보면 선반 평면으로 보이지 않으므로,
**선반 모드에서는 X와 Z를 바꿔서** 화면에 올린다.

| 선반 기계 축 | 의미 | 3D 월드 축 | 화면(선반 뷰)에서 |
|---|---|---|---|
| **Z** | 주축(스핀들) 방향, 길이 방향 | 월드 **X** | **수평** (오른쪽 +) |
| **X** | 지름 방향 | 월드 **Z** | **수직** (위 +) |
| C | 주축 회전각 | 월드 **Y**(회전 성분) | 화면 깊이 |

- 즉 월드 좌표 = `(Z, C에서 나온 성분, X_반경)`.
- 주축(스핀들 중심선)은 월드 X축과 일치하고, 반경 0이 화면상의 수평 중심선이 된다.
- **이 축 스왑은 데이터(월드 좌표)에 반영한다.** 카메라를 비틀어서 흉내 내지 않는다.
  그래야 ISO 뷰에서도 축이 바뀐 상태로 자연스럽게 보인다.

---

## 2. X는 지름(DIA) 값 — 실제 이동은 절반

> "선반은 X20 일 때 DIA 값으로 20이다. 그래서 실제로 10만 움직이면 된다."

- 프로그램의 **X 워드는 지름(diameter)** 이다. 경로를 그릴 때는 **반경 = X / 2** 를 쓴다.
- **R(원호 반지름)의 개념도 동일하게 유의**해서 처리한다. 원호로 X 값이 연결될 때,
  시작점·끝점의 X는 지름이므로 반경 공간으로 환산한 뒤 원호를 계산해야
  실제 형상과 같은 패스가 나온다.
- I(원호 중심의 X 방향 오프셋)와 R은 **반경 값**(Fanuc 선반 관례)이므로 다시 절반으로
  나누지 않는다. 절반으로 나누는 것은 **X 워드(끝점)뿐**이다.
- 좌표 표시(오버레이)에 보여주는 X는 **프로그램에 적힌 지름 값 그대로** 둔다.
  절반으로 나눈 값은 내부 렌더링 좌표일 뿐, 사용자에게 보여주는 값이 아니다.

원호 처리 요약:

```
반경공간 시작점 = (Z_시작, X_시작/2)
반경공간 끝점   = (Z_끝,   X_끝/2)
중심            = 시작점 + (K, I)        # I, K는 반경값, 그대로 사용
R 지정 시        = 반경값 R 그대로 사용
```

---

## 3. 투영(뷰) — 선반 모드 전용

선반 모드일 때 투영 버튼은 밀링용(ISO/XY/XZ/YZ)과 다르게 구성한다.

- **ISO** — 축이 바뀐 상태(1번 항목의 월드 매핑)로 보는 등각 뷰.
- **선반 뷰(평면 투영)** — Z가 수평, X가 수직으로 보이는 정면 평면 뷰.
  월드 기준으로 월드 XZ 평면을 정면에서 보는 것(elevation 0, azimuth -90)에 해당한다.
- **XC 뷰(v1.7.1)** — 극좌표(G12.1) 전용 정면 평면 뷰. 주축 방향(월드 X)에서
  보는 것(elevation 0, azimuth 0)으로, 화면 가로가 C(월드 Y), 세로가 X 반경
  (월드 Z)이다. G12.1 형상이 실제로 놓이는 평면을 그대로 보여준다 — "선반" 뷰는
  이 평면이 화면 깊이라 형상이 선으로 눌린다. 기존 "선반" 뷰/계산은 변경 없음.

밀링 모드의 XY/XZ/YZ 버튼은 선반 모드에서 노출하지 않는다(개념이 맞지 않음).
선반 모드 투영 버튼은 **ISO / 선반 / XC** 세 개다(v1.7.1). "선반"/"XC" 둘 다
평면 뷰라 좌드래그가 회전 대신 팬으로 잠기고(6항), 뷰 큐브도 숨겨진다.

축 화살표 라벨도 선반 모드에서는 월드 X에 **Z**, 월드 Z에 **X**, 월드 Y에 **C** 로 표기한다.

---

## 4. 공구 교체 기준 — `Tnn00`

> "선반에서 툴체인지 기준은 Tnn00입니다."

밀링/MCT는 `M6 Tnn`이 공구 교체 지점이지만, **선반은 M6가 없고 `Tnn00`이 기준**이다.

- T 뒤 네 자리에서 **앞 두 자리 = 공구 번호, 뒤 두 자리 = 옵셋 번호**.
- **옵셋이 `00`인 블록만 공구 교체 지점**이다. `T0101`처럼 옵셋이 살아 있는 블록은
  공구 보정을 거는 것이지 교체가 아니므로 새 공정을 만들지 않는다.
- **`T0000`은 제외한다** — 공구 교체가 아니라 옵셋 취소(프로그램 끝에서 흔히 쓴다).
- T 워드는 블록 어디에 있어도 된다(`G50 S2500 T0100 M08`). 다만 **괄호 주석 안의
  `T0101`은 코드가 아니므로** 주석을 걷어낸 뒤 판정한다.
- 다섯 자리 이상(`T012345`)은 잡지 않는다.

이 기준은 원래 세 곳(공구 리스트 산출, 3D 뷰어 공정 분리, '다음공구검색')에서
같이 쓰였으나, **v1.6.9부터 3D 뷰어의 공정 분리만 아래 §4-1의 N번호 기준으로
바뀌었다** — 공구 리스트 산출(`parse_lathe_program`)과 '다음공구검색'
(`find_next_tool_change_span`)은 여전히 이 Tnn00 기준을 그대로 쓴다(2026-09-06
사용자 확정 — '다음공구검색'의 목적은 "공구 교체 지점 찾기"라서 공정 경계와는
다른 개념). 셋 다 선반 분기에서만 갈라지고, 밀링의 `M6 Tnn` 인식은 그대로 둔다.

## 4-1. 3D 뷰어 공정 분리 기준 — `N번호 ~ M0/M1/M30` (v1.6.9)

> "선반 항목은 시작은 N넘버 공정시작부터 M1이나 m0 m30이야 내가 말한 공정
> 부분이라는 건 수행하는 부분이야."

v1.6.4~v1.6.8까지 3D 뷰어의 선반 공정 분리도 §4의 `Tnn00` 기준을 그대로 썼다.
그런데 실제 프로그램은 복귀 이동과 다음 공구 예약을 한 줄에 같이 쓰는 경우가
많다(`G00 X200. Z200. T0300`처럼). 이 경우 `T0300`이 그 줄에서 새 공정을
열어버려, 복귀 이동이 이전 위치가 아니라 새 공정 쪽에 붙어 그려지는 버그가
있었다(v1.6.9 사용자 리포트).

- **공정 경계는 이제 `N<번호>` 단독 라인(`process_nc_lines`가 이 라인을 원본
  줄번호로 판정)부터 그 블록 안에서 처음 만나는 `M00`/`M01`/`M30`까지다.**
  "공정 = 수행하는 부분"이라는 정의대로, 그 M라인 뒤 ~ 다음 N 전까지("닫힌
  구간")의 이동은 어느 공정에도 그려지지 않는다(모달 위치는 계속 갱신되므로
  다음 공정은 정확히 그 실제 위치에서 시작한다).
- 블록 안의 대표 공구번호는 공구 리스트 파서(`parse_lathe_program`)와 **같은
  우선순위**로 고른다 — 옵셋이 살아있는 `Tnnnn` 우선, 없으면 `Tnn00`. 그 덕에
  블록 끝의 "복귀 + 다음 공구 예약" 줄의 T워드는 무시되고, 그 블록이 실제로
  쓴 공구가 잡힌다.
- **N 라인이 하나도 없는 선반 프로그램은 기존 `Tnn00` 기준(§4)으로 자동
  폴백**한다(2026-09-06 사용자 확정) — v1.6.7 동작 그대로, 닫힌 구간 없이
  항상 열려 있다.
- 구현: `nc_viewer_widget.NCViewerWidget.LATHE_N_LINE_RE`/`LATHE_ANY_T_LINE_RE`가
  `NC_Tool_List.LATHE_N_RE`/`LATHE_ANY_T_RE`와 같은 패턴이어야 한다(순환
  임포트를 피하려 복제해 뒀고, 드리프트는 테스트로 막는다).

## 5. G02/G03 회전 방향

- 선반 뷰(Z 오른쪽, X 위)를 정면에서 볼 때 **G02가 시계 방향**으로 보여야 한다.
- 월드 매핑상 이는 (u=월드 X, v=월드 Z) 평면에서 각도가 감소하는 방향이며,
  기존 원호 보간 루틴의 G02 규약과 일치한다.

---

## 6. 선반 뷰 카메라 (v1.6.5)

> "선반뷰는 절대적으로 고정된 상태" + "상하좌우로는 움직일 수 있게" + "드래그 줌 버튼 추가"

- **"선반" 투영 버튼**을 누르면 `OrthographicGLViewWidget.orbit_locked = True`가 되어
  좌드래그가 orbit(회전) 대신 **pan(상하좌우 이동)** 으로 동작한다 — 평면 뷰가 회전으로
  깨지는 일이 없다. 휠 줌/줌아웃은 그대로 된다.
- **"ISO" 버튼**(선반 모드에서도 노출)은 `orbit_locked = False` — 자유 회전 3D로 되돌아간다.
  뷰 큐브(모서리 오리엔테이션 큐브)도 `orbit_locked`인 동안 숨겨, 큐브 드래그/면 클릭으로
  잠금을 우회하지 못하게 한다.
- **바운딩박스 리센터**: 선반 경로는 X(지름)가 반경으로 바뀌어 항상 월드 Z≥0(화면 절반)
  에만 그려지므로, 밀링처럼 원점(0,0,0)으로 리센터하면 경로가 위로 쏠려 보인다.
  `NCViewerWidget._lathe_path_center_and_radius()`로 로드된 경로의 바운딩박스 중심을 구해
  그 지점으로 카메라를 맞춘다(밀링은 원래대로 원점 리센터, 변경 없음).
- **드래그줌**: 선반 전용 토글 버튼(투영 오버레이, 밀링에는 없음). 켜진 동안 좌드래그는
  화면에 사각형을 그리고, 손을 떼면 그 영역이 화면에 꽉 차도록 한 번 확대한 뒤 자동으로
  꺼진다(`OrthographicGLViewWidget.set_drag_zoom_active`/`_apply_drag_zoom`). 직교 투영이라
  화면 픽셀과 카메라 거리가 선형이라 정확히 맞출 수 있다.
- **좌표 오버레이 위치**: 선반 모드에서는 좌표(X~C) 표시를 좌상단 목록에서 빼서 화면
  하단, 재생 속도바 바로 위로 옮긴다(`NCViewerWidget._place_coord_overlay`,
  `OrthographicGLViewWidget.bottom_coord_widget`/`_reposition_bottom_coord`). 밀링으로
  돌아오면 원래 좌상단 자리로 복귀한다.

## 7. 선반 전용 툴리스트 (v1.6.5)

> "선반 공구 별도 툴리스트 적립 필요, 현재 양식 사용하지 않아도 됨" + "1열 TOOL NO(옵셋
> 포함), 2열 인서트, 3열 홀더, 4열 N번 REMARK" + "필터 선에도 공구 이름 인서트로 넣을 것"

밀링 툴리스트(`COLUMNS`, `parse_program`, `TOOL_RE`/`N_RE`/`[SO ..]` 표기 기준)와
**완전히 별개인 새 양식**이다 — 서로 재사용하지 않는다.

- **파서**: `parse_lathe_program()`(`NC_Tool_List.py`). 실제 선반 프로그램(`O1699.nc`) 양식
  기준 — `N<번호>` 라인 단독(밀링의 `N#(#: Tool Change)`와 다름) 바로 아래, 통짜 괄호 주석
  두 줄이 각각 **1번째 = 홀더, 2번째 = 인서트**다. 그 블록(다음 N 라인 전까지) 안에서 옵셋이
  살아있는 `Tnnnn`(뒤 두 자리 ≠ `00`, `T0000` 제외)을 TOOL NO로 삼고, 없으면 옵셋 00짜리
  T워드를 그대로 쓴다(4항의 `Tnn00` 규약과 같은 우선순위).
- **표 스키마**: `LATHE_COLUMNS` = `NO(TOOL NO, 옵셋 포함) / INSERT / 홀더 / SO / REMARK(N번호)`.
  같은 TOOL NO(옵셋까지 동일)를 쓰는 N 블록은 한 행 + REMARK 누적(`N1, N5`), 옵셋이 다르면
  (`T0101` vs `T0111`) 별도 행 — 승인된 규약.
  `App.active_columns()`가 장비(`is_lathe_program()`)에 따라 `COLUMNS`/`LATHE_COLUMNS`를
  갈아 끼우며, 표 생성·편집·복사·PDF가 전부 이 메서드를 거친다.
  **SO(v1.7.2)**: 홀더/인서트 두 주석 줄 중 어느 쪽이든 밀링과 같은 `[SO nn]`
  표기가 있으면 `LATHE_SO_RE`로 뽑아 SO 열에 넣고(먼저 나오는 줄의 값 우선),
  INSERT/홀더 표시 문구에서는 그 표기를 걷어낸다.
- **PDF**: 밀링(16열 고정 레이아웃)과 별도로 `export_lathe_tool_list_pdf`/
  `make_lathe_pdf_table`(4열)을 새로 둔다.
- **3D 뷰어 필터**: 선반은 필터 라벨의 이름 자리에 NAME 대신 **INSERT**를 쓴다
  (`lathe_tool_name_map_from_rows`, 뷰어의 2자리 공구번호 키 T01~에 매핑).

---

## 8. 범위 (현재 단계)

v1.6.4~v1.6.5에서 구성하는 것은 **"기본 선반 모드"** 까지다. 다음은 이번 단계 범위:

- 축 스왑(Z 수평 / X 수직) 월드 매핑
- X 지름 → 반경 환산 (직선/원호 모두)
- 선반 전용 원호 계산(I/K/R, 반경 값 규약)
- 선반 전용 투영(ISO / 선반 뷰) 및 축 라벨
- C축 회전(있을 때) 주축 둘레 배치
- 공구 교체 기준 `Tnn00` (공구 리스트 / 3D 공정 분리 / 다음공구검색)
- 선반 뷰 카메라(회전 잠금/드래그줌/바운딩박스 리센터), 좌표 오버레이 하단 배치 (v1.6.5)
- 선반 전용 툴리스트(TOOL NO/INSERT/홀더/REMARK), 필터 라벨 인서트 표기 (v1.6.5)

v1.6.6에서 추가한 것(승인된 "밀링 툴(구동공구) 혼합 가공" 단계 진입):

- 선반 툴리스트 공구번호 정렬 + 빈 행 유지, 셀 폭 콘텐츠 맞춤(폰트 축소/말줄임 금지)
- `M35`(구동공구 ON) ~ `M34`(선삭 복귀) 상태 추적, 실제 기계 Y워드 반영
  (`lathe_local_point`/`lathe_rotate_c`로 로컬 좌표 계산 후 C만큼 배치 회전 —
  밀링 4/5축 원호와 같은 방식)
- `G12.1`/`G112` 극좌표 보간: C 워드를 각도가 아니라 Y(mm)로 해석
- M35 중 `G17`(기계 X-Y)/`G19`(기계 Y-Z) 평면 원호 (`ARC_PLANE_AXES`의
  `LATHE_G17`/`LATHE_G19` 키, 로컬 평면 보간 후 배치 회전)
- `M98 P<번호> [L<반복>]` 서브프로그램 호출 — `M30` 뒤 `O<번호>` 헤더 ~
  다음 헤더(또는 파일 끝)를 본문으로 보고 호출 지점에 펼침(`_expand_lathe_subprograms`).
  원본 줄번호를 유지해 커서 동기화가 깨지지 않는다. **G90/G91 증분 모드는
  이번 단계에 포함하지 않았다** — 이 선반 방언에서 G90은 고정 사이클과
  겹치는 표기라 절대/증분 전환이 아닐 수 있고, 증분은 보통 U/W 워드로
  표현된다(아래 "승인 후 별도 단계" 항목과 동일 사유로 보류).
- C축 회전 시뮬레이션: 재생 커서가 있을 때(동적 트레이스 + 커서 구)만
  공구가 +X 센터에 고정된 것처럼 반대 방향으로 회전. **정적 전체
  경로(plot_items)는 그대로 둔다** — "툴패스를 볼 때는 지금과 같이
  표현"(v1.6.4~5 그대로, C가 baked된 모습)와 "시뮬레이션 중에는 공구
  고정"을 구분한 것.
- 선반 ISO 투영에서도 뷰 큐브를 숨김(기존에는 "선반" 뷰에서만 숨겨져
  ISO에서 클릭으로 임의 각도로 샐 수 있었다) — ISO/선반 두 각도로만 보게.

v1.6.8에서 추가한 것(승인된 "고정 사이클" 단계):

- 드릴링 계열 고정 사이클 G73/G74/G76/G81~G89(취소 G80)를 MCT와 선반
  양쪽에서 인식한다. 코드별 세부 동작(Q 펙, P 드웰, G76/G87의 I/J
  시프트)은 구분하지 않고 전부 동일한 4점(접근/R점/깊이/복귀) 전개다.
  **[v1.7.1에서 정정]** 이 중 **G73/G74/G76은 선반에서는 인식 대상에서
  뺐다** — 아래 v1.7.1 항목 참고. MCT는 이 항목 그대로 변경 없음.
- MCT는 기존 그대로 R=절대 Z(초기점), Z=절대 가공깊이 — 인식 코드
  범위만 넓어졌다.
- 선반은 R/깊이 모두 **사이클 진입 직전 위치에서의 증분**이다(밀링과
  다름, 사용자 확정 2026-09-06). 방향은 평면(G17=주축 Z방향/G19=지름
  X방향)이 한 번이라도 명시됐으면 그 평면을 따르고, 평면 지령이 전혀
  없으면 사이클 블록의 깊이 워드로 자동 판정한다(Z워드면 Z축, X워드만
  있으면 X축). X축 방향에서는 R과 깊이 워드 모두 반경 공간 증분값을
  그대로 더한다(다시 나누지 않음 — 진입 시 지름→반경 환산은 기준점에서
  한 번만 적용). 선반은 이송 단위(G98/G99)와 무관하게 항상 초기점으로
  복귀한다(이전엔 G99에서 복귀 경로가 아예 안 그려지던 버그).
- 여기서 다루는 G76은 밀링 파인보링과 같은 단순 드릴 계열 취급이다 —
  선반의 실제 다중 패스 나사가공 G76(아래 계속 보류)과는 다르다.

v1.6.9에서 추가/수정한 것(승인된 "공정 경계·극좌표 보완" 단계):

- 3D 뷰어 공정 분리 기준을 `Tnn00`에서 `N번호 ~ M0/M1/M30`으로 옮겼다
  (§4-1 참고, N 라인이 없으면 `Tnn00`으로 자동 폴백).
- `G12.1`/`G112`(ON)·`G13.1`/`G113`(OFF) 극좌표 보간을 **`M35`(구동공구
  ON)와 무관하게** 인식한다(사용자 확정) — 이전에는 M35 밖에서 G12.1
  라인이 통째로 버려지고 C가 계속 각도로 읽혔다. 정규식에 `(?!\d)`
  가드도 추가했다.
- 극좌표 원호(G02/G03)는 **G17/G18/G19 평면 지령이나 M35와 무관하게**
  X(반경)-C(Y) 평면(`LATHE_G17`: I=반경 오프셋, J=C(Y) 오프셋)으로
  강제 보간한다 — "G17 평면 설정이 없어도 가공이 가능하다"는 요구.
  이전에는 `not polar_interpolation` 조건 때문에 극좌표 원호가 선삭용
  "LATHE" 평면(기계 Z × 반경)으로 잘못 보간됐다.
- `G13.1`로 극좌표를 빠져나오면 `cy_lathe`(로컬 Y)를 0으로 리셋한다 —
  안 그러면 이후 선삭 경로가 극좌표 중 마지막 C(Y) 값만큼 밀린 채
  남는다.
- `G28 H0.`(C축 원점 복귀, H는 C의 증분값 — 사용자 설명)을 인식해
  `cc_deg`를 0으로 리셋한다. 극좌표 구간의 음수 X는 `abs()` 없이 부호
  있는 반경 좌표 그대로 그린다("X는 실제 +위치에서 움직이고 C축이
  회전된 상태"라는 설명대로, 파트 좌표계로는 중심 반대편의 같은 점).
  U/W(X/Z 증분)는 여전히 아래 "승인 후 별도 단계"로 남겨 둔다.
- 툴리스트 산출 모드 콤보(`tool_mode_combo`) 표기에서 "MCT" 문구를
  뺐다 — 항목이 `['MCT (밀링)', '선반']`에서 `['밀링', '선반']`으로
  바뀌었다(판정 로직은 `'선반'` 문자열만 비교하므로 영향 없음).

v1.6.10에서 추가한 것("극좌표 시뮬레이션 재구현" — v1.6.9 배포 뒤 사용자
리포트로 발견됨):

> "극좌표 가공 모션이 안나옴 c축을 y로 계산해서 모션을 취하면 됨 먼저
> xy평면에 라인을 그리고 시뮬레이션에서는 x축을 상하로 움직이며 c축을
> 회전 시키면 됨 단 x항상 +위치에 있음"

- **증상**: v1.6.9의 극좌표 계산 자체(X-C를 Cartesian 평면으로 그리는
  것)는 맞았지만, 그 결과가 월드 Y(화면 깊이 축)에 놓여 재생
  커서/동적 트레이스에서 사실상 안 움직이는 것처럼 보였다 — 정적 경로는
  맞는데 "가공 모션"(시뮬레이션)이 안 보인다는 리포트와 일치한다.
- **정적 전체 경로는 그대로 둔다** — X(반경, 부호 있음)-C(Y) 평면에
  납작하게 그린 라인/원호(`target_pt`, `LATHE_G17` 원호 평면)는 이번에도
  손대지 않았다. "먼저 xy평면에 라인을 그리고"라는 요구를 이미 만족한다.
- **재생 커서/동적 트레이스(시뮬레이션)만 재구현**: 극좌표 구간의 매
  대상점에서 로컬 (y=C, z=반경)을 극좌표로 분해한다 —
  `r = hypot(y, z)`(항상 0 이상), `theta = atan2(y, z)`. 이 `theta`를
  v1.6.6 "C축 회전 시뮬레이션"이 쓰는 `line_to_c_rot[idx]`에 그대로
  얹는다(`cc_deg + theta`). 기존 `set_cursor_line()`의
  `lathe_rotate_c(pt, -c_rot)` 되돌리기 메커니즘이 그대로 재사용되어,
  커서가 반경(월드 Z, **항상 +**) 축 위에 고정되고 동적 트레이스는 그
  회전만큼 반대로 돌아간다 — "X축이 상하로 움직이며(반경, 항상 +)
  C축이 회전"하는 실제 기계 동작처럼 보이게 됐다. 새 코드/기존 메커니즘
  모두 M35/평면 지령과 무관하게 동작한다(v1.6.9와 동일 원칙).
- 정적 경로와 시뮬레이션 표현이 서로 달라지는 것은 v1.6.6부터 있던
  기존 설계 원칙("정적 전체 경로는 그대로 둔다" + "시뮬레이션 중에는
  공구 고정")을 극좌표에도 똑같이 적용한 것뿐이다 — 새 개념을 들여온
  게 아니다.

v1.7.0에서 수정한 것("극좌표 연속 X 가공 회전 고정" — v1.6.10 배포 뒤
사용자 리포트로 발견됨):

> "시물레이션에서 회전은 확인 하지만 지금 문제점이 X으로 연속 가공
> X36.F.05 X37. X29.9 X30.9 X23.8 X24.8 X17.7 X18.7 X11.6 X15.6이런
> 형태면 Z를 가지고 감 선반에서는 X만 움직이면 됨 그리고 g12.1툴패스
> 형상은 X Y(C)평면에 툴패스를 그대로 그려줘"

- **증상**: v1.6.10은 극좌표 구간의 매 대상점마다 그 순간의(로컬 Y, 반경)
  쌍으로 `theta = atan2(y, z)`를 **매번 새로 계산**했다. C 워드 없이
  X(반경)만 바뀌는 연속 가공(펙/플런지)에서, Y는 고정인데 반경만
  바뀌면 `atan2`값도 매 줄 달라져 — 실제로는 C가 전혀 안 바뀌었는데도
  시뮬레이션에서 회전이 줄마다 흔들리며 마치 다른 축으로 끌려가는
  것처럼 보였다("Z를 가지고 감").
- **정적 전체 경로는 이번에도 그대로 둔다** — `target_pt`/`target_local`
  계산은 손대지 않았다. X-Y(C) 평면에 납작한 라인/원호를 그대로 그린다
  ("g12.1 툴패스 형상은 XY(C) 평면에 그대로" 요구를 v1.6.9부터 계속
  만족하고 있다).
- **시뮬레이션용 회전각을 "C가 실제로 새로 지정될 때만" 다시 계산**하도록
  바꿨다: `lathe_polar_theta_deg`라는 모달 상태를 새로 두고, `C` 워드가
  있는 줄에서만 그 시점의 반경(`cx/2`, 같은 줄에서 X도 갱신됐다면 그
  값)과 새 Y로 `atan2`를 다시 구해 저장한다. C가 없는 줄(펙/플런지
  연속)은 이 값을 **그대로 물려받는다** — 그 구간 전체가 하나의 고정
  회전만큼만 돌고 반경(X)만 움직이므로, 서로 상대적으로 봤을 때
  "X만 움직이는" 것처럼 시뮬레이션된다(cc_deg/cy_lathe 같은 다른 극좌표
  모달 상태와 동일한 패턴). `G13.1`에서 이 값도 0으로 리셋한다.
- **M34가 극좌표 블록 "안에서" cy_lathe를 밀던 버그도 같이 고쳤다**
  (사용자 확정, 2026-09-06: "M35 밀링 가공 모드, M34 선반 가공 모드
  차이"). M35(밀링 가공 모드)~M34(선반 가공 모드) 토글은 G12.1~G13.1
  극좌표 블록 **안에서도** 나올 수 있다(예: 폴리곤 윤곽을 밀링하다
  잠깐 M34로 바꿔 X만으로 펙/플런지한 뒤 다시 M35로 돌아가는 구성).
  그런데 기존 M34 처리는 `polar_interpolation` 상태와 무관하게
  무조건 `cy_lathe = 0.0`으로 밀어버렸다 — 극좌표가 아직 열려 있는
  중에 M34가 나오면 그 순간 극좌표 Y가 갑자기 0으로 꺾여, X만
  바뀌어야 할 연속 가공이 다른 축으로 끌려간 것처럼 보였다("Z를
  가지고 감"의 또 다른 원인). G12.1~G13.1은 M35/M34와 **완전히
  별개의 모달 상태**이므로, `polar_interpolation`이 아직 True인
  동안은 M34가 나와도 `cy_lathe`를 리셋하지 않고 `G13.1`에서만
  리셋하도록 고쳤다.

v1.7.1에서 수정한 것("G76 오인식 차단 + M34/M35 모드 관리 + 극좌표 XC 뷰" —
v1.7.0 배포 뒤 사용자 리포트로 발견됨, 실제 프로그램 `O3230.nc`로 원인 확정):

> "m34읽고 홈 팩 가공 xz 이송 후 x축으로 가공하는데 사이클 같은 형태로
> 가공이 됨" / "m35를 읽고 다음 m34를 만나기전까지는 그사이 공정은
> 밀링 가공임 m34부터 m35를 읽기 전까지는 선반 공정임 2가지 코드가
> 없을 시 선반 모드 시작이 원칙임" / "극좌표 가공 패스 나타 낼 시 x c
> 평면으로 표기를 할 것"

- **근본 원인**: `O3230.nc`가 나사가공에 쓰는 `G76`(Fanuc 선반의 **복합형
  반복 사이클**, 그룹 00, one-shot)을 v1.6.8부터 **밀링 파인보링 G76(드릴
  계열, 그룹 10)** 으로 잘못 인식했다. 선반의 `G70~G76` 복합형 사이클은
  **`G80`으로 취소되지 않는데**, `O3230.nc`는 나사가공(428행) 뒤 `G80`이
  한 번도 안 나와 **그 뒤로 파일 끝까지 고정 사이클 모드가 켜진 채** 남았다.
  그 상태에서는 X/Z가 있는 모든 줄이 4점(접근/R점/깊이/복귀) 사이클로
  강제 전개된다 — "홈 팩이 사이클 같은 형태"의 원인이었고, 같은 누수가
  극좌표(G12.1) 구간의 C(로컬 Y) 성분까지 버려 "단순 X축에 라인이
  묶여" 보이게 만들었다.
- **선반은 G81~G89/G80(그룹 10, 드릴 계열)만 고정 사이클로 인식**하도록
  좁혔다 — G70~G76(복합형, 그룹 00)은 제외. MCT는 v1.6.8 그대로
  G73/G74/G76/G81~G89를 전부 인식한다(밀링 경로 불변, 가이드라인 §0).
- **M34/M35 가공 모드 전환 시 선반 고정 사이클 모달을 리셋**한다(방어선 —
  위 근본 원인은 이 리셋 없이도 이미 해결되지만, `G80` 없이 M34/M35가
  오가는 다른 프로그램에서 사이클이 다음 모드로 새는 것을 막는다). 리셋
  대상: `cycle_active`, `lathe_cycle_axis`/`lathe_cycle_ref`,
  `lathe_cycle_r`/`lathe_cycle_depth`, `lathe_plane_explicit`, 그리고
  사이클 코드로 남아있던 `current_motion`. **`G80`에 의한 정상 취소는
  그대로 병존한다**(사용자 확인 — "추가로 G80이 취소 코드임").
- **가공 모드 구간 원칙을 명문화**했다(§10 참고) — `M35`~다음 `M34` 전은
  밀링 가공 구간, `M34`~다음 `M35` 전은 선반 가공 구간, 둘 다 없으면
  처음부터 끝까지 선반 가공 구간. 기존 `lathe_milling_active` 초기값
  `False`가 이미 이 규칙을 만족해 코드 변경은 없었다.
- **극좌표(G12.1) 전용 "XC" 투영 버튼**을 추가했다(§3 참고) — 선반 모드
  투영 버튼이 ISO/선반/XC 세 개가 됐다. 극좌표 정적 경로 계산·시뮬레이션
  회전각은 v1.6.9~v1.7.0 그대로 손대지 않았다 — 보는 각도만 늘렸다.

v1.7.2에서 수정한 것(사용자 요청 3건, 2026-09-06, 실제 프로그램 `O3230.nc`로
가공시간 버그 원인 확정):

> "툴리스트 산출 모드에서 선반/밀링 드롭선택에서 보이는 창이 문자가 가려져
> 드롭창의 가로길이를 늘려 문자 가려짐 없게 설정" / "추후 선반 주석에 인서트
> 부분에 [SO nn] 값을 넣겠음 넣은 so 길이를 홀더와 remark 사이에 SO를 넣어서
> 그 데이터를 넣어줘" / "선반에서 극좌표나 밀링 가공 시 mm/min속도 계산이
> 맞지 않는 것으로 보임 지금 테스트 했던 파일에서 13공정 시간이 2시간이 넘음"

- **가공시간 — 선반의 G98/G99가 모션 코드를 덮던 버그(핵심)**. 밀링/MCT의
  `G98/G99`는 고정 사이클 복귀 레벨이라 만나면 `current_motion`을 `"G98"`로
  덮어써도 맞지만, **선반의 G98/G99는 이송 단위**(mm/min ↔ mm/rev)일 뿐
  모션 코드가 아니다(선반 전용 판정은 이미 따로 있다). 이 처리를 선반에도
  그대로 태우면, G98/G99만 있고 자기 G-워드가 없는 줄(모달 이동, 흔히
  위치 복귀 `G0`)이 절삭 이동으로 오인되고, 그 순간 유효 F가 직전
  나사가공 등의 mm/rev 값을 mm/min으로 잘못 적용해 시간이 수백~수천 배
  부풀었다(`O3230.nc:486` "G98X100.Z10.T0404" 뒤 13번 공정이 2시간을
  넘김). `not is_lathe`로 감싸 선반은 이 블록을 건너뛰게 했다 — 선반은
  이 `g98_active`를 아무 데서도 읽지 않는다(v1.6.8부터 선반 사이클은
  G98/G99와 무관하게 항상 초기점 복귀).
- **가공시간 — 이송 단위 전환 시 F 무효화(방어선)**. 같은 줄에 새 F 없이
  `G98`⇄`G99`가 실제로 바뀌면 `current_feed`를 0으로 되돌린다 — mm/rev로
  지정된 숫자를 mm/min으로(또는 그 반대로) 그대로 쓰면 시간이 어긋나기
  때문이다. F를 모르는 절삭 이동은 시간 0으로 넘기는 기존 설계
  철학(`effective_feed_mm_per_min` 참고)과 같은 원칙. 극좌표 좌표 계산과
  `lathe_spindle_rpm()`/`effective_feed_mm_per_min()` 공식 자체는 원래도
  맞았다 — 손대지 않았다.
- **선반 SO 열**. `LATHE_COLUMNS`가 `NO/INSERT/HOLDER/SO/REMARK` 5열이 됐다
  (§7 참고) — 밀링과 같은 `[SO nn]` 표기를 홀더/인서트 어느 주석 줄에서든
  읽어 SO 열에 넣고 표시 문구에서는 걷어낸다.
- **산출 모드 콤보 폭**. `tool_mode_combo`('밀링'/'선반')에 항목 실측 폭 +
  여유(`TOOL_MODE_COMBO_EXTRA_PX`)를 최소 폭으로 걸고
  `AdjustToContents`를 켜, 다크/라이트 공용 QSS의 테두리 때문에 화살표가
  글자를 가리던 문제를 없앴다. 장비 콤보와 공용 QSS는 손대지 않았다.

v1.7.3에서 추가한 것("H = C축 증분 좌표" — 사용자 요청, 실제 프로그램
`O4811.nc` N5 공정(측면 드릴 G87 + `H-180.`)으로 원인을 확정했다):

> "선반 가공 중 H= C축의 증분 좌표. 사이클 가공 중 C0에서 가공 후
> H-180.이라면 반대로 회전해서 가공 필요" / "g91g28h0은 c축을 기계원점으로
> 복귀한다는 뜻이야" / "선반에서는 G91개념이 없어 G28 H0일 때 원점 복귀
> 맞음 그냥 g0이나 g1모드에서 H값은 현재 C위치에서 증분 좌표야" / "m89는
> c축 클램프 m90은 c축 클램프 해제임"

- **근본 원인**: 좌표 생성 게이트(`process_nc_lines`, X/Y/Z/C/R 워드나 사이클
  모달의 R 워드가 있어야 통과)가 X/Y/Z/C/R 중 무엇도 없는
  `H-180.Q2500` 같은 줄을 통째로 걸러냈다 — G87 사이클이 모달로 열려
  있었는데도 이 게이트를 넘지 못해 반복 자체가 트리거되지 않았다. 또한
  H는 `G28`+`G91`이 같은 줄에 있을 때만 읽혔는데(v1.6.9), 선반에는 G91
  (증분 모드) 개념이 없어 `G28V0.H0.`(G91 없이 V 증분 워드로 쓰는 실사용
  형태)도 무시되고 있었다.
- **`G28`+`H` = C축 기계원점 복귀, G91 불요(선반 전용)**. 선반에는 G91
  개념이 없다(사용자 확정) — `G28 H..`는 G91 유무와 무관하게 C축
  기계원점 복귀다. 밀링/MCT는 기존대로 G91이 같은 줄에 있어야만
  이 분기를 탄다(가이드라인 §0, 밀링 경로 불변).
- **G0/G1(비극좌표) 모드의 단독 H = 현재 C 위치에서의 증분 회전**(절대각인
  `C`와 다름, 사용자 확정). `cc_deg += H값`으로 누적하고, 새 좌표 생성
  게이트에도 추가해 X/Y/Z/C가 전혀 없는 H-only 줄도 통과하게 했다 —
  이 덕에 모달로 열린 드릴 사이클(G81~G89)이 새 C 각도에서 그대로
  반복된다(예: O4811.nc N5의 `G87` 뒤 `H-180.`이 반대편에 같은 4점을
  다시 그린다). `G28`(C축 원점 복귀, 위에서 이미 처리·continue됨)이나
  `G43`(공구장보정, H가 옵셋 번호로 쓰임)이 있는 줄은 이 증분 해석에서
  제외한다.
- **M89(C축 클램프)/M90(클램프 해제)은 파싱하지 않는다**(사용자 확정) —
  좌표 워드가 없으므로 새 게이트를 추가한 뒤에도 여전히 무시되고, 다른
  모달 코드(M34/M35/M6 등) 정규식과도 겹치지 않아 오인식되지 않는다.
  `Q`(펙 깊이)/`P`(드웰)도 이번에도 파싱 대상이 아니다(§8 v1.6.8 범위
  그대로).

v1.7.4에서 바꾼 것("M98/M99 서브프로그램 호출을 선반·밀링 공용으로" —
사용자 요청, 2026-09-07, 예제 프로그램 `test files/1111.nc`로 실측 검증):

- **`_expand_lathe_subprograms()`(v1.6.6, 재귀 제너레이터) →
  `_expand_subprograms()`(pc + 호출 스택 프레임)로 교체**. 예전에는
  서브프로그램 본문의 끝을 "다음 O헤더(또는 파일 끝)"로만 봐서 M99를
  전혀 해석하지 않았다 — M99 뒤·다음 헤더 앞에 남은 줄이 있으면 잘못
  실행됐다. 이제 M99에서 정확히 멈추고, 복귀 지점(호출자 프레임의 pc)은
  자연히 "M98 바로 다음 줄"이 된다. 같은 M98을 다시 만나면(반복문이든
  단순 재호출이든) 그때마다 다시 펼쳐진다.
- **선반 전용에서 선반·밀링 공용으로 승격**(가이드라인 §0과 별개로,
  사용자가 밀링에도 이 기능을 요구했다 — §0의 "밀링 경로에 선반 로직을
  끼워 넣지 않는다"는 원칙은 그대로 지키되, M98/M99 확장기 자체는 순수
  텍스트 처리라 두 모드가 공유해도 안전하다). 단 **파일 전체에 M98이
  하나도 없으면 확장을 하지 않고 원본을 그대로 돌려준다** — 밀링 기존
  동작은 완전히 그대로고, 선반은 (M98 없이 M30만 있던 기존 파일에서)
  M30 뒤 내용도 계속 그려지게 됐다(이어붙인 여러 프로그램 포함).
- **`M99 P<n>` = `N<n>` 라벨로 점프**(사용자 확정: "본 프로그램에서 M99는
  N번호를 찾는 호출이다. M99P3이면 N1 N2를 건너뛰고 N3부터 시작"). 서브
  프로그램 안이면 호출자의 N<n>으로, 본 프로그램 안이면 자기 자신의
  N<n>으로 점프한다. 라벨이 없으면: 서브프로그램은 일반 M99(호출자
  복귀)로 폴백, 본 프로그램은 그 자리에서 끝낸다. **P 없는 본 프로그램의
  M99은 무시하고 다음 줄로 진행한다** — "M99 = N번호를 찾는 호출"인데
  찾을 N이 없으면 지시가 없는 것이고, 여기서 끝내면 뒤에 남은 본
  프로그램이 안 그려져 그림이 사라지는 쪽으로 틀리기 때문이다.
- **반복은 `L` 워드가 있을 때만**(사용자 확정: "반복은 M98P0001 L2 이런
  형태일 때만이다"). `P51002`처럼 P 앞자리를 반복수로 보는 Fanuc 축약형은
  쓰지 않는다. `M99 P<n>`은 반복을 적용하지 않고 즉시 빠져나간다 — 제어가
  다른 지점으로 명시적으로 넘어가므로 "이 서브프로그램을 다시 도는"
  개념이 없다(Fanuc 실기와 동일).
- **커서·재생 트리밍을 실행 순서(seq) 기준으로 전환**. 서브프로그램
  확장이 들어오면 원본 줄번호가 단조 증가하지 않는데(본문이 본 프로그램
  보다 뒤쪽 줄번호라), 예전 `_render_segments()`는 원본 줄번호 기준으로
  잘라 첫 M98에서 트레이스가 끊겼다(이 결함은 v1.6.6부터 선반에 이미
  있었다). 모든 경로 노드에 `"seq"`(실행 순서)를 붙이고 그 기준으로
  자르도록 바꿨다 — 서브프로그램이 없는 파일은 seq == 원본 줄번호라
  동작이 그대로다. 가공시간 누적(`_compute_machining_times`)도 seq
  기준으로 옮겨, 반복 구간에서 시간이 되감기지 않는다.
- 무한 루프 방어: 확장 스텝 상한(20만)을 두어 `M99 Pn`이 자기 앞으로
  계속 되돌아가는 프로그램도 앱이 멈추지 않는다.
- 사용자 제공 실제 예제 `test files/1111.nc`(9,870줄, M98 P0001~P0003
  각 2회 호출)로 확장 결과(19,653줄)와 공정 인식(T04/T07 2개)을
  실측 검증했다(스모크 테스트, 리포지터리에는 파일이 없어 있을 때만
  돈다).

아래는 여전히 **승인 후 별도 단계**로 다룬다(지금 임의로 넣지 않는다):

- U/W 증분 지령, G90/G91 증분 모드
- 선반 복합 선삭 사이클(G70~G72), 나사가공(다중 패스 G76), G92/G94
  — v1.7.1은 G76이 드릴 사이클로 오인되던 것만 막았을 뿐, 나사가공 자체의
  다중 패스 형상(P 나사산 높이/Q 첫 절입/R 테이퍼)은 여전히 전개하지
  않는다(현재는 시작~끝점 직선 한 개로만 그려짐).
- 공구 노즈 R 보정(G41/G42, TNRC)

---

## 9. 가공 모드 구간 — M34/M35 (v1.7.1)

> "m35를 읽고 다음 m34를 만나기전까지는 그사이 공정은 밀링 가공임 m34부터
> m35를 읽기 전까지는 선반 공정임 2가지 코드가 없을 시 선반 모드 시작이
> 원칙임"

- **`M35` ~ 다음 `M34` 전** = 밀링 가공 구간(`lathe_milling_active = True`,
  §7 M35 구동공구 밀링 참고).
- **`M34` ~ 다음 `M35` 전** = 선반 가공 구간(`lathe_milling_active = False`).
- **`M34`/`M35`가 하나도 없는 프로그램은 처음부터 끝까지 선반 가공 구간**
  — `lathe_milling_active` 초기값이 `False`라 코드 변경 없이 이미 성립한다.
- **모드가 바뀌는 순간 선반 고정 사이클 모달을 리셋**한다(`cycle_active`와
  그 부속 상태) — 한쪽 모드에서 취소(`G80`)되지 않은 사이클이 다음 모드로
  새지 않도록 하는 방어선이다. **`G80`에 의한 정상 취소는 이 리셋과 별개로
  그대로 동작한다** — 즉 "모드 전환 리셋"과 "G80 취소"는 서로 다른 두 경로로
  병존한다.
- `G12.1`~`G13.1` 극좌표는 M34/M35와 **완전히 별개의 모달**이므로(§8
  v1.6.9/v1.7.0 항목 참고) 이 리셋의 영향을 받지 않는다.

---

## 10. 승인 절차

사용자 지시: **"기본 선반 모드부터 구성 후 승인 대기"** → v1.6.4 승인 완료.
v1.6.5(줌/이동/전용 툴리스트)도 **플랜 승인 후 구현**(2026-09-05).
v1.6.6(M35 턴밀 Y축/평면 원호, M98, C축 회전 시뮬레이션, 뷰 제한)도 **플랜 승인 후 구현**,
버전 확정·설치본/포터블 빌드까지 승인 완료(2026-09-05).
v1.6.8(고정 사이클 G73/G74/G76/G81~G89, 선반은 R/깊이 증분 해석)도
**플랜 승인 후 구현**(2026-09-06). 산출 모드 선반/MCT 콤보, 좌표·투영
오버레이 하단 공통 배치도 같은 버전에서 함께 승인·구현.
v1.6.9(공정 경계 N번호 기준, G12.1 극좌표 M35/평면 비의존 보완, 산출
모드 콤보 MCT 문구 제거)도 **플랜 승인 후 구현**(2026-09-06).
v1.6.10(v1.6.9 배포 뒤 사용자 리포트로 발견된 극좌표 시뮬레이션 재구현)은
사용자가 직접 지시한 재구현·재빌드 요청으로 진행(2026-09-06).
v1.7.0(v1.6.10 배포 뒤 사용자 리포트로 발견된 극좌표 연속 X 가공 회전
고정)도 사용자가 직접 지시한 재구현·재빌드 요청으로 진행(2026-09-06).
v1.7.1(선반 G76 오인식 차단, M34/M35 가공 모드 구간 관리, 극좌표 XC 투영
뷰)도 **플랜 승인 후 구현**(2026-09-06) — 실제 프로그램 `O3230.nc`로
원인을 확정한 뒤 재구성한 플랜을 승인받았다.
v1.7.2(산출 모드 콤보 폭, 선반 SO 열, 선반 가공시간 G98 버그)도 **플랜
승인 후 구현**(2026-09-06) — 실제 프로그램 `O3230.nc`로 가공시간 버그
원인을 확정한 플랜을 승인받았다.
v1.7.4(M98/M99 서브프로그램 호출을 선반·밀링 공용으로 확장, M99 P<n> =
N<n> 라벨 점프)도 **플랜 승인 후 구현**(2026-09-07) — `v1.7.4_PLAN.md`
1차 플랜의 결정사항(A~E)을 사용자가 회신하고, 실제 예제 `test files/
1111.nc`로 재검증한 2차 플랜과 신규 판단(F/G)까지 승인받았다.

- 기본 선반 모드를 구현한 뒤에는 **사용자 승인 전까지 다음 단계로 나아가지 않는다.**
- 버전 번호 확정과 릴리스 패키징도 승인 후에 진행한다.
