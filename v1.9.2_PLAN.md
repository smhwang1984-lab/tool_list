# v1.9.2 플랜 — 소재(형상) 시뮬레이션: 속도 개선 + 저사양 표시 최적화

작성: 2026-09-24 / 상태: **승인 완료**(2026-09-24, §7 결정 A~G 모두 ★권장안 + 추가 지시 §8) — 구현 진행.
기반: v1.9.1 (`fd9c68b`, 브랜치 `worktree-v190-sim-plan`).
참고 자료: `PLAN_stock_simulation.md`(외부 플랜, 독립 `gcode_viewer.py` 기준으로 작성됨).
대상: `nc_sim.py`, `nc_viewer_widget.py`(시뮬레이션 부분만), `tests/test_nc_sim.py`, `NC_Tool_List.spec`.
**밀링 모드 전용** — 선반 모드 화면·계산은 손대지 않는다(LATHE_MODE_GUIDELINES.md).
툴패스 파서, 공구리스트, PDF, 라이선스는 변경 없음.

---

## 1. 요구사항 (사용자 지시, 2026-09-24)

> "PLAN_stock_simulation.md 이 플랜에서 지금 설정에서 도움이 될 만한 것과 속도에 개선될 수 있는 사항을
> 파악해서 소재 시뮬레이션하는 기능에 플랜을 구성하고 승인대기"

외부 플랜은 **새 뷰어를 처음부터 만드는 문서**다. 반면 이 저장소에는 v1.9.0/1.9.1에서 만든 Z-map 시뮬레이션(`nc_sim.py`)이 이미 있다.
그래서 외부 플랜을 통째로 옮기지 않는다. **지금 구조에 붙일 수 있는 것만** 골라 v1.9.2 범위로 정리한다.

---

## 2. 현재 성능 측정 (v1.9.1, 이 PC, Python 3.14.7 / numpy 2.5.2, numba 없음)

측정 조건: 소재 150×100mm, 자동 해상도 0.122mm → 1231×821 = **약 101만 셀**, D10 공구, 1mm 선분 지그재그 1,919개.

| 항목 | 현재 v1.9.1 | 문제 |
|---|---|---|
| 절삭 (FLAT, 공구색 기록 포함) | **10.9초** (선분당 5.7ms) | 샘플마다 `meshgrid` + `sqrt` + `height_grid`를 새로 계산하고 float64를 씀 |
| 절삭 (FILLET, 색 기록 없음) | 선분당 2.7ms | 같은 이유 |
| `to_mesh()` | 0.57초, **정점 608만 개 / 219MB** | 정점을 공유하지 않는 삼각형 묶음(soup)이고 전체 해상도 그대로 → 내장 그래픽에 너무 무거움 |
| 스냅샷 1개 | 12MB (float64 + int32) | 20개면 240MB |
| 실행 위치 | UI 스레드에서 동기 실행 | 계산하는 동안 화면이 멈춤 |

`ncdata.nc`(32,903줄) 규모에서는 절삭 한 번에 **수 분** 동안 UI가 멈출 것으로 추정한다(선분 수 × 3~6ms).

**시제품 측정**(외부 플랜 §2.3 "커널 미리 계산"을 numpy로만 구현): 같은 조건에서 **0.88초 (선분당 0.46ms)**.
기존보다 **6~12배 빠르다.** numba를 쓰면 여기서 수십 배 더 빨라질 것으로 예상한다.
numba 0.67은 cp314 휠이 있지만 numpy 2.5와의 호환은 설치해 확인해야 한다.

---

## 3. 외부 플랜 항목 분류

### 3.1 이미 있음 (v1.9.0/1.9.1) — 그대로 둔다
Z-map 방식, 공구 형상(FLAT/FILLET/BALL/DRILL/PL 챔퍼 — 외부 플랜보다 종류가 많음), 소재 팝업(T/W/L 축 배정),
공정 시작 스냅샷, G00 급속 절삭 경고, 경사면(G68.2) 구간 제외, 절삭면 공구 색, STL 내보내기, 안전 모드 해상도, 셀 100만 상한.

### 3.2 채택 — 속도
| # | 항목 (외부 플랜 출처) | 이 저장소에 적용하는 방식 |
|---|---|---|
| S1 | 공구 커널 미리 계산 (§2.3) | `ToolShape`마다 해상도별 **float32 높이 패치**(반경 밖 = +inf)를 한 번 만들어 캐시한다. 샘플 중심은 가장 가까운 격자점에 맞추고(오차 ≤ res/2), 절삭은 `np.minimum(sub, cz + patch)` 한 줄로 끝낸다. **시제품으로 6~12배 확인.** |
| S2 | 선분 샘플 정리 (§2.4) | 한 선분의 샘플을 한꺼번에 격자점으로 맞추고, 같은 셀에 떨어진 샘플은 **Z가 가장 낮은 것 하나만** 남긴다. 수직 이동은 낮은 끝점만 쓴다. |
| S3 | 소재 위 이동 건너뛰기 (신규) | 선분의 최저 Z ≥ `ztop`이면 깎을 것이 없으므로 계산하지 않는다. 대부분의 G00 복귀/접근이 여기에 해당하고 비용은 0에 가깝다. 소재 XY 밖 선분도 건너뛴다(§2.4). |
| S4 | Numba 커널 + numpy 대체 (§2.4) | `@njit(cache=True, nogil=True)` 커널은 **numba가 있을 때만** 쓴다. 없으면 S1~S3 numpy 경로를 쓴다. 두 경로의 결과는 같아야 한다(허용오차 1e-4). → **결정 B** |
| S5 | float32 사용 (§3) | `heights`는 float32, `color_ids`는 int16으로 바꾼다. 스냅샷이 12MB → **6MB**로 줄고, 20개 기준 240MB → 120MB가 된다. 스냅샷 개수 대신 **메모리 상한 200MB**로 관리한다(§2.5). |
| S6 | 백그라운드 계산 (§2.5) | 절삭을 `QThread` 워커에서 **자체 소재 복사본**으로 계산하고, 끝나면 UI 스레드가 결과를 넘겨받는다. 진행률은 상태 줄에 표시한다. 공정 선택을 바꾸면 진행 중인 작업은 **선분 청크 사이에서 취소**된다. → **결정 C** |

### 3.3 채택 — 저사양 표시 (외부 플랜 §3·§4)
| # | 항목 | 적용 방식 |
|---|---|---|
| R1 | 계산 격자와 표시 격자 분리 | 화면에 그리는 격자는 **최대 600×600**(안전 모드 300×300)으로 줄인다. 블록마다 **최솟값(min)**을 써서 좁은 홈도 사라지지 않게 한다. 공구 색은 블록의 최저점 셀 색을 쓴다. STL은 지금처럼 **계산 격자 전체 해상도**로 내보낸다. → **결정 D** |
| R2 | 정점 공유 메쉬 + 면 인덱스 재사용 | soup 대신 격자 정점을 공유하는 인덱스 메쉬로 바꾼다. 600×600이면 정점 약 36만 개로, 현재 608만 개의 약 1/17이다. `faces`는 표시 격자 크기가 바뀔 때만 만들고, 평소에는 정점 Z와 색만 바꾼다. 옆벽 4면과 바닥은 같은 인덱스 체계로 붙인다. |
| R3 | 조명을 정점 색에 미리 계산 | 법선은 `np.gradient`로 격자에서 바로 구한다. 고정 광원(램버트) 결과를 정점 색에 곱해 넣고 `GLMeshItem(shader=None, computeNormals=False)`로 그린다. 지금은 `shader='shaded'`(GLSL)를 쓰는데, v1.8.3에서 겪은 **그래픽 드라이버 문제**와 같은 계열의 위험을 줄이는 효과도 있다. |
| R4 | 색상 모드 | 팝업에 콤보박스를 추가한다: **공구 색**(현재 기본) / **깎인 깊이**(ztop − z, 컬러맵) / 단색. → **결정 E** |

### 3.4 채택 — 기능 보완
| # | 항목 | 적용 방식 |
|---|---|---|
| F1 | G00 충돌 목록 (§5) | 팝업의 "급속 절삭 N건"을 누르면 줄 번호 목록이 나오고, 항목을 누르면 그 줄로 커서가 이동한다(기존 줄 클릭 동작을 재사용). |
| F2 | 성능 상수 모음 (§9) | 표시 격자 상한, 스냅샷 메모리 상한, 청크 크기 등을 `nc_sim.py` 상단 상수로 모은다. |
| F3 | 벤치마크 스크립트 (§7) | `tests/bench_nc_sim.py`: 무작위 선분으로 절삭·메쉬 시간을 출력한다(pytest 대상 아님). |

### 3.5 제외 — 지금 설정과 맞지 않음
| 외부 플랜 항목 | 제외 이유 |
|---|---|
| 독립 `gcode_viewer.py`, `parse_gcode_ex`, `stock_render.py` 분리 | 파서·노드(`tool_paths`)·seq가 이미 `nc_viewer_widget.py`에 있다. 렌더링 헬퍼는 `nc_sim.py`에 함수로 둔다(Qt 의존 없음). |
| T코드 공구 테이블 UI (QTableWidget) | 공구 값은 **툴리스트에서 자동으로** 가져온다(v1.9.0에서 확정한 사용자 요구). |
| 툴팁/볼 중심 기준 옵션 | Tool tip 기준으로 이미 합의했다(v1.9.0 §1-6). |
| 슬라이더 드래그 중 초당 15회 재절삭 | v1.9.1에서 "재생 프레임마다 계산은 무겁다"는 사용자 피드백으로 뺐다. → **결정 F**에서 다시 켤지 정한다. |
| 도킹 패널 | 기존 [소재] 팝업을 유지한다. |
| 공구 형상 메쉬 표시 | v1.9.0 범위에서도 보류됐다. 이번에는 속도 개선에 집중한다. |

---

## 4. 설계 요약

```python
# nc_sim.py
SIM_DISPLAY_GRID_MAX = 600          # 안전 모드 300
SIM_SNAPSHOT_MEMORY_MB = 200
SIM_CHUNK_SEGMENTS = 2000           # 취소 확인 단위

class ToolShape:
    def kernel(self, res) -> np.ndarray      # (2k+1, 2k+1) float32, 캐시
class ZMapStock:                             # heights float32, color_ids int16
    def cut_segments(self, P0, P1, tool_ids, kernels, rapid, color_ids, cancel=None)
        # 일괄 처리: S2 샘플 정리 → S3 건너뛰기 → numba 또는 numpy 커널
        # 반환: 급속 절삭 인덱스 목록, dirty 영역
    def cut_segment(...)                     # 기존 API 유지(내부적으로 cut_segments 호출)
    def display_grid(self, max_n) -> (xs, ys, z_min, color)   # R1 블록 min
def build_display_mesh(xs, ys, z, rgba, zlo, cache) -> (verts, faces, colors)  # R2+R3
def to_mesh(...)                             # STL용 전체 해상도, 인덱스 메쉬로 교체
```

- `nc_viewer_widget.py`: `_rebuild_sim_nodes()`가 세그먼트를 **numpy 배열**(P0/P1/tool_idx/rapid/color/seq)로 만든다.
  `_sim_cut_forward`는 bisect로 구간을 잘라 `cut_segments`를 부른다(워커 스레드).
  `_sim_do_mesh_refresh`는 `build_display_mesh`를 쓴다.
- 결과 일관성: 증분 계산 = 처음부터 한 번에 계산 = 스냅샷 복원 후 계산(비트 단위로 같아야 함).

---

## 5. 구현 단계

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | S1·S2·S3·S5 numpy 커널 + `cut_segments` | 기존 30개 테스트 통과(격자 맞춤 오차 때문에 필요하면 허용오차를 ±1셀로 조정), 벤치 10.9초 → **≤ 1.5초** |
| 2 | S4 numba 경로 (결정 B) | numpy 결과와 같음(1e-4), 벤치 **≤ 0.2초**, numba 없는 환경에서도 테스트 통과 |
| 3 | R1·R2·R3 표시 메쉬 | 정점 ≤ 40만 개, 메쉬 갱신 ≤ 0.15초, 안전 모드에서도 정상 표시(스크린샷 확인) |
| 4 | S6 워커 스레드 + 취소 + 진행률 | `ncdata.nc`로 계산 중에도 뷰 회전·공정 선택이 멈추지 않음, 연속 선택 변경에도 결과 일치 |
| 5 | R4 색상 모드, F1 충돌 목록, F3 벤치 | 기능 확인, 전체 테스트 통과 |
| 6 | 버전 올림, 사용설명서, 패키징 확인 | exe에서 numpy 경로(또는 numba)로 동작, 성능 수치를 결과 보고에 기록 |

---

## 6. 테스트 추가 (`tests/test_nc_sim.py`)

- 커널 패치: FLAT/FILLET/BALL/DRILL/챔퍼의 커널 값이 `height_grid`와 같다(반경 밖은 inf).
- 새 경로와 기존 경로 비교: 무작위 선분 200개에서 두 결과의 차이가 공구 테두리 1셀 이내다.
- 소재 위 선분 건너뛰기: 결과가 바뀌지 않는다. 급속 경고 줄 번호도 유지된다.
- numba 경로 = numpy 경로 (numba가 없으면 skip).
- 표시 격자: 블록 min 다운샘플에서 폭 1셀짜리 홈이 사라지지 않는다. 면 인덱스 캐시가 재사용된다.
- 인덱스 메쉬 STL: 닫힌 메쉬(모든 변 2회 공유), 삼각형 수 = 기존 공식과 일치.
- 취소: 청크 도중 취소해도 원래 소재가 오염되지 않는다(워커는 복사본으로만 계산).
- 회귀: 전체 스위트(현재 377 passed, 1 skipped). 선반 모드에서는 시뮬레이션 비활성 유지.

---

## 7. 결정 필요 (권장안 ★)

| # | 질문 | 선택지 |
|---|---|---|
| **A** | 버전 | ★ v1.9.2 (성능·표시 개선) / v1.10.0 |
| **B** | numba | ★ **선택 의존성**: 개발 PC에 있으면 쓰고, exe는 우선 numpy 경로로 동작한 뒤 포함 여부를 따로 검토(llvmlite로 약 +60MB) / exe에도 포함 / 쓰지 않음(numpy만) |
| **C** | 계산 방식 | ★ QThread 워커 + 진행률 + 취소 / 지금처럼 UI 스레드에서 동기(빨라진 속도만 믿음) |
| **D** | 표시 격자 상한 | ★ 600×600 (안전 모드 300×300) / 400×400 / 제한 없음 |
| **E** | 색상 모드 | ★ 공구 색(기본) + 깎인 깊이 + 단색 / 공구 색만 유지 |
| **F** | 재생(F6/F8·슬라이더) 중 실시간 절삭 | ★ v1.9.1처럼 **공정 선택 시에만** 계산 / 빨라진 뒤 "재생 연동" 체크박스 추가(기본 꺼짐, 초당 10회 제한) |
| **G** | 격자 맞춤 정확도 | ★ 샘플 중심을 격자점에 맞춤(오차 ≤ res/2, 0.12mm 격자면 ≤ 0.06mm) / 정확한 거리 유지(numba에서만 속도 확보) |

---

## 8. 승인 결과와 추가 지시 (2026-09-24)

- 결정 A~G: **모두 ★ 권장안**. B(numba)는 개발 PC에 없어 **설치·다운로드를 허용**받았다 — 설치 후 numba 경로를 검증한다.
- **추가 지시 — 소재 표시는 단순하게**: 소재 형성 표시에 **샤이닝(조명/음영) 기능은 불필요**하다.
  단순한 블록 채움 형태로, **밝은 계통 색**으로 채우고 **모서리(엣지)가 표현**되게 한다.
  - R3(조명을 정점 색에 미리 계산)은 **폐기**한다. 조명 계산도, 셰이더도 쓰지 않는다(`shader=None`).
  - 면은 조명 없이 **밝은 색으로 평평하게** 채운다(공구 색 모드는 밝은(파스텔) 톤으로, 깊이 모드도 밝은 컬러맵).
  - 음영이 없으면 형상이 안 읽히므로 **모서리 선**을 그린다: ① 소재 블록 외곽 12개 모서리, ② 절삭으로 생긴 **경사 45° 이상의 단차 경계**(윗 테두리·아랫 테두리 선).
    어두운 회청색 가는 선, 안티앨리어싱 없음, 선 개수 상한을 둔다(표시 격자 기준).
  - 이렇게 하면 법선 계산(`np.gradient`)도 필요 없어져 표시 갱신이 더 가벼워진다.

---

## 9. 구현 결과 (2026-09-24)

### 9.1 계획 대비 달라진 점

| 항목 | 계획 | 실제 | 이유 |
|---|---|---|---|
| S1·S2·G (절삭 방식) | 공구 커널을 미리 계산해 샘플 위치를 격자점에 맞춰 찍음 | **각 칸에서 이동 선분까지의 수평 거리 d로 `z + h(d)`를 바로 계산** (반경 LUT 보간, 격자 간격의 1/32 간격) | 시제품 구현 후 측정해 보니 "샘플 × 커널 면적" 구조가 병목이었다(D6, 3만 선분: numpy 8.0초 / numba 3.0초). 선분 거리 방식은 연산이 약 1/7이고 격자 맞춤 오차가 아예 없다. 경사 구간은 Z 변화가 격자 간격의 1/4 이하가 되도록 잘게 나눠 처리한다. |
| R3 (조명 색 미리 계산) | 법선·램버트를 정점 색에 곱함 | **폐기 → 조명 없는 밝은 단색 채움 + 모서리 선** | 사용자 추가 지시(§8). 법선 계산도 필요 없어졌다. |
| numba 모듈 위치 | `nc_sim.py` 안 | **`nc_sim_numba.py` 별도 모듈(지연 import)** | 시작 시 numba import 비용 제거, PyInstaller가 numba를 자동으로 끌어오지 않게 하기 위함. `NC_Tool_List.spec`의 `excludes`에도 명시. |
| 모서리 선 | 45° 이상 단차의 위/아래 테두리 | **기울기가 45° 넘게 꺾이는 정점(2차 차분)** | 첫 구현(경사 45° 이상인 모든 칸)은 곡면 벽에 빗살무늬가 생겨 지저분했다. 꺾이는 곳만 그리니 선이 절반(15,502 → 7,857개)으로 줄고 깔끔하다. |
| 스냅샷 | 공정 시작 스냅샷 + 급속 경고 | 급속 경고 개수를 스냅샷과 함께 저장 | v1.9.1까지는 되감은 뒤 다시 깎으면 **급속 경고가 중복으로 쌓이는** 결함이 있었다(이번에 고침, 테스트 있음). |

### 9.2 측정 결과 (이 PC, Python 3.14.7 · numpy 2.5.2 · numba 0.67.0)

| 조건 | v1.9.1 | v1.9.2 numpy(exe 기본) | v1.9.2 numba(개발 PC) |
|---|---|---|---|
| 소재 150×100mm(약 101만 셀), D10, 1,919선분 | 10.9초 | **0.46초 (약 24배)** | **0.15초 (약 72배)** |
| 같은 소재, D6, 30,000선분 | 약 139초(표본 1,500선분 4.64ms/선분으로 추정) | **4.9초 (약 28배)** | **1.05초 (약 130배)** |
| 30,010줄 합성 3축 프로그램(뷰어 통합, 백그라운드) | UI 정지 | UI 정지 없음, 계산 8.9초 | UI 정지 없음, 계산 3.2초(콜드 로드 포함) |
| [적용] 클릭 후 UI가 막히는 시간 | 계산 시간 전체 | **0.07초** | **0.07초** |
| 화면 메쉬 정점 수 | 6,076,206 (219MB) | **약 17만 (600 상한)** / 안전 모드 약 6.9만 | 동일 |
| 화면 메쉬 생성 시간 | 0.57초 | **0.03~0.07초** | 동일 |
| 스냅샷 1개 | 12MB | **6MB** | 동일 |

정확도: v1.9.1과 같은 무작위 경로로 비교하면 높이 차이가 평균 0.01~0.03mm, 95%가 0.07mm 이하다.
(수직 벽 경계에서 그 칸이 잘렸는지 아닌지가 갈리는 셀 몇 개만 크게 차이 나는데, 이는 격자 맞춤 방식이 원래 가졌던 오차다.)

### 9.3 테스트

- 신규: `tests/test_nc_sim.py` 21건(반경 LUT, 배치 절삭 = 하나씩 절삭, 청크 분할 동일성, **numba = numpy 비트 단위 동일**, 소재 위/밖 건너뛰기, 취소, 표시 격자·좁은 홈 보존, 닫힌 인덱스 메쉬와 바깥 법선, 모서리 선, 색상 모드),
  `tests/test_nc_sim_viewer.py` 16건(선분 배열, 스레드 계산 = 동기 계산, **무작위 seek 20회 = 처음부터 계산**, 긴 프로그램 도중 연속 요청 8회 후 결과 일치, 오래된 세대 결과 폐기, 스냅샷 메모리 상한, 급속 경고 중복 없음, 셰이더 없는 메쉬와 모서리 선, 색상 모드 전환 시 재계산 없음, 밝은 색, STL, 팝업 상태).
- 전체 스위트: **415 passed, 1 skipped, 10 subtests passed** (v1.9.1 기준 378 passed).
- 실제 GL 화면 스크린샷으로 3가지 색상 모드와 모서리 선을 눈으로 확인했다.
- 참고: `tests/test_nc_tool_list.py`의 일부 테스트(11건)는 **실제 QSettings에 저장된 장비**가 선반이면 실패한다(v1.9.1 스냅샷에서도 똑같이 재현되는 기존 문제 — 메모리 `project_tests_share_real_qsettings`).
  새 테스트는 장비를 속성으로만 바꾸고 저장하지 않아 이 문제를 더 만들지 않는다.

### 9.4 남은 것 / 다음 후보 (이번 범위 아님)

- exe의 numba 포함 여부: 현재 제외(numpy 경로도 기존 대비 28배 빠름). 포함하면 numba 경로로 추가 4~5배이지만 +60MB.
- 직선 연속 구간을 하나의 선분으로 합치기(선분 수 자체를 줄임) — 3D 곡면 가공처럼 점이 촘촘한 프로그램에서는 효과가 작아 보류.
- 재생 중 실시간 절삭(결정 F는 지금처럼 공정 선택 시에만 유지).
