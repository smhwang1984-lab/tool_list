# v1.6.7 업데이트 플랜 (승인 대기)

원본 요구: `v1.6.7.md` (사용자 작성, 2026-09-05)

---

## 항목 1. 프로그램 단일 실행 (Single Instance)

**현재 상태**
- `NC_Tool_List.iss:49-55`가 `.nc/.mpf/.tap`을 이 앱에 연결해 둠 → NC 파일을 더블클릭할 때마다
  `NC_Tool_List.exe "<path>"`가 **새 프로세스**로 뜬다.
- `NC_Tool_List.py:3144 main()`에는 중복 실행 차단 로직이 전혀 없다.

**변경**
- `main()`에 `QLocalSocket`/`QLocalServer` 기반 단일 인스턴스 게이트를 추가한다.
  - 서버 이름: `SumPath.SingleInstance.<사용자명>` (사용자별 격리, 다중 세션 PC 대비)
  - **후속 실행**: 소켓 연결 성공 → `sys.argv`의 파일 경로를 UTF-8로 전송하고 즉시 `sys.exit(0)`.
  - **최초 실행**: 연결 실패 → stale 소켓 정리(`QLocalServer.removeServer`) 후 서버 listen.
    `newConnection` 수신 시 경로를 읽어 `window.load_file(path)` 호출.
  - **창 활성화**: 경로 수신 시 `showNormal()` → `raise_()` → `activateWindow()`로 기존 창을 앞으로.
    최소화 상태면 `setWindowState(state & ~Qt.WindowMinimized | Qt.WindowActive)`.
- 서버 생성 자체가 실패하면(권한/샌드박스) **기존처럼 그냥 실행** — 단일화 실패가 앱 실행을 막지 않게 한다.
- 이미 파일이 열려 있는 상태에서 새 파일을 받으면 **현재 문서를 교체**한다(앱이 단일 문서 구조).

---

## 항목 2-a. 장비 명칭 변경

| 기존 | 신규 |
|---|---|
| `5축 밀링 (A to C)` | `5축 MCT (A to C)` |
| `5축 밀링 (B to C)` | `5축 MCT (B to C)` |
| `2축 선반 (X Z 평면, X 2배)` | `CNC 선반 (턴밀 포함)` |

**손댈 지점**
- `nc_viewer_widget.py:100,108,109` / `NC_Tool_List.py:1270,1278,1279` — `DEFAULT_MACHINE_SPECS` 키
- `nc_viewer_widget.py:2354,2378,2379` — `"5축 밀링 (A to C)" in machine_type` 등 하드코딩 비교
- `tests/test_nc_tool_list.py:536,540,715,2539` — 테스트 문자열

**주의**
- `is_lathe_machine()`은 `"선반"` 키워드 판정(`nc_viewer_widget.py:122-127`)이라 `CNC 선반 (턴밀 포함)`도 그대로 선반으로 잡힌다. 로직 변경 불필요.
- **QSettings 마이그레이션**: 사용자 PC에 저장된 이전 장비 이름을 신규 이름으로 바꿔 읽는 매핑 테이블을 추가한다(없으면 업데이트 후 장비 선택이 초기화됨).

---

## 항목 2-b. 가공시간 계산 (신규 기능, 최우선)

현재 F(이송) 파싱 자체가 없으므로 **전부 신규**다. `parse_program()`(`nc_viewer_widget.py:2380~`)의
기존 좌표 계산 루프에 시간 누적을 얹는다.

### 공통 규칙
| 모션 | 유효 이송속도 |
|---|---|
| `G00` | **무조건 7000 mm/min** (F 무시) |
| `G01`, 세그먼트 길이 > 0.5mm | F × 100% |
| `G01`, 세그먼트 길이 ≤ 0.5mm | F × 70% |
| `G02` / `G03` | F × 70% |

- 세그먼트 시간(min) = 이동거리(mm) ÷ 유효 이송속도(mm/min)
- 원호는 이미 계산된 보간 세그먼트들의 현 길이 합을 쓴다(호 길이 근사, 기존 `ARC_CHORD_TOLERANCE_MM=0.05` 수준이라 오차 무시 가능).

### MCT(밀링)
- `F` = mm/min 직독, 모달 유지. G94 고정 취급.

### 선반(CNC 선반)
- **이송 단위 2갈래**
  - `G98`: F = mm/min → MCT와 동일
  - `G99`(선반 기본): F = mm/rev → 이송속도(mm/min) = **F × 현재 RPM**
- **회전수 2갈래**
  - `G97 S###`: 정회전(고정 RPM). 이후 `G50` 상한 이내로 유지.
  - `G96 S###`: 정속절삭(V, m/min). `V = D × π × N / 1000` → **N = V × 1000 / (π × D)**
    - D = 현재 X **지름**값(mm). D가 0에 가까우면 `G50` 최대 RPM으로 클램프.
  - `G50 S###`: 최대 회전수 상한 — G96/G97 모두 이 값을 넘지 못하게 클램프.
- **거리 계산**: 선반 X는 지름이므로 이동량은 **X/2(반경)** 로 본다. 기존 `lathe_world_point()`가
  이미 반경으로 변환해 두므로 그 월드 좌표 기준 거리를 그대로 쓰면 규칙과 일치한다.
- `G00`은 선반에서도 무조건 7000 mm/min.

### 데이터 구조 (신규)
- `self.line_to_elapsed_sec[line_idx]` — 프로그램 시작부터 그 줄까지 누적 초
- `self.process_time_sec[process_key]` — 공정별 소요 초
- `self.total_time_sec` — 전체 소요 초

### 화면 표기
1. **공정별 경로 필터 항목 끝** — `_tool_display_text()`(`nc_viewer_widget.py:2300-2317`) 반환값 뒤에
   ` | 12:34` 형태로 공정 시간 추가 → `공정 01 | T01 | D10 F.EM | 12:34`
2. **3D 화면 좌표 오버레이 끝** — `CoordOverlayWidget`(`nc_viewer_widget.py:1034~`)에 시간 라벨을 추가하고,
   `set_cursor_line()`(`:3034`)에서 `진행중 / 전체` 형태로 갱신 → `00:12:34 / 01:20:05`
   - 선반 모드의 하단 좌표 오버레이(`bottom_coord_widget`)에도 같은 라벨이 따라간다(같은 위젯 클래스).
3. **형식 가정**: 1시간 미만은 `MM:SS`, 1시간 이상은 `H:MM:SS`.

---

## 항목 3. 선반 공정별 경로 필터 중복 제거

**현재 문제**
`nc_viewer_widget.py:2422`의 `T(?!0000)(\d{2})00` 패턴은 공정 **시작**의 `T0100`과
공정 **끝(옵셋 취소)** 의 `T0100`을 구분 못 해, 한 공정이 필터에 2개로 뜬다.

**변경 (선반 분기 한정)**
- 활성 공정의 공구 번호를 기억하고, **같은 번호의 `Tnn00`이 다시 나오면 새 공정으로 만들지 않는다**(옵셋 취소로 간주).
- `M00` / `M01` / `M30`을 만나면 기억한 공구 번호를 리셋 → **같은 공구를 연속 두 공정에 쓰는 프로그램도 정상 분리**된다.
- 밀링(`M6 Tnn`) 판정 경로는 **전혀 손대지 않는다**(LATHE_MODE_GUIDELINES.md 0항 준수).

---

## 항목 4. 마우스 휠 줌 — 커서 위치 기준

**현재**: `OrthographicGLViewWidget.wheelEvent()`(`nc_viewer_widget.py:484-499`)가
`opts['distance']`만 곱해 **화면 중앙 기준**으로 줌.

**변경**
- 줌 직전 커서의 화면 좌표에 대응하는 지점이 줌 후에도 **같은 픽셀에 남도록** 팬 보정한다.
  1. 커서와 뷰 중심의 픽셀 오프셋 `(dx, dy)` 계산
  2. `distance` 변경 비율 `r = new_distance / old_distance` 산출
  3. `pan()`으로 `(dx, dy) × (1 - r)` 만큼 뷰 평면에서 중심을 커서 쪽으로 당김 (`relative='view'`)
- 직교/원근 투영 모두 동일 식으로 동작(`pan(relative='view')`이 두 경우를 이미 처리).
- `Ctrl+휠`(FOV 줌), `Alt+휠`(감도 조절)은 **기존 동작 유지**.
- 선반 `orbit_locked` 평면 뷰에서도 같은 보정을 적용한다.

---

## 항목 5. 버전 확정 및 회귀 방지

- `APP_VERSION`/`APP_BUILD_DATE`(`NC_Tool_List.py:29-31`), `NC_Tool_List.iss:5`,
  `version_info.txt` → **1.6.7**
- `tests/test_nc_tool_list.py` 신규 테스트
  - MCT/선반 시간 계산 (G00·G01·G02/G03·0.5mm 룰, G98/G99, G96/G97/G50 클램프)
  - 선반 `T0100 … T0100 M1` 이 공정 **1개**로 잡히는지
  - 커서 기준 휠 줌 팬 보정
  - 장비 명칭 변경 + QSettings 마이그레이션
  - 단일 인스턴스 게이트(소켓 헬퍼 함수 단위 테스트)
- `ABOUT_RELEASE_INSTRUCTIONS.md`에 v1.6.7 이력 추가
- **설치본/포터블 빌드는 사용자 승인 후 별도 수행** (기존 릴리스 절차와 동일)

---

## 작업 순서

1. 항목 2-b 가공시간 (요구서 "우선 수행") — 파싱 + 데이터 구조 + 표기
2. 항목 3 선반 공정 중복 제거 (같은 파싱 루프라 함께 처리)
3. 항목 2-a 장비 명칭 변경 + QSettings 마이그레이션
4. 항목 4 커서 기준 휠 줌
5. 항목 1 단일 인스턴스
6. 테스트 작성/전체 통과 → 버전 1.6.7 확정 → 문서 갱신

## 확정 사항 (2026-09-05 사용자 승인)

- 5축 명칭에서 `(A to C)` / `(B to C)` 구분은 **유지**한다 — 설정이 서로 다르므로.
- 시간 표기는 `MM:SS` / 1시간 이상 `H:MM:SS`.
- **F는 모달**이다. 한 번 나온 F는 이후 `G01`/`G02`/`G03`에 계속 같은 값으로 적용되며,
  `G00`을 만나는 구간만 F를 무시하고 7000 mm/min을 쓴다(G00이 F 모달값을 지우지는 않는다 —
  G00 뒤의 G01은 새 F가 없으면 직전 F를 그대로 이어 쓴다).
  F가 한 번도 안 나온 상태의 절삭 이동은 시간 0으로 둔다.
