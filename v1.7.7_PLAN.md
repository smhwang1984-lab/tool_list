# v1.7.7 플랜 — 선반 툴리스트에 SPINDL / FEED 2열 자동 입력

작성: 2026-09-07 / 상태: **승인 대기**
대상: 선반 툴리스트(`parse_lathe_program`, `LATHE_COLUMNS`, 선반 전용 PDF).
밀링 16열 툴리스트와 밀링 툴패스 계산은 손대지 않는다
(`LATHE_MODE_GUIDELINES.md` §0 완전 분리 원칙).

---

## 1. 요구사항 (사용자 지시, 2026-09-07)

> "선반 툴리스트에서 so와 remark 사이에 Spindle Feed 2가지 자동 입력 추가.
> 만약 F.1~.35면 F.1~F0.35, F1000.~F5000 이런 형태로 기입."

정리:

1. 선반 전용 열 구성에 **SPINDL / FEED 2열을 SO와 REMARK 사이에 추가**한다
   (밀링 16열의 `... 홀더, SPINDL, FEED, REMARK` 순서와 같은 배치).
2. 두 열은 **프로그램에서 자동으로 뽑아 채운다**(채운 뒤 표에서 수동 편집하는
   건 지금처럼 그대로 가능).
3. 값이 여러 개면 **"최솟값~최댓값" 범위 표기**로 적고, 각 값 앞에 `S` / `F`를
   붙인다. 예) `F.1~F0.35`, `F1000.~F5000`.

---

## 2. 현재 구조 (조사 결과)

| 항목 | 위치 | 비고 |
|---|---|---|
| 선반 열 스키마 | `NC_Tool_List.py:163 LATHE_COLUMNS` | 현재 5열 `NO/INSERT/HOLDER/SO/REMARK` |
| 선반 파서 | `NC_Tool_List.py:176 parse_lathe_program()` | N블록 단위로 홀더/인서트/SO/TOOL NO 수집 |
| 표 폭 | `NC_Tool_List.py:402 _LATHE_COL_WIDTH_BASE` | + 동적 폭 `_update_lathe_dynamic_col_width()` |
| 선반 PDF | `NC_Tool_List.py:626 LATHE_PDF_COLUMN_WEIGHTS` | 열 개수와 개수가 맞아야 함(테스트로 고정) |
| 표 생성/편집/복사 | `App.active_columns()` (`:2999`) | 전부 이 메서드를 거치므로 스키마만 늘리면 자동 반영 |

즉 **열 추가에 필요한 손질 지점은 4곳**(스키마 / 폭 / PDF 가중치 / 파서)이고,
화면 표·행 편집 대화상자·클립보드 복사·PDF 출력은 `active_columns()`를 쓰므로
자동으로 따라온다.

---

## 3. 값 추출 규칙 (핵심 설계)

N 블록(`N<번호>` 다음 줄부터 다음 `N` 전까지) 안에서, **주석을 걷어낸 코드
부분**(`code_without_comments()`)만 훑어 S 워드 / F 워드를 모은다.
주석을 반드시 걷어내야 한다 — 실제 파일에 `( T10 - S07J SWUBR 06-D08 )`,
`( = .7500-16UNJF 3A AS8879 = )` 처럼 S로 시작하는 인서트 문구가 있어
그대로 읽으면 S07 / S8879 가 주축 회전수로 잘못 잡힌다(O4812.nc, O2222.nc 실측).

### 3.1 SPINDL

- 수집 대상: 코드부의 `S<숫자>` 워드.
- **제외: `G50`(및 `G92`) 뒤에 오는 S** — 이건 주축 최고 회전수 클램프이지
  절삭 회전수가 아니다. 한 줄 안에서 `G50` 다음에 나오는 첫 S만 건너뛴다
  (`G50S1500`, `N1 G50 S2500 T0100 M08` 두 표기 모두 처리).
- 실측 예(O1699.nc N2): `G50S1500` / `G96S225M3P11` → **S225** 하나만 수집.

### 3.2 FEED

- 수집 대상: 코드부의 `F<숫자>` 워드 전부(직선 절삭 `G1 ... F`, 고정 사이클
  `G83 ... F`, 나사 `G76 ... F`(리드), 단독 모달 `F2500.` 줄 포함).
- 실측 예(O2222.nc N13): `F2500. / F2500. / F500. / F200. / F2500.`
  → **F200.~F2500.**

### 3.3 표기 형식

- 값이 **하나뿐이면** 단일 표기: `S800`, `F.07`
- **둘 이상이면** 숫자 크기 오름차순으로 `최소~최대`: `F.05~F4.`, `S302~S1000`
  (등장 순서가 아니라 값 크기 기준 — 사용자 예시 `F.1~F0.35`가 오름차순)
- **원문 표기를 그대로 보존**하고 접두어만 붙인다. 프로그램에 `F.1` / `F0.35`로
  적혀 있으면 `F.1~F0.35`, `F1000.` / `F5000.`이면 `F1000.~F5000.`
  (사용자 예시가 `.1`과 `0.35`, `1000.`과 `5000`을 섞어 쓴 것을
  "프로그램에 적힌 그대로"로 해석 — **결정 B** 참고)
- 값이 하나도 없으면 빈칸. 공구번호가 비는 행(v1.6.6 빈 행)도 빈칸.

### 3.4 같은 TOOL NO가 여러 N블록에 쓰일 때

기존 REMARK 누적(`N2, N3`) 규약과 동일하게 **모든 블록의 값을 합쳐서**
전체 최소~최대로 한 줄에 적는다.

---

## 4. 변경 파일 / 작업 목록

1. `NC_Tool_List.py`
   - `LATHE_COLUMNS` → `NO, INSERT, HOLDER, SO, SPINDL, FEED, REMARK` (7열)
   - `LATHE_SPINDLE_RE` / `LATHE_FEED_RE` / `LATHE_SPINDLE_CLAMP_RE` 신설
     (선반 전용, 밀링 정규식 재사용 금지 — 지침 §0-2)
   - `_lathe_word_range(values, prefix)` 헬퍼 신설 (3.3 표기 규칙)
   - `parse_lathe_program()`: 블록별 S/F 수집 → entry에 누적 → `_row_for()`에
     `SPINDL`/`FEED` 추가
   - `_LATHE_COL_WIDTH_BASE`에 `SPINDL`/`FEED` 폭 추가(각 100)
   - `LATHE_PDF_COLUMN_WEIGHTS` 7개로 조정 `[55, 250, 250, 45, 85, 90, 95]`
   - 선반 PDF 가운데 정렬 지정을 SO 단독 인덱스가 아니라
     `('SO', 'SPINDL', 'FEED')` 키 기준으로 확장
   - `APP_VERSION` → `1.7.7`
2. `tests/test_nc_tool_list.py`
   - 기존 `test_lathe_columns_schema_has_so_between_holder_and_remark` 갱신
     (7열 순서 검증)
   - 신규 5건:
     (a) 단일 S/F → `S800` / `F.07`
     (b) 여러 F → 오름차순 최소~최대 + 원문 표기 보존
     (c) `G50S1500` 클램프는 SPINDL에서 제외
     (d) 주석 속 `S07J`/`AS8879` 오검출 없음
     (e) 같은 TOOL NO 여러 블록 합산, S/F 없는 블록·빈 행은 빈칸
   - PDF 가중치 개수 테스트는 자동으로 7열을 따라감(수정 불필요)
3. `LATHE_MODE_GUIDELINES.md` — 툴리스트 열 구성 절에 SPINDL/FEED 규칙 추가
4. `ABOUT_RELEASE_INSTRUCTIONS.md` — v1.7.7 이력 추가
5. 회귀 확인: `pytest tests/test_nc_tool_list.py` 전체(현재 226건) 통과

---

## 5. 승인이 필요한 결정

| # | 결정 | 권장안 | 대안 |
|---|---|---|---|
| **A** | `G50 S1500`(주축 클램프)를 SPINDL에 넣을지 | **제외** — 절삭 회전수가 아님 | 포함(그러면 대부분 `S225~S1500`으로 보임) |
| **B** | 숫자 표기 | **프로그램 원문 그대로**(`F.1~F0.35`, `F1000.~F5000.`) | 정규화(`F0.1~F0.35`, `F1000~F5000`) |
| **C** | `G96`(주속 일정, m/min)과 `G97`(rpm) 구분 | **구분 없이 숫자만** — 표가 간결 | `G96 S85`처럼 라벨 붙임 |
| **D** | 접근/도피 이송(예 `G1Z2.F3.`)이나 나사 리드(`G76 ... F1.5875`)도 FEED 범위에 포함할지 | **포함**(코드상 F 워드 전부) — 절삭/접근을 코드만 보고 구분할 방법이 없음 | 특정 G코드만 수집(누락 위험) |
| **E** | `G98`(mm/min)/`G99`(mm/rev)가 한 공구에 섞였을 때 | **구분 없이 합쳐 최소~최대** | 단위별로 나눠 두 값 표기 |

> A~E 전부 권장안으로 진행해도 되는지, 바꿀 항목이 있는지 알려주시면
> 그대로 구현하겠습니다.
