# v2.2.0 플랜 — 선반 형상 시뮬레이션 (선삭 + 턴밀)

작성 2026-09-26 · 개정 2026-09-26(턴밀 포함 확정) · 상태: **구현 완료(M0·M1·M2)** (사용자 승인 2026-09-26: G·H 권장안, **I = 진입 벡터 방향으로 공구 축 판정, J = SO를 날장 최대로 인식**)
요청: "선반 시뮬레이션 플랜 구성해줘" → 결정 회신: **A 턴밀까지 한 번에**, B~F 권장안대로.
v2.1.0에서 만든 인서트·홀더 규격표와 R/T/PITCH/종류/방향 열을 쓰는 실제 시뮬레이션.

## 확정된 결정 (사용자 회신)

| | 결정 |
|---|---|
| A | **선삭 + 턴밀을 v2.2.0 한 릴리스에** (마일스톤 M1 선삭 → M2 턴밀 순서로 구현) |
| B | 툴리스트에 **`인선`**(가상 인선 번호 1~9) 열 추가 — 자동 추천 + [수정]에서 고침 |
| C | 나사는 **피치 간격 V홈 고리**로 근사 |
| D | 기본 보기 **3/4 단면**(전체·반 단면 전환 버튼 제공) |
| E | G41/G42 **인선 R 보정은 이번 범위 밖**(오차 안내 표시) |
| F | 형상 정보가 없는 공구는 **제외하고 진단 목록에 표시** |
| G | 턴밀이 있는 프로그램은 **통합 3D 복셀**로 표시(선삭만 있는 프로그램은 미세 회전체) |
| H | 턴밀 공구 지름·길이: 문구의 D 자동 읽기 + 태그 `[D ..]` + [수정] 창, 표에 **`D` 열** 추가 |
| **I** | **턴밀 공구 축은 "진입 벡터 방향"으로 판정한다**(평면 코드가 아니라 진입 이동의 방향 — 사용자 지시). 3D 엔진은 이동마다 축을 받으므로 그대로 쓸 수 있다 |
| **J** | **날 길이(FL)가 없으면 SO(툴리스트 SO 열)를 "날장 최대"로 인식한다**(사용자 지시). 밀링 엔진의 `cut_length = FL 또는 SO` 규칙과 같다 |

---

## 0. 조사 결과 (근거)

### 0-1. 실제 선반 샘플의 가공 방식 (`test files/`)

| 파일 | 줄 | G71/G72/G70 | G76 | M35(밀링) | G12.1 | G41 | 비고 |
|---|---|---|---|---|---|---|---|
| O1699.nc | 669 | 0 | 0 | 있음 | 0 | 3 | 선삭 5공정 + C축 드릴/밀링 4공정 |
| O2222.nc | 593 | 0 | 4 | 있음 | 1 | 0 | 나사 `16ER 16UNJ` G76 2회 + 홈 + 극좌표 밀링 |
| O4811.nc | 311 | 0 | 0 | 있음 | 0 | 0 | 널링, 레이디얼 드릴(G19/G87), 페이스커터 |
| O4812.nc | 478 | 0 | 0 | 있음 | 1 | 6 | 소형 보링(WBGT), 극좌표 밀링(`MTI … A60`) |

- **복합형 사이클(G71/G72/G70)은 쓰지 않는다.** CAM이 황삭까지 G0/G1/G2/G3로 풀어 쓴다 → 뷰어 경로를 그대로 깎으면 된다.
  (뷰어는 G70~G76·G90/G92/G94·U/W 증분을 원래 전개하지 않는다 — §8 기존 규약.)
- **G76 나사가 있다**(O2222: `G76X17.12Z-9.2P914Q120R0.F1.5875`). 뷰어는 G76 둘째 줄을 그 직전 모달(G00)로 기록해 **시작점→(Z-9.2, X17.12)의 급속 사선**이 된다
  (실측: O2222 나사 공정의 절삭 이동 0개). 그대로 두면 ① 나사가 전혀 깎이지 않고 ② 이 사선이 소재를 지나가 **급속 절삭 경고가 잘못 뜬다**
  → 파서가 G76/G32 줄에 표시를 남기고, 엔진이 그 이동을 **나사 구간으로 대체**한다(급속 경고 제외).

### 0-2. 뷰어의 선반 경로 데이터

- 경로 점 = 월드 `(Z, Y성분, X/2)` — 선삭은 `(Z, 0, 반경)`, C가 있으면 주축(월드 X) 둘레로 회전(`lathe_world_point`, `lathe_rotate_c`),
  극좌표(G12.1)는 뷰어가 이미 월드 좌표로 변환한다. → 단면 좌표는 `z = pt[0]`, `r = hypot(pt[1], pt[2])`, 턴밀은 월드 좌표를 **그대로** 3D 엔진에 줄 수 있다.
- 경로 노드에 **"이 이동이 M35 밀링 구간인가"·"어느 평면(G17/G19)인가"** 표시가 없다(`lathe_milling_active`·`current_plane`은 파싱 중 지역 변수).
  → 선반 분기에서만 노드에 표시를 추가해야 한다.
- 현재 `is_sim_available()`은 선반이면 무조건 `False`. 밀링 시뮬레이션(`zmap`/`3d`, 백그라운드 계산·스냅샷·되돌리기)은 밀링 전용.

### 0-3. v2.1.0 규격 해석기가 실제 샘플에서 놓치는 것 (실측)

| 실제 문구 | 지금 결과 | 문제 |
|---|---|---|
| `CNMG 12 04 08`, `VCMT 16 04 04` (띄어 쓴 코드) | ISO 해석 실패 | 형상·날끝각을 모름(R은 `R-0.8` 문구로만 채워짐) |
| 홀더 `S16R STFPR 11`, `E08K STFPR 09`, `S07J SWUBR 06` | 종류 **외경** | 실제는 **내경 보링 바**(대시 없는 바 접두어를 못 읽음) → 반대쪽을 깎게 된다 |
| `16ER 16UNJ` | 나사로 인식 못함, 피치 빈칸 | `UNJ` 표기 미지원(피치 = 25.4/16 = 1.5875 = 프로그램 F와 일치) |
| `D1.5 CENTER` | 종류 빈칸 | 센터 드릴 키워드 없음 |
| `ROLLE NULLING`, `D10.SETTING PIN`, `MILL TOOL CHECK` | 종류 빈칸 | **깎지 않는 공구**를 구분할 방법이 없음 |
| `WBGT 060102` | 내접원 9.525 | 소형 트라이곤 규격은 카탈로그 값과 다를 수 있음 — **확인 필요** |
| `MINTR07-140015D050` | 전부 빈칸 | 제조사 전용 소형 내경 홈 인서트 — 직접 입력 대상 |

### 0-4. 턴밀 측정 결과 (실측, 이번 개정에서 추가)

**소재·규모**: 선삭 절삭 범위 — O1699 Z[−36.8, 2.6] r 100.5(Ø201), O2222 Z[−18.7, 10] r 50(Ø100), O4811 Z[−55, 3] r 44.8(Ø90), O4812 Z[−14.6, 10] r 50(Ø100).
→ 부품은 **Ø90~200 × 길이 20~60mm**. 턴밀 공정은 프로그램당 1~4개, 경로 점 20~760개(소규모).

**턴밀·중심 공정과 공구 문구**

| 문구 | 실제 가공 | 평면 코드 | 엔진 처리 |
|---|---|---|---|
| `D50.0 X H103 T-DRILL`, `D12 CARBIDE DRILL`, `D8.5 CARBIDE DRILL`, `D1.5 CENTER` | X0 중심선에서 Z만 이동(M35 없음) | G18 | **M1 축대칭**(주축 중심 구멍) — 3D 필요 없음 |
| `D10 X 90 NC DRILL` (O1699 T12) | C 분할 정면 드릴(`G83 … C90/C60/…`) | G17 | M2 축방향 드릴 |
| `D5.5 CARBIDE DRILL, ANGLE`, `D4.5 …, ANGLE`, `D3. FLAT END MILL, ANGLE` (O1699) | C 분할 정면 드릴/밀링 | **G17** | M2 축방향 |
| `D10.2 CARBIDE DRILL, STRAIGHT` (O4811 T04) | 레이디얼 드릴(`G87X-5.225Z-16.51 …`) | **G19** | M2 반경방향 |
| `D50. FACE CUTTER, STRAIGHT` (O4811 T12) | 옆면 페이스 밀링(Y 이동) | G19 | M2 반경방향 |
| `D16 FLAT END MILL, ANGLE` (O2222 T04), `MTI 0808 D30 A60 MT8, STRAIGHT` (O4812 T12) | 극좌표 밀링(`G12.1`) | G17 | M2 축방향(월드 좌표는 뷰어가 변환) |
| `D10.SETTING PIN`, `ROLLE NULLING` | 세팅·널링 | — | **비절삭**(제외) |

> 정정: `MILL TOOL CHECK`는 비절삭이 아니라 **턴밀(구동공구) 공정의 홀더 주석 표식**이다(구동공구 전부에 붙어 있음). 종류를 가리지 않는다.

> **핵심 발견 — 문구의 `ANGLE`/`STRAIGHT`는 공구 축 방향과 무관하다.** `ANGLE` 공구가 G17(축방향)에서, `STRAIGHT` 공구가 G19(반경방향)에서 돈다.
> → 공구 축은 문구가 아니라 **진입 벡터 방향**(결정 I)으로 판정한다. 평면 코드(G17=축방향, G19=반경방향)는 진입 이동이 애매할 때의 보조 근거로만 쓴다.
> **공구 지름**은 문구의 `D<숫자>`에서 읽을 수 있다(전 샘플 공통). **날 길이(FL)는 어디에도 없다.**

---

## 1. 범위와 마일스톤 (한 릴리스, 두 단계로 구현)

| 마일스톤 | 내용 | 검증 |
|---|---|---|
| **M0** 해석기 보강 | §2-0 | 실제 샘플 문구 표를 테스트로 |
| **M1** 선삭 | 외경·내경 선삭, 정면, 홈, 절단, 중심 드릴(X0), G76/G32 나사 — **축대칭 엔진** | 알려진 형상 정답 비교, O1699/O2222/O4811/O4812 선삭 공정 |
| **M2** 턴밀 | M35 구간의 축방향/반경방향 드릴·엔드밀·페이스커터·극좌표 밀링 — **기존 3D 복셀 엔진 재사용** | O1699 정면 드릴 C 분할 구멍 위치, O4811 레이디얼 구멍·페이스커터 |

- 각 마일스톤이 끝날 때마다 커밋하고 전체 테스트를 돌린다. 릴리스(v2.2.0)는 M2까지 끝낸 뒤 한 번에 빌드한다.
- **위험 완충**: M2에서 예상 못 한 문제가 크게 나오면, M1까지를 v2.2.0으로 먼저 내고 M2를 v2.3.0으로 미루는 안을 그때 다시 여쭙는다(임의로 바꾸지 않는다).

---

## 2. 설계

### 2-0. 규격 해석기 보강 (`lathe_insert_spec.py`, M0)
- 띄어 쓴 ISO 코드(`CNMG 12 04 08`) 인식.
- 대시 없는 보링 바 접두어(`S16R STFPR`, `E08K`, `S07J`, `A32S` …) → 내경.
- 나사 표기 `UNJ`, `UNC`, `UNF`, `NPT` 등 산/inch 계열 확장.
- 종류 확장: `센터`→`드릴`, **`엔드밀`, `페이스커터`, `비절삭`**(SETTING PIN·NULLING·ROLLE·TOOL CHECK).
- 문구에서 **공구 지름 `D<숫자>`**, 공구 종류 키워드(DRILL/CENTER/FLAT END MILL/BALL/FILLET END MILL/FACE CUTTER/CHAMF …)를 읽는다.
- `WBGT`/소형 트라이곤 크기: 표준과 대조해 확인하고, 확인 못 하면 "불확실"로 두고 형상에 쓰지 않는다.

### 2-1. 축대칭 엔진 `nc_lathe_sim.py` (M1, 새 파일, Qt 비의존, 선반 전용)
- **소재 표현**: 반단면 점유 격자 `(nz × nr)` — z = 주축 방향, r = 반경. 칸마다 "남아 있음"과 "마지막으로 깎은 공정 색 id".
  한 z에 소재 구간이 여러 개인 형상(외경/내경 홈·언더컷·파이프)도 그대로 표현된다.
- **해상도**: 소재 크기에서 자동(칸 약 400만 목표, 0.02~0.2mm). Ø100×60 소재 → 약 0.04mm.
- **공구 형상(단면 다각형)**: v2.1.0 형상 맵(`geometry_map_from_rows`)에서 만든다.
  - 선삭 인서트: 날끝각 + 노즈 R + 홀더 접근각으로 만든 "노즈 원이 붙은 쐐기".
  - 홈·절단: 폭 T + 코너 R의 사각형. 나사: 나사각(60°/55°) V + 선단 R. 드릴/센터(X0): 반경 D/2 + 선단 원뿔(기본 118°).
  - 깎는 부분만(인서트) 모델링, 홀더 몸체는 이번 범위 밖.
- **공구 자세 = 가상 인선 번호(Fanuc 1~9, 결정 B)**: 종류(외경/내경) + 방향(R/L) + 실제 경로의 주 절삭 방향(G01 이동이 주로 −Z이면 척 쪽으로 깎는 공구)에서 자동 추천하고 툴리스트 `인선` 열에서 고칠 수 있다.
- **제어점**: 프로그램 좌표 = 가상 인선점. G41/G42 구간은 보정하지 않는다(결정 E) → 테이퍼·원호에서만 최대 `R·(1−cos θ)` 오차(R0.8 45° 테이퍼에서 약 0.23mm). 진단에 안내.
- **절삭**: 선분마다 공구 다각형을 시작→끝으로 쓸어낸 영역(볼록 껍질)을 격자에 칠해 제거. 원호는 뷰어가 점으로 나눠 준다. numba 커널(배포에 이미 포함) + numpy 대체 경로(결과 동일).
- **G76/G32 나사(결정 C)**: 뷰어의 사선 이동은 깎지 않고, 시작점·끝 X(골 지름)·끝 Z·P(나사산 높이, µm)·F(리드)로 나사 구간을 따로 만든다.
  축대칭 단면이라 나선은 못 그리므로 **피치 간격의 V홈 고리로 근사**한다. G32도 같은 방식.
- **급속(G00) 절삭 경고**: 급속이 소재를 깎으면 줄 번호와 함께 목록으로 알린다(밀링 시뮬레이션과 같은 방식) — 선반 충돌 사전 점검에 가장 쓸모 있는 기능.
- **진단**: 형상 정보가 없는 공구는 제외하고 공구 번호를 알린다(결정 F). 비절삭 공구, U/W 증분·G90/G92/G94 사용 줄, G18 평면의 M35 밀링(미지원)도 개수를 알린다.

### 2-2. 표시 — 선삭만 있는 프로그램 (M1)
- **회전체 메쉬**: 격자의 소재 경계를 등고선(marching squares)으로 뽑아 주축 둘레로 돌린다 — 미세 형상(노즈 R, 나사산)이 그대로 보인다.
- **보기 방식(결정 D)**: 기본 **3/4 단면**(앞쪽 1/4을 잘라 내경·홈이 보임) / 전체 / 반 단면. 색은 공정 색·깎인 깊이·단색, 조명 없는 밝은 단색 + 모서리 선(기존 표시 원칙, v2.0.2 선명 경계).
- 화면 삼각형 상한을 두고 넘으면 둘레 분할 수·등고선 간격을 줄인다(저사양 PC, 그래픽 안전 모드).

### 2-3. 소재 입력 — 선반 전용 창 (M1)
- 소재 **지름**, **길이**, **앞면 Z 위치**(기본 0, 소재는 −Z 쪽), **내경**(0 = 봉재).
- 밀링 소재 설정과 **따로 저장**(`EmbeddedViewer/lathe_stock`). v1.9.3 원칙 그대로 **입력값 그대로 쓰고**, [툴패스 범위에 맞추기]를 눌렀을 때만 맞춘다.

### 2-4. 뷰어 연결 (선반 분기 안에서만 — 지침 §0)
- 선반 파싱 분기에서만 경로 노드에 `lathe_mill`(M35 구간), `lathe_plane`(G17/G18/G19), `lathe_thread`(G76/G32 정보)를 추가한다.
- `is_sim_available()`에 선반 분기를 추가하고 `sim_engine = 'lathe'`(선삭) / `'lathe3d'`(턴밀 포함)로 연결한다. 백그라운드 계산·취소·진행률·공정별 스냅샷·되돌리기·재생 위치 연동은 밀링 v1.9.2 구조를 **선반 전용으로 본떠** 만든다(밀링 코드 경로는 수정하지 않는다).
- 밀링 테스트 전체가 그대로 통과하는지 매 마일스톤 확인(지침 §0-3).

### 2-5. 툴리스트 열 (M0~M1)
- 현재 12열 → **`인선`**(결정 B)과 **`D`(지름, 밀링 공구용, 결정 H)** 를 추가해 14열(M0에서 구현됨). 순서: `… 종류 / 방향 / 인선 / D / SPINDL …`.
  - **인선 번호 규약**(화면에서 X 위, Z 오른쪽 기준, 노즈 중심에서 가상 인선 방향): 1 우상 / 2 우하 / 3 좌하 / 4 좌상 / 9 중심.
    외경 우수 = 3, 외경 좌수 = 2, 내경 우수 = 4, 내경 좌수 = 1로 자동 추천(홈은 기준 모서리를 몰라 추천 안 함 — 직접 지정).
    **이 규약은 제가 가정한 것이므로 사용자 확인이 필요하다**(표 한 곳에서 바꿀 수 있게 만든다).
- `D`는 문구의 `D<숫자>`를 자동으로 읽고, 태그 `[D 10] [FL 25]`와 [수정] 창 입력을 지원한다(v2.1.0의 R/T/PITCH와 같은 우선순위·저장 규칙). 종류 콤보에 `엔드밀`·`페이스커터`·`비절삭` 추가.

### 2-6. 턴밀 (M2) — 축대칭 격자 + 3D 복셀 하이브리드

**핵심 통찰**: 재료 제거는 순서와 무관하게 최종 형상이 같다(교환 법칙). 선삭 = 축대칭 제거, 턴밀 = 3D 제거 → 따로 계산해 합치면 된다.

- **구조**: ① 축대칭 격자 A(§2-1, 진실 원천, 미세) + ② 3D 복셀 V(**기존 `nc_sim3d.VoxelStock` 재사용** — `cut_batch`·표면 넷·색·압축 스냅샷).
  턴밀 절삭이 하나라도 소재와 겹치는 프로그램만 V를 만든다(없으면 M1 그대로, 추가 비용 0).
- **선삭 → V 반영**: 선삭 공구 공정이 끝날 때마다 **A를 V에 한 번에 동기화**한다(`V.occ &= A[z칸, r칸]`, 바뀐 z 범위만).
  선분마다 하지 않는 이유: 선분 수천 × 환형 영역 200만 복셀 = 60억 연산(불가) vs 공정마다 한 번 = 복셀 2000만 × 공정 20 = 4억 연산(numba 1~2초).
- **턴밀 → V**: 뷰어가 이미 월드 좌표로 만든 이동(C 회전·극좌표 변환 포함)을 `cut_batch`에 그대로 넣는다. 공구 자세는 **평면 코드**로:
  - **진입 벡터(결정 I)**: 턴밀 공정의 각 "가공 묶음"(급속 접근 뒤 첫 절삭 이송부터 다음 급속 후퇴까지)에서 **진입 이동(첫 절삭 이송)의 방향 벡터 e**를 구하고,
    **공구 축(팁→홀더) = −e**(진입 반대 방향)로 둔다. 드릴·엔드밀의 플런지 이동이 곧 축 방향이다. 진입 이동이 짧거나 축과 어긋나는 경우(측면 진입)는 평면 코드
    (G17 = +월드 X, G19 = 접근점의 바깥쪽 반경 방향)로 보정하고 진단에 표시한다. **문구의 ANGLE/STRAIGHT는 쓰지 않는다**(§0-4 실측). G18 + M35는 이번 범위 밖.
- **공구 형상**: 종류·지름은 툴리스트(`종류`·`D`)에서 `nc_sim.tool_shape_from_values`로 만든다(드릴 선단각 기본 118°, 밀링과 같은 기본값).
  **날 길이 FL은 어디에도 없으므로 툴리스트 `SO`를 날장 최대로 쓴다(결정 J)**. SO도 없으면 "무제한"으로 취급하고 진단에 안내한다
  — 프로그램이 정상이면 결과는 같고, 충돌(샹크가 소재를 밀고 들어가는 경우)만 못 잡는다.
- **표시 — 턴밀이 있는 프로그램(결정 G)**: 통합 **3D 복셀 메쉬**(기존 밀링 5축 표시와 같은 표면 넷 + 모서리 선 + 공정 색). 해상도는 소재 크기에서 자동(약 2,000만 복셀).
  예: Ø100×60 → 약 0.31mm, Ø201×40 → 약 0.43mm — 해상도 콤보로 조절(최대 6,000만 복셀).
  **한계(안내 표시)**: 이때 선삭 세부(노즈 R 0.4, 나사산)는 복셀 크기로 뭉개진다. 선삭만 있는 프로그램은 미세 회전체를 그대로 쓴다.
  (미세 회전체 + 복셀 합성은 이음새와 불리언 문제로 채택하지 않는다 — 위험 큼.)
- **스냅샷·재생**: A(작음)와 V(압축)를 함께 저장해 공정 선택·재생 위치 되돌리기를 밀링과 같게 지원.
- **색**: 공정 색. 급속 절삭 경고는 선삭·턴밀 통합 목록.

---

## 3. 성능 목표 (측정 전 목표치)

- 선삭만: 샘플 규모(수백~수천 줄) **1~2초 이내**. 축대칭 격자 약 400만 칸(메모리 약 8MB).
- 턴밀 포함: V 약 2,000만 복셀(점유+색 40MB, 압축 스냅샷 수 MB), A→V 동기화 20회 1~2초, 턴밀 절삭 numba 수 초.
- 3/4 단면 회전체 메쉬 삼각형 50만 개 이하, 통합 복셀 메쉬는 기존 상한(표면 사각형 70만 개).

## 4. 테스트

- **M0**: 실제 샘플 문구 표(§0-3, §0-4)를 그대로 테스트로(띄어 쓴 코드·보링 바·UNJ·센터·비절삭·D 읽기).
- **M1 엔진**: 알려진 형상 정답 비교 — Ø50 봉 외경 Ø40 선삭 길이·깊이, 폭 3 홈 깊이, X0 드릴 구멍 지름·선단각, 노즈 R 모서리, 급속 절삭 경고, numba/numpy 동일.
- **G76**: O2222 실사례(`X17.12 Z-9.2 P914 F1.5875`) — 골 지름 17.12, 피치 1.5875, 사선으로 깎이지 않음.
- **M2**: 축대칭 → V 동기화(A와 V 점유 일치, 동기화 전후 선삭 부분 불변), 정면 드릴 C 분할 위치(O1699 T12: 각도 30° 간격 구멍), 레이디얼 드릴(O4811 G87) 구멍 위치·방향, 페이스커터 평면, 극좌표 밀링(O2222/O4812), 교환 법칙(선삭↔턴밀 순서를 바꿔도 최종 점유 같음).
- **뷰어**: 선반 모드에서 켜기·끄기, 공정 선택/재생 위치 따라 되돌리기, 진단 문구, 밀링 모드 불변, 밀링 테스트 전체 통과.
- 실제 샘플 O1699/O2222/O4811/O4812 실행 결과를 이미지로 확인(육안 확인은 설치 후 사용자 확인 병행).

## 5. 이번 개정에서 새로 정해야 할 결정 (★ 추천)

- **G. 턴밀 프로그램의 표시 방식**: ★통합 3D 복셀(구현 단순·안정, 선삭 세부 해상도가 복셀 크기로 낮아짐; 선삭만 있는 프로그램은 미세 유지) / 미세 회전체 + 복셀 합성(미세 유지, 이음새·불리언 위험으로 비추천).
- **H. 턴밀 공구 지름·길이 입력**: ★문구의 `D` 자동 읽기 + 태그 `[D ..] [FL ..]` + [수정] 창, 표에 `D` 열 추가 / 열 없이 문구·태그만.
- **I. 턴밀 공구 축 판정**: **확정 — 진입 벡터 방향**(위 §2-6).
- **J. 날 길이(FL) 미상 공구**: **확정 — SO를 날장 최대로**(SO도 없으면 무제한 + 안내).

## 6. 위험과 한계 (미리 알려 둘 것)

1. **턴밀 프로그램의 선삭 세부 해상도가 낮아진다**(복셀 0.3~0.4mm). 노즈 R0.4·나사산은 화면에서 뭉개진다 — 결과 정확도가 아니라 표시 해상도 문제이며, 선삭만 있는 프로그램은 영향 없음.
2. **G41/G42 미보정 오차**(테이퍼·원호에서 최대 R·(1−cosθ)).
3. **나사는 나선이 아니라 V홈 고리 근사**.
4. **홀더·터릿·척 간섭은 검사하지 않는다**(공구 인서트 형상만).
5. 이번 규모는 v1.9.0(밀링 시뮬) + v1.10.0(3D 엔진)을 합친 것보다 크다 — 한 릴리스에 M1·M2를 다 넣는 선택(A)이므로 §1의 후퇴안(M1만 먼저 릴리스)을 열어 둔다.
6. 실제 GL 화면·표시 품질은 오프스크린 환경에서 확인이 안 되므로 설치 후 사용자 확인이 필요하다.

## 6-1. 구현 중 확인·변경한 것 (2026-09-26)

- **G19 법선**: 공구 위치의 반경 방향이 아니라 **C 각도의 기계 X 방향**(O4811 D50 페이스커터 Y-34.273 옆면 가공에서
  비스듬한 축으로 잘못 보정되던 것 — 이 때문에 계산도 45초 걸렸다. 수정 후 3.4초).
- **급속 축**: 두 가공 묶음 사이의 후퇴·중간 급속은 앞 묶음 자세, 마지막 접근만 뒤 묶음 자세(드릴 후퇴가 옆으로 누운
  자세로 계산돼 급속 경고가 잘못 뜨던 것).
- **G76 뒤 시작점 복귀**, **급속 경고 0.5mm² 기준**, **나사 V홈 높이 = 나사산 높이 ×2**(어깨를 깎아 먹지 않게).
- **실측**(이 PC, numba): O1699 4.8초 / O2222 6.2초 / O4811 3.4초 / O4812 0.5초(선삭만). 표시 메쉬 0.1초 안팎.
- **열린 문제**: 선반 드릴 고정 사이클 깊이 해석(선반 지침 §12 끝) — 사용자 결정 필요.

## 7. 작업 순서 (승인 후)

1. M0 해석기 보강 + `인선`·`D` 열 → 테스트 → 커밋
2. M1 축대칭 엔진 + 선반 소재 창 + 뷰어 연결 + 회전체 표시 → 테스트 → 커밋
3. M2 노드 표시(평면) + A→V 동기화 + 턴밀 절삭 + 통합 표시 → 테스트 → 커밋
4. 전체 테스트, 코드 리뷰, 릴리스 이력, v2.2.0 빌드(설치 파일·포터블), 커밋·push (병합은 별도 요청 시)
