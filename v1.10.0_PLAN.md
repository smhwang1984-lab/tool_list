# v1.10.0 플랜 — 형상 시뮬레이션: G68.2(3+2 경사면) · G43.4(동시 5축) 가공 표현

작성: 2026-09-24 / 상태: **구현 완료**(§9) — 사용자가 샘플 `O3210.NC`와 프로그램 규약을 주어 §7 결정은 ★권장안으로 진행.
기반: v1.9.3 (브랜치 `worktree-v190-sim-plan`).
대상: `nc_viewer_widget.py`(파서에 공구 축 기록, 엔진 선택, 표시), 신규 `nc_sim3d.py`(3D 소재 엔진, Qt 없음),
`nc_sim_numba.py`(가속 커널 추가), `NC_Tool_List.spec`/설치본(numba 포함 여부 — 결정 C), `tests/`.
**밀링 모드 전용** — 선반은 손대지 않는다(LATHE_MODE_GUIDELINES.md). 3축 프로그램은 지금의 Z-map 경로를 **그대로** 쓴다.

---

## 1. 요구사항 (사용자 지시, 2026-09-24)

> "일반 3축가공에서도 모델이 깍이지 않음" → (v1.9.3에서 원인 진단 표시 추가)
> "추가로 g68.2 g43.4 가공에 대해서도 시물레이션 표현 필요함"

지금 v1.9.3은 G68.2 구간을 **통째로 제외**한다(예: `ncdata.nc`는 이동 32,416개가 전부 경사면 구간이라 아무것도 안 깎임).
G43.4(공구 중심점 제어, 동시 5축)는 뷰어가 모드 자체를 모른다. 두 가지 모두 소재를 깎아 보여 줘야 한다.

---

## 2. 조사 결과 (지금 코드/프로그램 기준)

| 항목 | 확인한 사실 |
|---|---|
| G68.2 좌표 | 뷰어가 이미 경사면 로컬 좌표를 `active_matrix @ 로컬좌표`로 **월드(파트) 좌표로 바꿔** 노드 `pt`에 넣는다(`nc_viewer_widget.py` 파서). 회전 규칙은 5축 A→C 장비에서 `Rz(K)·Rz(I)·Rx(J)` — **I=C축, J=A축**. |
| 프로그램과의 일치 | `ncdata.nc`의 `A-90. C0.` → `G68.2 … I0. J-90.`, `A-90. C-90.` → `I-90. J-90.`, `A-90. C20.` → `I20. J-90.` — C=I, A=J로 항상 짝이 맞는다. 즉 **공구 축(월드) = Rz(C)·Rx(A)·(0,0,1)**. |
| 빠진 것 | 노드에는 **공구 축 방향이 없다**(위치만 있음). 그래서 Z-map(공구가 항상 +Z)은 경사면 구간을 못 깎는다. |
| G68.2 X Y Z(원점) | 뷰어가 **무시**한다(`ncdata.nc`는 전부 0이라 문제없음). 0이 아닌 프로그램은 화면 경로도 어긋난다. |
| G43.4 | 뷰어가 인식하지 않는다(`ncdata.nc`에는 없음). G43.4에서 X/Y/Z는 **공구 끝점(TCP)** 이라 지금 노드 위치와 같고, **공구 방향은 A/C 값**에서 나온다. 뷰어는 A/C 단어를 읽지 않는다. |
| 성능 프로토타입 | 3D 복셀에 기울어진 공구(D10, 날장 25)를 1.5mm 선분 3,000개로 절삭: 복셀 0.5mm(410만) **0.24ms/선분**, 0.3mm(1,860만) **0.81ms/선분**, 0.25mm(3,230만) **1.39ms/선분**(numba, 이 PC). 3만 선분이면 8~26초 — 백그라운드 계산으로 충분. |
| numba 크기 | 설치 상태 numba 28MB + llvmlite 122MB. exe에 넣으면 배포 폴더가 크게 늘어난다(§7 결정 C). C 컴파일러(gcc/cl)는 이 PC에 없어 C DLL 대안은 불가. |

---

## 3. 설계

### 3.1 파서: 노드마다 공구 축 기록
- 5축 장비(A→C, B→C)에서 **모달 A/B/C 값**을 읽는다(`G28 … A0 C0` 같은 원점복귀 줄, 주석 속 글자는 제외).
- **공구 축 결정**(우선순위): ① G68.2+G53.1 경사면 활성 → `active_matrix · (0,0,1)` ② G43.4 활성 → `Rz(C)·Rx(A)·z`(B→C 장비는 `Rz(C)·Ry(B)·z`) ③ 그 외 `(0,0,1)`.
  G43.4는 G49(취소)와 G43(일반)에서 해제한다.
- 기록 방식은 줄 번호별 `line_axis_map[줄] = (ax, ay, az)`(축이 +Z가 아닐 때만). 노드의 기존 `src_line`으로 조회하므로 **노드 생성부 수십 곳을 건드리지 않는다.**
- G68.2의 **원점 X Y Z**를 받아 `월드 = 원점 + R·로컬`로 변환한다(지금은 무시). ※ 원점이 0이 아닌 프로그램은 **화면 경로도 바뀐다**(맞게 고쳐지는 변화) — 결정 G.
- 장비/툴패스/PDF/공구리스트 결과는 그대로다. 기존 `tilt`(경사면 제외) 플래그는 "3D 엔진 필요" 플래그로 바꾼다.

### 3.2 엔진 선택 — 프로그램마다 자동
- **공구 축이 +Z가 아닌 이동이 하나도 없으면**(순수 3축): 지금의 Z-map(고해상도 ~100만 셀, 빠름) **그대로**.
- **하나라도 있으면**(3+2, 동시 5축, 혼합): 새 3D 소재 엔진을 프로그램 **전체**에 쓴다(3축 공정도 같은 소재에 이어서 깎여야 하므로).
  소재 팝업/화면에 "3D 소재 방식(경사면/5축 포함)"을 알린다.

### 3.3 3D 소재 엔진 `nc_sim3d.py`
- **표현**: 격자 점유(uint8, 1=소재) + 절삭면 공구 색 id(int8). 해상도는 **자동으로 전체 복셀 ≤ 약 2,000만**(소재 100×100×50이면 약 0.35mm), 팝업에서 0.2~1.0mm 수동 선택 가능. STOCK 범위는 지금과 같이 사용자 지정 그대로.
- **절삭 커널**(numba, 프로토타입과 동일 산식): 선분(공구 끝 A→B)과 그 구간의 공구 축 `a`에 대해, 복셀 중심 p마다
  `s = (p−A)·a`(축 방향 높이), `d = 축에 수직한 평면에서 선분까지의 거리`를 구하고,
  `h(d) ≤ s_max` 이고 `s_min ≤ 날장`이면 제거. `h`는 지금의 공구 형상 LUT(평/볼/불노즈/드릴/챔퍼)를 그대로 쓴다.
  축 방향 이동(플런지)이 큰 선분은 지금처럼 잘게 나눠(조각당 축 방향 변화 ≤ 격자 간격/4) 처리한다.
- **동시 5축(G43.4)**: 선분마다 끝점 방향을 쓰고, 끝점 사이 각도 변화가 2°를 넘으면 위치·축을 보간해 조각으로 나눈다.
- **numpy 대체 경로**: 같은 산식을 bbox 블록 벡터 연산으로 구현(결과 동일 검증). 속도는 numba의 수십 분의 1이라 exe에 numba를 넣지 않으면 **해상도를 자동으로 거칠게(약 0.6mm)** 잡고 진행률을 보여 준다.
- **날장/홀더**: 절삭 길이는 **FL(날장)**, 없으면 SO(돌출)를 쓴다. 홀더 충돌 표현은 이번에도 범위 밖.
- **스냅샷**: 공정 시작마다 저장하되 점유·색을 **zlib(레벨 1)** 로 압축해 메모리 상한(200MB) 안에 유지한다(2,000만 복셀 기준 1개 수 MB).
- **백그라운드 계산·취소·진행률·급속 절삭 경고**: v1.9.2 구조(워커가 복사본만 깎고 끝나면 UI가 넘겨받음)를 그대로 재사용한다.

### 3.4 표시
- 점유 격자에서 **표면 메쉬**를 뽑는다(surface nets — 계단 없이 평평한 면은 평평하게, 경사 면은 완만하게). 조명은 여전히 없다(밝은 단색 채움).
- **모서리 선**: 이웃 면의 법선 각도가 약 35° 넘게 꺾이는 곳 + 소재 외곽. 계단 노이즈 없이 진짜 모서리만 그린다.
- 색상 모드(공구 색/깎인 깊이/단색), 표시 격자 상한, 안전 모드 설정은 v1.9.2 규칙 그대로. 표면 삼각형이 상한(약 60만)을 넘으면 표시 전용으로 한 단계 거칠게 뽑는다.
- STL 내보내기: 같은 표면 메쉬(전체 해상도)로 저장.
- **공구 축 확인용 표시**(선택): 재생 위치의 공구 축을 짧은 선으로 보여 회전 규칙이 맞는지 눈으로 검증할 수 있게 한다(결정 G의 안전장치).

### 3.5 파일 · 빌드
| 파일 | 변경 |
|---|---|
| `nc_sim3d.py` (신규) | 점유 격자, 표면 메쉬, 스냅샷 압축, numpy 경로 |
| `nc_sim_numba.py` | 3D 절삭 커널 추가 |
| `nc_viewer_widget.py` | 파서(축·G43.4·G68.2 원점), 선분 배열에 축 열, 엔진 선택, 표시 |
| `NC_Tool_List.spec`/`.iss` | numba 포함 여부(결정 C), 버전 1.10.0 |
| `tests/test_nc_sim3d.py` (신규), 뷰어 테스트 확장 | §5 |

---

## 4. 범위 밖
- 홀더/샹크 충돌·과삭 검사, G43.5(공구 벡터 I/J/K 지령), 회전축 이동 중 소재 자체의 운동(TCP는 공구 끝점 직선 이동으로 근사), 공구 반경 보정(G41/G42), 선반, 언더컷이 아닌 것 이외의 5축 특수 사이클.
- 3축 프로그램의 Z-map 경로 변경(그대로 유지).

---

## 5. 구현 단계와 완료 기준

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 파서: A/B/C 모달, G43.4/G49, 공구 축 `line_axis_map`, G68.2 원점 | `ncdata.nc`의 모든 경사면 구간 축이 `Rz(C)Rx(A)z`와 일치(줄별 표로 검증), G43.4 합성 프로그램 축 검증, 기존 표시 회귀 없음 |
| 2 | `nc_sim3d` 엔진(numpy) + numba 커널 | numba = numpy 비트 단위 동일, 수직 축(+Z)일 때 결과가 Z-map과 허용오차 내 일치, 수평 축 플런지/측면 절삭 해석해 검증 |
| 3 | 표면 메쉬 + 모서리 + 스냅샷 압축 | 닫힌 메쉬, 정점 수 상한, 스냅샷 복원 후 결과 동일 |
| 4 | 뷰어 통합(엔진 자동 선택, 워커, 진단 메시지 갱신) | `ncdata.nc`가 실제로 깎여 보임(스크린샷), 3축 프로그램은 기존과 완전히 같음 |
| 5 | 공구 축 표시, 소재 팝업 안내, 도움말 | 회전 규칙 사용자 확인(스크린샷 전달) |
| 6 | 성능 측정·튜닝, 패키징(numba 결정 반영), 릴리스 기록 | `ncdata.nc` 전체 계산 시간·메모리 기록, exe 검증 |

## 6. 테스트
- 축 계산: A/C 조합별 `Rz(C)Rx(A)z`, B→C 장비, G43.4 켜짐/꺼짐(G49), 주석 속 A/C 무시, 원점복귀 줄 무시.
- 3D 절삭: +Z 축 = Z-map 일치, 수평 축으로 옆면에 홈 파기(폭·깊이 해석해), 45° 기울인 공구 볼엔드밀 구멍, 플런지, 드릴, 소재 밖/취소/청크 분할 동일성, numba=numpy.
- 표면 메쉬: 닫힘, 법선, 평면이 계단 없이 평평, 모서리 개수.
- 통합: 순수 3축 → Z-map 경로, 혼합 프로그램 → 3D 경로, 무작위 seek = 처음부터 계산, 취소 연속 요청, 진단 메시지(경사면이 이제 "제외"가 아니라 "3D로 계산").
- 회귀: 전체 스위트(현재 423 passed).

---

## 7. 결정 필요 (권장안 ★)

| # | 질문 | 선택지 |
|---|---|---|
| **A** | 버전 | ★ v1.10.0 (기능 추가라 마이너 올림) / v1.9.4 |
| **B** | 3D 소재 표현 | ★ **복셀 점유 격자**(프로토타입 검증, 구현 단순·정확) / 다중 구간 덱셀(메모리 적으나 구현·표시 복잡, 측면 해상도 낮음) |
| **C** | 배포 exe에 numba 포함 | ★ **포함**(3+2/5축 계산이 실용 속도: 3만 선분 8~26초. 대신 배포 폴더가 크게 커짐 — llvmlite 122MB가 큼, 설치 파일도 커짐, AhnLab 등 백신의 JIT 오탐 가능성은 실기에서 확인 필요) / **미포함**(numpy 전용 — 해상도 자동 0.6mm, 수 분 걸릴 수 있음) |
| **D** | 3D 해상도 | ★ 자동(복셀 ≤ 약 2,000만) + 수동 0.2~1.0mm / 고정 0.5mm |
| **E** | 표시 방식 | ★ 표면 넷 메쉬 + 꺾임 각도 모서리(깔끔) / 복셀 블록 면(단순하지만 계단 모양, 모서리 노이즈) |
| **F** | 절삭 길이 | ★ 날장(FL), 없으면 SO / 돌출(SO) 전체(샹크까지 깎임) |
| **G** | 회전 규칙 | ★ 기존 뷰어와 동일(A→C: `Rz(C)Rx(A)z`, B→C: `Rz(C)Ry(B)z`) + **G68.2 원점 X Y Z 반영**(0이 아닌 프로그램은 화면 경로도 바뀜) + 공구 축 표시로 눈 검증 / 원점은 계속 무시 |
| **H** | G43.4 이동 표현 | ★ 끝점 방향 + 각도 변화 2° 초과 시 보간 조각 / 선분 끝점 방향만 사용(빠르나 거칠음) |
| **I** | 순수 3축 프로그램 | ★ 지금 Z-map 그대로(고해상도·빠름) / 모두 3D 엔진 |
| **J** | 혼합 프로그램(3축+3+2) | ★ 프로그램 전체를 3D 엔진으로(같은 소재에 이어 깎임) / 3축 공정은 Z-map, 경사 공정만 3D(소재 상태 이원화로 구현 복잡·부정확) |

## 8. 요청 사항 — 샘플 프로그램
- **G43.4가 들어 있는 NC 프로그램 1개**(가능하면 A/C가 이동 중 연속으로 변하는 것)를 주세요. 지금 저장소의 `ncdata.nc`에는 G43.4가 없어, 없으면 합성 프로그램으로만 검증하게 됩니다.
- 회전 규칙(G) 검증용으로 **"이 공정은 소재의 어느 면을 깎아야 한다"** 한 가지를 알려 주시면(예: `ncdata.nc`의 #1-1 Pocketing은 소재 +Y 옆면), 화면 결과가 그 방향인지 바로 확인해 부호 오류를 잡을 수 있습니다.

---

## 9. 구현 결과 (2026-09-24)

### 9.1 사용자 확인 사항
- 샘플 `O3210.NC`(B→C, 동시 5축): "깎이는 부위는 **벡터 방향 진입**을 하면서 가공되어야 함".
- "**G43.4에서는 I/J/K를 쓰지 않고**, 지정 축에서 A/B/C가 회전하면서 벡터를 결정하고 그 방향에서 가공이 이루어짐".
- "프로그램에서 기본 **G68.2로 기본 설정 → G69로 취소 → 다시 G43.4로 보정**".
- 회전 규칙은 이 샘플로 **데이터 검증**했다: `G0 X-114.977 … B21.333 C-4.571` → `X-151.239 Y-12.008 Z14.958`(길이 100.000mm)의 진입 방향이
  `-(Rz(C)·Ry(B)·z)` 와 **0.0005°** 차이로 일치(#5.3도 0.000°). A→C는 `ncdata.nc`의 `A-90 C20` ↔ `G68.2 I20 J-90`(C=I, A=J)로 일치(최대 0.0000°).

### 9.2 결정 반영
A v1.10.0 / B 복셀 점유 격자 / C **exe에 numba 포함**(빼려면 `NC_INCLUDE_NUMBA=0`) / D 자동(복셀 ≤ 약 2,000만) + 수동 / E 표면 넷 + 꺾임 모서리 /
F 날장(FL) / G 기존 회전 규칙 + G68.2 원점 반영 / H 끝점 방향 + 2° 초과 시 보간 조각 / I 순수 3축은 Z-map 그대로 / J 혼합 프로그램은 전체를 3D로.

### 9.3 계획 대비 달라진 점
- **G43.4 상태**: G43.4 켜짐 → G49 또는 일반 G43에서 꺼짐. G68.2+G53.1 행렬이 살아 있으면 행렬이 우선(G69에서 해제).
- **축 결정**: `NCViewerWidget.tool_axis_from_rotary()` — 모달 A/B/C 값에서 계산, 4축(B) 장비는 `Ry(B)`. 줄별 `line_axis_map`으로 노드에 붙는다(노드 생성부 무수정).
- **절삭 판정 후보**를 5개로 확정: {반경상 가장 가까운 점, 날장 경계 두 곳, 반경 경계 두 곳}. 처음 계획의 3개는 "축 방향으로 크게 진입해 날장 안을 통째로 가로지르는" 복셀을 놓칠 수 있었다.
- **표면 색**: 소재 색은 "그 면을 만든 공구" — 복셀이 제거될 때 색 id를 그 빈 복셀에 남기고, 표면 정점은 인접한 **빈 쪽** 색을 쓴다.
- **덧붙인 수정**: 스냅샷이 "공정 시작 줄(M6)과 seq가 정확히 같은 선분"에만 잡혀, 실제 프로그램(M6 뒤 G43 이전 이동)에서는 **한 개도 안 잡히던 결함**(v1.9.1~)을 "공정별 첫 절삭 선분"으로 고쳤다(Z-map에도 적용).
- 깎이기 전 소재는 2,000만 복셀에서 표면을 뽑지 않고 상자 메쉬로 즉시 그린다([적용] 후 UI 정지 2.35초 → 0.01초 수준).

### 9.4 측정 결과 (이 PC, 소재는 툴패스 범위 ± 5mm)

| 프로그램 | 엔진 | 이동 수(기울어진 것) | 해상도 / 복셀 | 계산 시간 | 제거율 |
|---|---|---|---|---|---|
| `O3210.NC`(B→C, G43.4) 26,153줄 | 3D + numba | 26,188 (7,906) | 0.589mm / 1,990만 | **28~37초** | 56.6% |
| 같은 프로그램, numba 없음(numpy) | 3D | 동일 | 1.18mm / 250만(자동으로 거칠게) | 45초 | 55.8% |
| `ncdata.nc`(A→C, G68.2) 32,903줄 | 3D + numba | 32,416 (32,416) | 0.434mm / 2,000만 | **16초** | 44.2% |
| 마이크로: 21° 기울인 D6, 1.5mm 선분 1,500개, 0.29mm | numba / numpy | | 2,000만 | 2.65초 / 20.7초 (1.77 / 13.8 ms) | |

- numba = numpy **복셀 단위 완전 일치**(다른 복셀 0개), 스냅샷 압축 40MB → 2MB(0.07초), 화면 메쉬 26만 정점(첫 추출 약 1초, 색상 모드 전환은 캐시로 즉시).
- **동결 exe 검증**: 앱 전체 구성으로 빌드한 점검 exe에서 3+2 프로그램이 스레드 경로 + numba로 정상 절삭·화면 갱신(제거율 22%). numba 첫 호출 컴파일 4.2초, 캐시 후 0.3초(exe 재실행에서도 캐시 유지).
  numba를 넣으면 배포 폴더 151MB → 275MB(+124MB, llvmlite가 대부분).
- 화면 스크린샷으로 `O3210.NC`(판재 + 포켓 + 벽 측면 가공)와 `ncdata.nc`(여러 면에 걸친 인덱스 가공과 드릴 구멍)의 형상을 확인했다.

### 9.5 테스트
- 신규 `tests/test_nc_sim3d.py` 24건: 격자·해상도, 공구 날장 길이, **수직 축 = Z-map 일치**, 수평 축 옆면 홈(해석해), 경사 볼엔드밀 진입, 축 방향 벡터 진입(원기둥), 날장 이상은 안 깎임, 이동 중 축 회전, numba=numpy, 청크·배치 동일성, 취소·복제, 스냅샷 압축 복원, 공구 색, 닫힌 표면 메쉬·바깥 법선·모서리·상자 메쉬·캐시.
- `tests/test_nc_sim_viewer.py` 확장: 축 공식, **G68.2 → G69 → G43.4 순서**, 일반 G43의 TCP 해제, G68.2 원점, **실제 O3210 발췌본의 벡터 진입 = -축**, 3D 경사면 절삭(해석 위치), 실제 BC 파일 발췌 절삭, 3D 무작위 seek = 처음부터 계산, 스냅샷 압축, 진단.
- 전체 스위트: **459 passed, 1 skipped**. (`SingleInstanceTests`는 이 PC에 사용자가 설치한 Sum Path가 실행 중이면 실패하는 환경 조건 — 앱을 끄면 통과.)

### 9.6 남은 것
- 홀더/샹크 충돌·과삭 검사, G43.5(공구 벡터 I/J/K 지령)는 범위 밖(사용자 확인: G43.4는 I/J/K 미사용).
- 소재 팝업에 "공구 축 표시"는 넣지 않았다(회전 규칙이 데이터로 검증돼 우선순위 하향). 필요하면 후속.
- 3D 해상도 콤보의 "자동"은 numba 유무·안전 모드에 따라 복셀 수를 2,000만/250만으로 잡는다.

### 9.7 패키지
Setup 80.1MB / Portable 111.9MB(numba 포함, 폴더 275MB). 자세한 해시는 `ABOUT_RELEASE_INSTRUCTIONS.md`.
