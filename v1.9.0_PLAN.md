# v1.9.0 플랜 — 밀링 3축 형상(가공) 시뮬레이션: 공구 형상 · 소재 설정 · 절삭 표시 · STL

작성: 2026-09-23 / 상태: **승인 대기**(§6 결정 A~H).
기반: v1.8.4 (메인 `agent/drag-drop-installer` `b312a8a`, PR #24 병합 완료).
대상: 새 모듈 `nc_sim.py`(순수 numpy — Qt 없음), `nc_viewer_widget.py`(소재 버튼·팝업·메쉬 표시·재생 연동),
`NC_Tool_List.py`(툴리스트 → 공구 형상 값 전달), `NC_Tool_List.spec`(hiddenimports).
**밀링 모드 전용** — 선반 모드 화면·계산은 손대지 않는다(LATHE_MODE_GUIDELINES.md).
툴패스 파서, 공구리스트 산출, PDF, 라이선스는 변경 없음.

---

## 1. 요구사항 (사용자 지시, 2026-09-23)

> "공구의 정보는 툴리스트에서 가져오면 될 것 같음 그리고 소재 설정부분은 뷰어 모드에서 설정을 할 수 있는
> 버튼이나 창이 필요함 팝업이면 좋겠음. T W L 을 입력 하고 T=Y W=Z L=X 와 같은 설정변경이 필요함 그리고
> 원점에서 위치를 지정할 수 있어야함 기본은 Z는 W의 Z0부터 시작 W와 T는 센터에서 시작을 기본으로 두고
> 별도 조정할 수 있게 설정이 필요함. … FLAT은 평엔드밀 DRILL은 드릴 SIG에서 날 각도를 지정 함 FILLET이나
> 커터 페이스 밀 같은 경우는 대부분 R이 있음 평엔드밀과 동일하나 공구 끝 R이 형성이 있음 그외 TAP이나 이런
> 내용은 그대로 우선 3축 부터 … (지금 형상 관련은 밀링 모드에서만 해당) 플랜 구성 후 승인대기"

정리:

1. 공구 형상은 **툴리스트 표의 값**(TYPE, D, FL, R, SIG, SO)으로 만든다. 따로 입력받지 않는다.
2. 뷰어에 **[소재] 버튼 → 팝업**. T/W/L 치수 입력, 각 치수를 X/Y/Z 중 어디에 둘지 설정, 원점 기준 위치 조정.
3. 기본 위치: **Z는 소재 윗면 = Z0에서 아래로**, 나머지 두 축은 **원점 중심 대칭**. 각각 따로 조정 가능.
4. 공구 형상 규칙: FLAT = 평엔드밀, DRILL = SIG 각도의 드릴, FILLET·CUTTER·FACE MILL = 평엔드밀 + 끝 R.
   그 밖의 공구(TAP 등)는 지금 규칙을 따로 만들지 않는다.
5. **3축부터** 한다. 밀링 모드 전용.
6. (앞선 답변에서 합의) 가공 결과 **STL 내보내기**, Tool tip 기준 절삭.

---

## 2. 현재 구조 (v1.8.4 기준 줄 번호)

| 항목 | 위치 | 현재 동작 |
|---|---|---|
| 툴리스트 컬럼 | `NC_Tool_List.py:588 COLUMNS` | `NO TYPE NAME D FL LCF F R SIG PL SO GL HOLDER …` — 형상에 쓸 값이 이미 있음 |
| TYPE 판정 | `:1096 DEFAULT_NAME_TYPES` | FLAT E/M, FILLET E/M, BALL E/M, DRILL, FACE MILL, CUTTER, REAMER, CENTER, T-CUTTER, DYNAMIC E/M, CHAMF MILL |
| 뷰어로 넘기는 값 | `:1465 tool_name_map_from_rows()` → `viewer.set_source_text(text, map)` | T번호 → NAME만 넘김 |
| 밀링 경로 노드 | `nc_viewer_widget.py:3315~3700` `tool_paths[key]` | `{"pt":[x,y,z], "type", "valid", "src_line", "seq"}` — `pt`는 G43이 적용된 **프로그램 좌표 = Tool tip** |
| 원호 | `:3790 _arc_points()` | 이미 점으로 쪼개져 노드에 들어감 |
| 드릴 사이클 | `:3578~3622` | 접근(G00) → R점 → 바닥(G01) → 복귀 노드로 펼쳐짐 |
| G68.2 경사면 | `:3429` | I/J/K 오인만 막고, 경사면 구간 좌표는 **로컬 그대로** 노드에 들어감 |
| 재생 | `PlaybackBarWidget` `:1259`, `_render_segments(seq_limit)` `:3907` | `seq` 기준으로 잘라 그림 |
| 뷰 버튼 오버레이 | `ProjectionOverlayWidget` `:1064` | ISO/XY/XZ/YZ(밀링), ISO/선반/XC(선반) |
| 선반 판정 | `NCViewerWidget.is_lathe_mode()` `:2082` | 장비 이름에 '선반' |
| 뷰어 설정 저장 | `QSettings("NC Tool List", "EmbeddedViewer")` `:1427` | — |

---

## 3. 설계

### 3.1 공구 형상 — 툴리스트에서 가져오기

`NC_Tool_List.py`에 `tool_shape_map_from_rows(rows)`를 추가한다. 키는 `tool_name_map_from_rows()`와 같다(`T01`/`T1`/`1`).
뷰어에는 `set_source_text(text, tool_name_map, tool_shape_map=None)`로 함께 넘긴다(인자를 생략해도 지금처럼 동작).

```python
{'T01': {'type': 'FLAT E/M', 'D': 12.0, 'FL': 30.0, 'R': 0.0, 'SIG': None, 'SO': 45.0}, ...}
```

**형상 규칙**(공구 축 = +Z, 원점 = Tool tip):

| TYPE | 형상 | 사용하는 값 |
|---|---|---|
| FLAT E/M, DYNAMIC E/M | 평바닥 원기둥 | D |
| FILLET E/M, CUTTER, FACE MILL | 평바닥 + 바닥 모서리 R | D, R (R이 없거나 0이면 FLAT과 같음, R > D/2면 D/2로 제한) |
| DRILL | 원기둥 + 원뿔 날끝, **Tool tip = 원뿔 꼭지점** | D, SIG (비어 있으면 118°, 결정 D) |
| 그 외 (TAP, REAMER, CENTER, BALL, CHAMF, T-CUTTER …) | **결정 C** | — |

- **길이**: 날 부분 = FL(색 구분), 전체 돌출 = SO. SO가 비어 있으면 FL + 10, FL도 비어 있으면 D × 3으로 표시만 한다.
  절삭은 날 부분만이 아니라 **돌출 길이(SO) 전체**로 빼서, 날장보다 깊이 들어간 경우도 결과에 드러나게 한다. 홀더는 1차 범위에서 그리지 않는다.
- **D가 없는 공구**: 형상을 만들지 않는다. 그 공정은 절삭하지 않고 상태 줄에 "T05: D 값 없음 — 시뮬레이션 제외"로 알린다.
- **표시**: 공구 프로파일(반단면 꺾은선)을 Z축 둘레로 회전시킨 메쉬(`GLMeshItem`)를 재생 위치의 Tool tip에 붙인다.

### 3.2 소재 설정 — [소재] 버튼 + 팝업

**버튼**: 밀링용 뷰 버튼 줄(ISO/XY/XZ/YZ) 끝에 `[소재]`를 추가한다. 선반 버튼 세트에는 넣지 않는다.
누르면 모달이 아닌 팝업(`StockDialog`)이 열린다. [적용]을 누르면 바로 반영되고 창은 열린 채로 남는다.

```
┌ 소재 설정 ──────────────────────────────────────────┐
│ [✓] 소재 / 가공 시뮬레이션 표시                       │
│                                                       │
│  치수(mm)     축 배정     기준 위치        추가 이동(mm) │
│  L [ 150.0 ]  [ X ▼ ]   [ 중심   ▼ ]     [  0.0 ]      │
│  W [ 100.0 ]  [ Y ▼ ]   [ 중심   ▼ ]     [  0.0 ]      │
│  T [  50.0 ]  [ Z ▼ ]   [ 윗면=0 ▼ ]     [  0.0 ]      │
│                                                       │
│  → X -75.0 ~ 75.0 / Y -50.0 ~ 50.0 / Z -50.0 ~ 0.0     │
│  [툴패스 범위에 맞추기]  여유 [ 2.0 ]                   │
│                                                       │
│  해상도 [ 자동(약 0.10) ▼ ]                             │
│  [STL 내보내기…]              [적용]  [닫기]            │
└───────────────────────────────────────────────────────┘
```

- **축 배정**: T/W/L마다 X/Y/Z 중 하나를 고른다. 한 줄을 바꾸면 겹친 축은 **자동으로 서로 바꾼다**. 그래서 항상 X·Y·Z가 하나씩 배정된다.
  예) `T=Y, W=Z, L=X`.
- **기준 위치**(축마다):
  - X/Y에 배정된 치수: `중심`(기본, 원점 대칭) / `-쪽 끝 = 0` / `+쪽 끝 = 0`
  - Z에 배정된 치수: `윗면 = 0`(기본, 0 ~ −치수) / `바닥 = 0` / `중심`
  - **추가 이동**: 기준 위치에서 해당 축으로 더 옮길 양.
- 아래 줄에 실제 범위(X/Y/Z min~max)를 바로 계산해서 보여 준다.
- **[툴패스 범위에 맞추기]**: 절삭 이송(G01/G02/G03) 경로 범위 + 여유로 치수와 이동을 채운다. 사용자 편의 기능이다.
- **저장**: 마지막 설정을 뷰어 QSettings(`stock/…`)에 저장하고 다음 실행 때 불러온다(결정 F).
  테스트는 실제 사용자 QSettings를 오염시키지 않도록 기존 방식대로 분리한다.

### 3.3 절삭 엔진 — `nc_sim.py` (Z-map)

Qt 없이 numpy만 쓰는 모듈이라 단위 테스트가 쉽고, 나중에 3+2용 엔진(Tri-dexel)으로 바꿀 수 있게 인터페이스를 고정한다.

```python
class ToolShape:      # profile(r) -> 바닥에서 반경 r 지점의 높이(없으면 inf), radius, length
class StockSpec:      # T/W/L, 축 배정, 기준, 이동 -> bounds (xmin..zmax)
class ZMapStock:      # heights[ny, nx] 초기값 = zmax
    def cut_segment(p0, p1, tool)   # Tool tip이 p0→p1로 이동할 때 깎기
    def snapshot() / restore(snap)
    def to_mesh() -> (verts, faces)  # 윗면 + 옆벽 + 바닥 폐곡면
def write_stl_binary(path, verts, faces)
```

- **절삭 계산**: 선분을 격자 간격의 절반 이하로 나누고, 각 점마다 공구 반경 안의 격자에
  `heights = min(heights, tip_z + profile(r))`를 적용한다. 이때 해당 영역만 잘라서 numpy로 벡터 연산한다.
  원호는 이미 점열이라 같은 방식으로 처리된다.
- **G00 급속**: 계산은 같이 하고, 소재를 깎은 급속 이동은 **경고 목록**에 줄번호로 남긴다. 팝업 아래에 "급속 절삭 N건"으로 표시한다.
- **제외 구간**: `valid=False` 노드(G43 전), **G68.2/G53.1 경사면이 켜진 구간**, 회전축 A/B가 0이 아닌 구간은 1차 범위(3축)에서 절삭하지 않는다.
  파서 노드에 `"tilt": True` 플래그만 추가하고(좌표·그리기는 그대로), 상태 줄에 "3+2 공정 N개 제외"로 알린다.
- **해상도**: `자동`은 소재 XY 격자 수가 약 100만 셀 이하가 되도록 0.05~0.5mm 사이에서 고른다. 수동으로 0.05/0.1/0.2/0.5도 고를 수 있다.
  **안전 모드**(v1.8.3)에서는 자동 해상도를 한 단계 거칠게 잡는다.

### 3.4 표시와 재생 연동

- 소재를 켜면 원래 소재(반투명 회색) 대신 **가공 결과 메쉬**(불투명, 조명)와 현재 공구 메쉬를 그린다. 툴패스 선은 지금처럼 그 위에 표시된다.
- **재생 앞으로**: 이전 seq부터 현재 seq까지의 선분만 이어서 깎는다(증분 계산).
- **뒤로 / 점프**(F6·F8, 되감기, 줄 클릭): **공정 시작마다 저장한 스냅샷**에서 복원한 뒤 목표 seq까지 다시 깎는다.
  스냅샷은 메모리 상한(예: 200MB)을 넘으면 오래된 것부터 버린다.
- 긴 계산은 `QTimer`로 잘게 나눠 UI를 멈추지 않게 한다. 진행률은 상태 줄에 표시한다.
- 메쉬 갱신은 재생 중 최대 초당 10회로 제한한다(바뀐 영역만 다시 만들지는 않음 — 1차는 전체 재생성).
- **공구 필터**(전체/해제)는 선 표시에만 적용된다. 절삭은 항상 실행 순서 전체를 기준으로 한다(결정 G).

### 3.5 STL 내보내기

- 팝업의 `[STL 내보내기…]` → 저장 대화상자. 기본 파일명은 `<프로그램번호>_sim.stl`.
- 내보내는 대상은 **현재 재생 위치의 가공 결과**(재생이 끝났으면 최종 형상), 단위 mm, 바이너리 STL이다. 소재나 공구만 따로 내보내는 기능은 1차 범위에서 제외한다.
- 외부 라이브러리 없이 `write_stl_binary()`가 직접 쓴다.

### 3.6 파일 · 빌드

| 파일 | 변경 |
|---|---|
| `nc_sim.py` (신규) | ToolShape / StockSpec / ZMapStock / STL |
| `nc_viewer_widget.py` | `[소재]` 버튼, `StockDialog`, 공구 형상 수신, 노드 `tilt` 플래그, 메쉬 표시, 재생 연동 |
| `NC_Tool_List.py` | `tool_shape_map_from_rows()`, `set_source_text` 호출부, `APP_VERSION = '1.9.0'` |
| `NC_Tool_List.spec` | `nc_sim` hiddenimports |
| `tests/test_nc_sim.py` (신규) | 아래 §5 |
| 사용설명서 / 도움말 | 소재 설정·시뮬레이션 항목 추가 |

---

## 4. 범위 밖 (1차)

- 3+2(G68.2) · 동시 5축 절삭: 다음 단계에서 Tri-dexel로 확장한다(v1.9.x 이후 별도 플랜).
- 선반 모드 형상 시뮬레이션.
- 홀더 표시와 충돌 검사, 과삭(Gouge) 비교, 모델 대비 잔삭 비교.
- TAP 나사산, T-커터 언더컷(Z-map으로는 표현 불가).
- 공구 반경 보정(G41/G42)의 형상 반영: 경로 표시 방식을 그대로 따른다.

---

## 5. 테스트

- **공구 프로파일**: FLAT·FILLET·DRILL의 `profile(r)` 값을 검증한다(예: D12 R2에서 r=5 → 높이 2 − √(4 − 1)).
- **값 누락**: R 누락 → FLAT으로 처리, SIG 누락 → 118°, D 누락 → 제외 목록.
- **소재 배정**: T/W/L 축 배정 교환, 기준 위치·추가 이동에 따른 bounds 계산.
- **Z-map 절삭**: D10 평엔드밀로 Z−5 직선 슬롯 → 폭 10 ± 격자 간격, 바닥 −5. SIG118 드릴 구멍 → 원뿔 깊이 검증.
- **G00 급속 절삭 경고**와 `tilt` 구간 제외.
- **재생 연동**: 앞으로 증분 계산한 결과 = 처음부터 한 번에 계산한 결과, 스냅샷 복원 후 결과도 같음.
- **STL**: 헤더와 삼각형 수, 법선, 닫힌 메쉬(모든 변이 두 번씩 공유되는지).
- **회귀**: 기존 `tests/` 전체 통과. 선반 모드에서는 `[소재]` 버튼이 보이지 않아야 한다.

---

## 6. 결정 필요 (권장안 ★)

| # | 질문 | 선택지 |
|---|---|---|
| **A** | 버전 | ★ v1.9.0 (큰 기능이라 마이너 올림) / v1.8.5 |
| **B** | 축 배정 **기본값** | ★ `L=X, W=Y, T=Z` (T=두께를 높이로) / 말씀하신 예시 그대로 `T=Y, W=Z, L=X` |
| **C** | 규칙 없는 공구(TAP, REAMER, CENTER, BALL, CHAMF, T-CUTTER …) | ★ D 지름의 **평엔드밀 원기둥으로 절삭**(형상은 대략 맞게) / 표시와 절삭 모두 제외 / BALL만 R=D/2로 추가하고 나머지는 원기둥 |
| **D** | DRILL에 SIG가 비었을 때 | ★ 118° / 140° / 제외 |
| **E** | 소재 기본 크기(설정한 적 없을 때) | ★ 툴패스 범위 + 여유 2mm로 자동 / 빈 값으로 두고 입력 요구 |
| **F** | 소재 설정 저장 단위 | ★ 마지막 설정 하나(전역) / 프로그램 번호별로 기억 |
| **G** | 공구 필터로 일부 공정을 끄면 | ★ 절삭은 전체 공정 기준(선만 숨김) / 켜진 공정만 절삭 |
| **H** | 소재 켰을 때 툴패스 선 | ★ 그대로 표시 / 자동 숨김(버튼으로 다시 켜기) |

"기본은 Z는 W의 Z0부터 시작, W와 T는 센터에서 시작"은 이렇게 해석했습니다: **Z에 배정된 치수는 윗면 = Z0에서 아래로, X·Y에 배정된 두 치수는 원점 중심.**
이 해석이 맞는지 결정 B와 함께 확인 부탁드립니다.
