# v1.8.0 플랜 — 라이선스 도입 (발급 프로그램 + Sum Path 라이선스 검사)

작성: 2026-09-12 / 상태: **승인 대기** (§4 결정 A~F 확인 부탁)
기반: v1.7.9 (메인 `agent/drag-drop-installer` `424d45c`, PR #19 병합 완료).
대상: 앱 시작 경로(`main()`), About 창, 설치 스크립트, **신규** 공용 모듈과 발급 프로그램.
파서·툴패스·3D 뷰어·PDF·공구리스트 산출은 손대지 않는다.

---

## 1. 요구사항 (사용자 지시, 2026-09-12)

> "v1.8.0 요청 사항 이제 부터 라이센스를 부여하려고 해 라이센스파일 만들 수 있는
> 프로그램을 하나 만들고 sum path의 프로그램 라이센스를 부여해서 사용할 수 있게 해줘
> 라이센스는 7일 30일 365일(1년) 3년으로 설정 할 수 있게 부탁해 플랜 구성 후 승인대기"

정리:

1. **라이선스 발급 프로그램**(별도 exe)을 새로 만든다 — 라이선스 파일(`.lic`)을 만든다.
2. **Sum Path**(`APP_NAME`, `NC_Tool_List.exe`)는 유효한 라이선스가 등록돼 있어야 실행된다.
3. 라이선스 기간은 **7일 / 30일 / 365일(1년) / 3년** 중에서 고른다.

---

## 2. 현재 구조 (조사 결과, v1.7.9 기준 줄 번호)

| 항목 | 위치 | 비고 |
|---|---|---|
| 앱 시작 | `NC_Tool_List.py:3960 main()` | 로그 → `QApplication` → 단일 실행 핸드오프(`:3979 send_to_running_instance`) → `:3982 App()` |
| About 창 | `App.show_about()` (`:2502`) | 용도/버전/제작일/제작자/오픈소스 + `업데이트` 그룹 |
| 사용자 설정 | `app_settings_path()` (`:1163`) | `%APPDATA%\NC Tool List\app_settings.json` (사용자별) |
| 시작 로그 | `startup_log_path()` (`:1605`) | `%LOCALAPPDATA%\NC_Tool_List\startup.log` |
| 오픈소스 목록 | `OPEN_SOURCE_COMPONENTS` (`:34`) | About 창에 표시 |
| 설치 | `NC_Tool_List.iss` | `C:\NC_Tool_List`, 관리자 권한 설치, 파일 연결 등록 |
| 빌드 | `NC_Tool_List.spec` | PyInstaller onedir, UPX 끔 |

- 라이선스 관련 코드는 **현재 없다.**
- 기존 테스트(`tests/test_nc_tool_list.py`)는 `App()`을 직접 만든다. 라이선스 검사를
  **`main()`에만** 두면 기존 테스트 275개는 영향을 받지 않는다(`App.__init__`에는 넣지 않음).
- 개발 PC 확인(2026-09-12): `cryptography` 50.0 설치됨(Ed25519 서명 동작 확인), 현재 앱
  빌드에는 **포함돼 있지 않음**(넣으면 약 11 MB + 네이티브 DLL 추가). Windows
  `HKLM\SOFTWARE\Microsoft\Cryptography\MachineGuid`는 일반 권한으로 읽힘. `C:\ProgramData` 사용 가능.

---

## 3. 설계

### 3.1 전체 흐름

```
[발급 PC]  SumPath License Maker ── 개인키(발급 PC에만 보관)로 서명 ──▶  xxx.lic
                                                                            │ (메일/USB/공유폴더)
[사용 PC]  Sum Path 첫 실행 → "라이선스 등록" 창 → PC 코드 표시 ──(발급자에게 전달)
           xxx.lic 등록 → 앱에 내장된 공개키로 서명 검증 → 통과하면 실행
```

- 방식: **Ed25519 전자서명**. 라이선스 파일은 사람이 읽을 수 있는 JSON이지만, 기간·PC 코드·
  사용자명 중 **한 글자만 고쳐도 서명이 깨져 무효**가 된다. 개인키 없이는 새 라이선스를 만들 수 없다.
- 네트워크 인증 서버는 두지 않는다(완전 오프라인 — 공장 PC 환경).

### 3.2 라이선스 파일 형식 (`.lic`, UTF-8 JSON)

```json
{
  "format": 1,
  "product": "SumPath",
  "license_id": "3F2A9C1E-8B4D-4E21-9A57-0C6D2E1F7B33",
  "licensee": "생산부 1공장",
  "machine": "7K3F-A9Q2-M4XD-8P1C",
  "plan_days": 30,
  "starts": "2026-09-12",
  "valid_until": "2026-10-11",
  "issued_at": "2026-09-12T10:30:00",
  "memo": "",
  "signature": "<base64 Ed25519 서명>"
}
```

- 서명 대상 = `signature`를 뺀 나머지 필드를 `sort_keys=True`, `separators=(',', ':')`,
  `ensure_ascii=False`로 직렬화한 UTF-8 바이트(한글 사용자명 포함해도 결과가 항상 같게).
- `product`가 `SumPath`가 아니거나 `format`이 모르는 값이면 무효.

### 3.3 기간 규칙

| 선택 | `plan_days` | 예: 시작일 2026-09-12 → 사용 기한(`valid_until`, 그날까지 사용 가능) |
|---|---|---|
| 7일 | 7 | 2026-09-18 |
| 30일 | 30 | 2026-10-11 |
| 1년 (365일) | 365 | 2027-09-11 |
| 3년 | 1095 (결정 D) | 2029-09-10 |

- `valid_until = starts + (plan_days - 1)일`. 시작일 00:00부터 사용 기한 당일 23:59:59까지,
  **꽉 채워 N일**. 판정은 사용 PC의 로컬 날짜 기준.
- 시작일 기본값은 **발급일(오늘)**. 연장 발급 때 남은 기간을 버리지 않도록 발급 프로그램에서
  시작일을 바꿀 수 있다(예: 기존 사용 기한 다음 날부터 시작).

### 3.4 PC 코드 (결정 A — PC 고정)

- `MachineGuid` → SHA-256 → 앞 16자리(Base32 대문자, 헷갈리는 `0/O/1/I` 제외) →
  `XXXX-XXXX-XXXX-XXXX`. 원래 GUID는 밖으로 드러나지 않는다.
- 등록 창과 About 창에 표시하고 **[복사]** 버튼을 둔다. 사용자는 이 코드를 발급자에게 보낸다.
- 라이선스의 `machine`이 이 PC 코드와 다르면 "다른 PC용 라이선스"로 거부 → 파일을 다른 PC에
  복사해도 쓸 수 없다.
- Windows를 새로 설치하면 `MachineGuid`가 바뀌어 **재발급이 필요**하다(프로그램 재설치나 업데이트로는
  바뀌지 않는다).

### 3.5 Sum Path 쪽 동작

**공용 모듈 `sumpath_license.py` 신설**(앱과 발급 프로그램이 같이 씀 — 형식·기간·PC 코드 규칙을
한 곳에 둔다):

- `LICENSE_PLANS = ((7, '7일'), (30, '30일'), (365, '1년 (365일)'), (1095, '3년'))`
- `machine_code()`, `canonical_payload(data)`, `compute_valid_until(starts, days)`
- `verify_license(data, public_key=LICENSE_PUBLIC_KEY, machine=None, today=None)` →
  `LicenseStatus(ok, reason, message, license)` — 오류 종류별 사용자 문구(아래 표)
- 서명 **검증**은 순수 파이썬 Ed25519(RFC 8032 참조 구현) — `cryptography`를 앱에 넣지 않는다
  (결정 F). **서명(발급)** 은 발급 프로그램에서만 `cryptography`로 한다.

**저장 위치**

1. `C:\ProgramData\NC Tool List\license.lic` — PC 전체 공용(어느 Windows 계정으로 켜도 동작).
   등록 창에서 고른 파일을 여기에 복사한다.
2. 보조: exe 폴더의 `license.lic` — 포터블 zip이나 관리자가 직접 넣어 두는 경우.

**검사 시점** — `main()`에서 단일 실행 핸드오프 **뒤**, `App()` 생성 **앞**:

```python
if send_to_running_instance(initial_file):
    ...
    return
status = ensure_license()            # 유효하면 바로 통과, 아니면 등록 창
if not status.ok:
    write_startup_log('License not accepted: %s' % status.reason)
    return                           # 앱 창을 만들지 않고 종료
window = App()
```

- 이미 떠 있는 창에 파일을 넘기는 경로(v1.6.7 단일 실행)는 검사를 다시 하지 않는다 — 떠 있는
  창이 이미 통과한 상태이므로.

**라이선스 등록 창** (라이선스가 없거나 무효일 때)

```
┌ Sum Path 라이선스 ───────────────────────────────┐
│ 라이선스가 만료되었습니다. (사용 기한: 2026-10-11) │
│                                                  │
│ 이 PC 코드:  7K3F-A9Q2-M4XD-8P1C   [복사]         │
│ 위 코드를 관리자에게 보내 라이선스 파일을 받으세요. │
│                                                  │
│          [라이선스 파일 등록...]     [종료]        │
└──────────────────────────────────────────────────┘
```

- `[라이선스 파일 등록...]` → 파일 선택(`*.lic`) → 검증 → 통과하면 저장 위치 1에 복사하고
  **바로 앱이 시작**된다. 실패하면 이유를 창에 보여 주고 창은 그대로 둔다.

| 상태 | 문구(요지) |
|---|---|
| 없음 | 등록된 라이선스가 없습니다. |
| 만료 | 라이선스가 만료되었습니다. (사용 기한: …) |
| 시작 전 | 라이선스 시작일(…)이 아직 되지 않았습니다. |
| 다른 PC | 이 PC용 라이선스가 아닙니다. (라이선스 PC 코드: …) |
| 변조/손상 | 라이선스 파일이 손상되었거나 수정되었습니다. |
| 시계 이상 | PC 날짜가 올바르지 않습니다. 날짜/시간을 확인하세요. |

**시계 되돌림 방지** — PC 날짜를 과거로 돌려 기간을 늘리는 것을 막는다.

- `C:\ProgramData\NC Tool List\license_state.json`에 **마지막으로 확인한 시각**을 기록하고,
  현재 시각이 그보다 **24시간 넘게 과거**면 "시계 이상"으로 거부.
- 현재 시각이 라이선스 `issued_at`보다 24시간 넘게 과거여도 거부(파일을 지워도 우회 불가).
- 24시간 여유는 표준시 변경·BIOS 시계 오차로 멀쩡한 PC가 막히지 않게 하려는 것.

**만료 임박 안내** — 남은 기간이 **7일 이하**(7일 라이선스는 **2일 이하**)면 시작할 때 하루 한 번
안내 창(차단 아님, [확인]만). About 창에도 남은 일수를 항상 표시.

**실행 중 만료**(결정 C) — 1시간마다 다시 확인해 만료되면 안내 후 앱을 닫는다.

**About 창** — `업데이트` 그룹 위에 `라이선스` 그룹 추가:
사용자 / 기간(예: 1년 (365일)) / 사용 기한 + 남은 일수 / PC 코드 [복사] / `[라이선스 파일 교체...]`
(연장 라이선스를 받았을 때 등록 창을 거치지 않고 교체).

**로그** — `startup.log`에 `License OK id=… valid_until=…` 또는 거부 사유 한 줄.

### 3.6 발급 프로그램 — `SumPath License Maker`

- 파일: `SumPath_License_Maker.py` + `SumPath_License_Maker.spec` → `dist\SumPath_License_Maker\`
  (onedir, UPX 끔 — 본 앱과 같은 빌드 원칙). 자체 버전 `1.0.0`.
- **Sum Path 설치본·포터블·업데이트 공유 폴더에 절대 포함하지 않는다.** 발급자 PC에만 둔다.
  배포물: `installer\SumPath_License_Maker_v1.0.0.zip`(개인키는 들어가지 않음).

**[발급] 탭**

| 입력 | 비고 |
|---|---|
| 사용자(회사/부서/이름) | 필수. 파일과 About 창에 표시 |
| PC 코드 | 필수. 붙여넣기 — 형식(`XXXX-XXXX-XXXX-XXXX`) 검사, 소문자·공백 자동 정리 |
| 기간 | 라디오: **7일 / 30일 / 1년 (365일) / 3년** |
| 시작일 | 기본 오늘. 사용 기한이 바로 아래에 계산돼 보임 |
| 메모 | 선택 |

- `[라이선스 발급]` → 저장 대화상자. 기본 파일명
  `SumPath_<사용자>_<PC코드 앞 4자리>_<사용 기한>.lic` (파일명에 못 쓰는 문자는 `_`).
- 발급할 때마다 `%APPDATA%\SumPath License Maker\issued_licenses.csv`에 이력 한 줄
  (license_id, 사용자, PC 코드, 기간, 시작일, 사용 기한, 발급 시각, 메모) — 누구에게 언제까지
  줬는지 엑셀로 열어 볼 수 있게.

**[확인] 탭** — `.lic`를 열어 내용 / 서명 정상 여부 / 남은 기간을 보여 준다(문의 대응용).

**서명 키** — `%APPDATA%\SumPath License Maker\signing_key.pem`(Ed25519 개인키).
키가 없으면 발급 버튼을 끄고 안내한다. 키의 공개키가 앱에 내장된 공개키와 다르면 발급을 막는다
(엉뚱한 키로 만든, 쓸 수 없는 라이선스 방지).

### 3.7 서명 키 준비 (구현할 때 1회)

1. 이 개발 PC에서 Ed25519 키 쌍을 만든다.
2. **공개키**만 `sumpath_license.py`에 상수(`LICENSE_PUBLIC_KEY`)로 넣는다 — git에 올라가도 안전.
3. **개인키**는 `%APPDATA%\SumPath License Maker\signing_key.pem`에만 둔다. 저장소(git)·빌드
   폴더·zip에는 절대 넣지 않는다(`.gitignore`에 `*.pem` 추가, 이를 검사하는 테스트 추가).
4. ⚠️ **개인키 백업 필수**(USB 등 2곳). 잃어버리면 새 라이선스를 더는 발급할 수 없어, 새 키로
   앱을 다시 배포하고 모든 PC에 재발급해야 한다. 유출되면 누구나 라이선스를 만들 수 있다.

### 3.8 설치 스크립트

- `[Dirs]`에 `Name: "{commonappdata}\NC Tool List"; Permissions: users-modify` 추가 —
  관리자 권한 없는 Windows 계정으로도 라이선스를 등록할 수 있게.
- 제거 시 이 폴더를 지우지 않는다 → 재설치·업데이트해도 라이선스 유지.
- 버전 `1.8.0`(`NC_Tool_List.iss`, `version_info.txt`, `APP_VERSION`).

### 3.9 보안 한계 (미리 알려 둠)

- 오프라인 라이선스는 **exe 자체를 뜯어고치는** 공격(검사 코드 제거, 공개키 바꿔치기)까지는 막지
  못한다. 이번 목표는 **기간과 PC를 통제**하는 것 — 파일 복사, 파일 내용 수정, 날짜 되돌리기로는
  우회되지 않게 한다. 코드 난독화는 하지 않는다.

### 3.10 바꾸지 않는 것

- 파서 / 툴패스 / 3D 뷰어 / PDF / 공구리스트 산출 / 검색 / 단일 실행 핸드오프 / 파일 연결.
- 업데이트 기능(공유 폴더에서 설치 파일 찾기)은 그대로 — 라이선스와 무관하게 동작.

---

## 4. 결정 사항 (승인 시 확인 부탁)

| # | 결정 | 권장안 | 대안 |
|---|---|---|---|
| **A** | 라이선스를 PC에 묶을지 | **PC 고정** — PC 코드가 맞아야 동작(파일 복사로 다른 PC 사용 불가) | PC 무관 — 파일만 있으면 어느 PC든 동작(배포는 쉽지만 복사 사용을 못 막음) |
| **B** | v1.8.0 업데이트 직후(아직 라이선스 없음) | **유예 없음** — 첫 실행에 등록 창 → PC 코드 받아 발급(§8 순서대로 발급 준비 후 배포) | 첫 실행부터 **7일 체험** 자동 부여(편하지만 재설치·파일 삭제로 반복될 수 있음) |
| **C** | 켜 둔 채로 사용 기한이 지나면 | **1시간마다 확인, 만료되면 안내 후 종료** | 켜져 있는 동안은 계속 사용, **다음 실행부터** 차단 |
| **D** | "3년"의 길이 | **1095일**(365×3 — 요청의 "365일(1년)"과 같은 일 단위) | 달력 기준 3년(윤년이 끼면 1096일) |
| **E** | 서명 키(개인키) 비밀번호 | **없음** — 발급 PC에만 두고 백업으로 관리(발급이 간단) | 발급할 때마다 비밀번호 입력(PC를 같이 쓰면 더 안전) |
| **F** | 앱의 서명 검증 방식 | **순수 파이썬 검증** — 새 DLL 없음, 용량 증가 거의 없음(RFC 8032 공식 테스트 벡터 + `cryptography` 교차 검증 테스트로 정확성 보장) | `cryptography`를 앱에도 포함(+약 11 MB, 네이티브 DLL 추가 → 보안 프로그램 호환 재확인 필요) |

---

## 5. 변경 파일 / 작업 목록

1. **신규** `sumpath_license.py` — 형식·기간·PC 코드·순수 파이썬 Ed25519 검증·`LicenseStatus`,
   공개키 상수, 저장/불러오기(`license_dir()`는 테스트에서 바꿔 끼울 수 있게)
2. `NC_Tool_List.py`
   - `main()`에 `ensure_license()` 삽입(핸드오프 뒤, `App()` 앞)
   - `LicenseDialog`(등록 창), 만료 임박 안내, 1시간 주기 확인 타이머(결정 C)
   - `show_about()`에 `라이선스` 그룹
   - `APP_VERSION = '1.8.0'`, `APP_BUILD_DATE`
3. **신규** `SumPath_License_Maker.py`, `SumPath_License_Maker.spec` — 발급 프로그램(§3.6)
4. `NC_Tool_List.spec` — `hiddenimports`에 `sumpath_license` 명시(자동 수집되지만 봉인용)
5. `NC_Tool_List.iss` — `[Dirs]` 공용 폴더 권한(§3.8), `MyAppVersion 1.8.0`; `version_info.txt` 1.8.0
6. `.gitignore` — `*.pem`, `*.lic`(테스트용 제외)
7. **신규** `tests/test_license.py` — §6 / 기존 `tests/test_nc_tool_list.py`에 봉인 테스트 소수
8. `ABOUT_RELEASE_INSTRUCTIONS.md` — v1.8.0 이력, 라이선스 운영 절차(§8) 요약
9. 회귀 확인: 전체 스위트를 미수정 v1.7.9와 **같은 범위로** 돌려 실패 집합 비교
   (공유 QSettings 영향 — [[project_tests_share_real_qsettings]])

---

## 6. 테스트 (신규)

1. **Ed25519 정확성** — RFC 8032 §7.1 공식 테스트 벡터(TEST 1~3, 1024바이트 SHA(abc)) 통과.
   `cryptography`로 만든 서명 50개를 순수 파이썬 검증기가 전부 통과, 1비트 바꾼 서명·메시지는
   전부 거부.
2. **기간** — 7/30/365/1095일의 `valid_until` 정확(월말·윤년 2028-02-29 포함), 사용 기한
   당일 통과·다음 날 만료, 시작일 전 거부.
3. **변조** — 발급 후 `valid_until`/`machine`/`licensee`/`plan_days` 중 하나만 바꿔도 "변조",
   필드 추가/삭제, 잘못된 JSON, 빈 파일, `product` 불일치 → 전부 거부(예외 없이 상태로 반환).
4. **PC 고정** — 다른 PC 코드 → "다른 PC", PC 코드 형식·정규화(소문자/공백).
5. **시계** — 기록된 마지막 확인 시각보다 25시간 과거 → "시계 이상", 23시간 과거는 통과,
   `issued_at`보다 25시간 과거 → 거부, 상태 파일이 없거나 깨져도 예외 없음.
6. **한글 사용자명** — 직렬화가 결정적이고 서명/검증 왕복 통과.
7. **발급 프로그램 왕복** — 임시 폴더에 테스트용 키 쌍 생성 → 발급 함수로 `.lic` 작성 →
   앱 검증기(테스트 공개키 주입)로 통과, 이력 CSV 한 줄 추가, 기본 파일명 규칙.
8. **키 불일치** — 내장 공개키와 다른 개인키면 발급 거부.
9. **저장 위치** — ProgramData 우선, exe 폴더 보조, 둘 다 없으면 "없음"(임시 폴더로 주입).
10. **등록 창**(offscreen) — PC 코드 표시, 무효 파일 등록 시 이유 표시 후 창 유지, 유효 파일 등록 시
    저장 위치로 복사되고 수락.
11. **봉인** — `inspect.getsource(main)`에서 `send_to_running_instance` → `ensure_license` →
    `App()` 순서. 공개키 상수만 있고 개인키(`PRIVATE KEY`) 문자열이 저장소 어느 파일에도 없음.
    설치 스크립트 `[Files]`가 발급 프로그램을 담지 않음. `.iss`에 `users-modify` 공용 폴더.
12. 기존 버전 동기화 테스트(`test_installer_uses_c_drive_onedir_package_without_direct_taskkill`)가
    1.8.0으로 그대로 통과.

---

## 7. 회귀 위험과 대응

| 위험 | 대응 |
|---|---|
| 기존 테스트가 라이선스 때문에 깨짐 | 검사를 `main()`에만 둠(`App()`은 무관), 테스트 11 봉인 |
| 멀쩡한 PC가 날짜 오차로 막힘 | 24시간 여유, 테스트 5 |
| 일반 사용자 계정에서 라이선스 저장 실패 | 설치 시 공용 폴더 `users-modify`, 실패하면 이유를 등록 창에 표시 |
| 순수 파이썬 검증이 틀리거나 느림 | RFC 벡터 + 교차 검증(테스트 1), 시작 시 1회 — 구현 후 소요 시간 측정해 이력에 기록 |
| 개인키 분실/유출 | 저장소·빌드물 제외 봉인(테스트 11), 백업 안내(§3.7), 운영 절차(§8) |
| 등록 창이 떠 있는 동안 파일을 또 더블클릭 | 단일 실행 서버가 아직 없어 두 번째 등록 창이 뜬다 — 기능상 문제 없음(둘 다 같은 저장 위치 사용) |

---

## 8. 배포 순서 (운영 절차 — 중요)

v1.8.0을 업데이트 공유 폴더에 올리는 순간, 각 PC는 **라이선스를 등록하기 전까지 Sum Path를 쓸 수
없다**(결정 B 권장안 기준). 그래서 순서를 지킨다:

1. 발급 프로그램 준비 + 개인키 백업(§3.7).
2. 각 PC에 v1.8.0 설치 → 뜨는 등록 창의 **PC 코드**를 모은다.
3. 발급 프로그램으로 PC마다 `.lic` 발급 → 전달.
4. 각 PC에서 `[라이선스 파일 등록...]` → 바로 사용.
5. 연장: 새 `.lic` 발급(시작일 = 기존 사용 기한 다음 날) → About 창 `[라이선스 파일 교체...]`.

---

## 9. 준비 상태 (2026-09-12)

- 작업 브랜치: `worktree-v180-license` (메인 `424d45c` 기반, worktree
  `.claude/worktrees/v180-license`)
- **플랜 작성 완료, 승인 대기** — 코드 변경 없음. 승인 후 §5 순서대로 구현·테스트하고,
  빌드는 사용자 지시가 있을 때 진행한다.
