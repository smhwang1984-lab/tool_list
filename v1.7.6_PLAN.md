# v1.7.6 플랜 — 시뮬레이션을 "실행 순서(seq)" 기준으로 전환

작성: 2026-09-07 / 상태: **승인 완료** (결정 A~D 전부 권장안대로 확정,
2026-09-07). 서브프로그램 번호(`M98 P<n>`)는 `P0001`에 한정되지 않고
`_M98_P_RE = r"P0*(\d+)(?!\d)"`로 임의 번호를 받는다 — 플랜 본문의
`P0001`/`O0001`은 전부 설명용 예시.
대상: 뷰어 시뮬레이션(PG 매칭 재생 + 방향키). 선반·밀링 공용
(v1.7.4에서 `_expand_subprograms`를 이미 공용으로 만들어 뒀다).
경로 파싱 로직(좌표 계산)은 손대지 않는다.

---

## 1. 요구사항 (사용자 지시, 2026-09-07)

> "시뮬레이션에서는 M30에서 종료 되게 설정, 뒤에 서브프로그램들은 호출
> 사용만 허용. 지금 시뮬레이션 운용은 M98 P0001일 시 전체 툴패스를
> 보여주고 바로 넘어감. 그리고 마지막에 다시 서브프로그램 시뮬레이션을
> 보여줌. 즉 M98 P0001이라면 O0001을 호출해서 시뮬레이션 라인을 보여주고
> 다시 원래 라인으로 복귀해서 다음 진행. 그중에 또 M98 P0001이 나온다면
> 다시 호출 후 M99에서 다시 복귀해서 보여주는 방식. 방향키로도 동일하게
> 설정 필요."

정리 (요구 1~4):

1. **재생은 M30에서 끝난다.** M30 뒤에 붙은 `O<번호>` 서브프로그램 본문은
   그 자체로는 재생되지 않고, `M98`로 호출될 때만 실행된다.
2. **`M98 P0001`을 만나면 그 자리에서 `O0001` 본문 줄로 커서가 들어가**
   한 줄씩 시뮬레이션되고, `M99`에서 **`M98` 바로 다음 줄로 복귀**한다.
3. **중첩** — 서브프로그램 안에서 또 `M98`이 나오면 다시 들어가고,
   `M99`에서 그 호출자로 복귀한다(현재 깊이 상한 10).
4. **방향키(↓/↑)도 동일한 순서**로 움직인다.

---

## 2. 현재 동작과 원인

### 2.1 두 개의 서로 다른 "진행 순서"가 공존한다

v1.7.4에서 **경로 파싱**은 이미 실행 순서로 돌고 있다:

- `nc_viewer_widget.py:2971` — `line_sequence = self._expand_subprograms(lines)`
  가 `M98`을 그 자리에 펼친 `(원본 줄번호 idx, 줄 텍스트)` 시퀀스를 만든다.
- 경로 노드마다 `"src_line": idx`(원본 줄번호)와 `"seq": seq_pos`(실행 순서)
  를 같이 박아 둔다(`nc_viewer_widget.py:3238` 외).
- 트레이스 자르기(`_render_segments`)와 가공시간(`seq_to_elapsed_sec`)은
  **seq 기준**이다.

반면 **재생기**는 문서 줄 번호를 그대로 따라간다:

- `NC_Tool_List.py:2797 _playback_tick()` — `self.src.textCursor().blockNumber()`
  에서 `+steps` 한 줄로 커서를 옮긴다. 즉 **문서 순서 = 재생 순서**.
- `NC_Tool_List.py:2658 source_cursor_changed()` → `viewer.set_cursor_line(줄번호)`.
- `nc_viewer_widget.py:3967` — `set_cursor_line()`은 그 줄번호를
  `line_to_seq`로 seq로 바꿔서 트레이스를 자른다. 그런데 `line_to_seq`는
  **"그 줄의 마지막 실행" seq**만 들고 있다(`nc_viewer_widget.py:2988`).

### 2.2 그래서 정확히 이렇게 깨진다

| 재생 커서가 있는 문서 줄 | line_to_seq가 주는 seq | 화면 |
|---|---|---|
| `M98 P0001` | M98 호출 시점 | 정상 |
| `M98` **다음 줄**(본 프로그램) | 서브프로그램 전체를 다 돈 **뒤**의 seq | 서브프로그램 툴패스가 **한 틱에 통째로** 나타남 → "전체 툴패스 보여주고 바로 넘어감" |
| `M30` | 실행 끝 | 전체 경로 |
| `M30` 뒤 `O0001` 본문 줄들 | 그 줄의 **마지막 실행** seq | 이미 끝난 프로그램인데 서브프로그램 구간이 **다시** 재생됨 → "마지막에 다시 서브프로그램 시뮬레이션" |

방향키도 같은 경로(`source_cursor_changed`)를 타므로 증상이 같다.

**결론: 파싱은 이미 맞고, 재생기만 문서 줄 기준이라 어긋난다.**
고칠 곳은 "재생/커서 진행의 단위"를 줄 번호에서 seq로 바꾸는 것이다.

### 2.3 부수적으로 같이 틀리는 것 (반복 호출 구간)

`modal_state_map` / `line_to_tool_map` / `line_to_coord_map` /
`line_to_c_rot`는 전부 **원본 줄번호 키에 마지막 실행 값**이 남는다
(`nc_viewer_widget.py:2985~3612`). `M98 P0001 L3`처럼 같은 본문을 여러 번
도는 구간에서는 1회차를 재생하는 중에도 **3회차의 좌표/공구/C각**이
표시된다. seq 기준으로 재생하려면 이 맵들도 seq 키가 필요하다.
(가공시간 `seq_to_elapsed_sec`은 이미 seq 키라 정상.)

---

## 3. 설계

핵심: **"지금 어디를 실행 중인가"의 단일 진실 소스를 문서 줄번호에서
`seq`(실행 순서 위치)로 옮긴다.** 에디터 커서는 그 seq에서 **파생**되는
표시일 뿐이 된다.

### 3.1 뷰어 — 실행 시퀀스와 seq 키 맵을 공개한다 (`nc_viewer_widget.py`)

```python
self.seq_to_line = []      # seq_pos -> 원본 줄번호(idx)
self.line_to_seq_all = {}  # idx -> [seq_pos, ...]  (모든 실행 시점)
self.line_to_seq = {}      # idx -> 마지막 seq_pos  (기존 유지, 회귀 봉인)

# 재생용 seq 키 맵 (기존 idx 키 맵은 그대로 두고 나란히 채운다)
self.seq_to_tool_map = {}
self.seq_to_coord_map = {}
self.seq_to_modal_state = {}
self.seq_to_c_rot = {}
```

채우는 방법은 **기존 대입문을 연쇄 대입으로 한 줄 늘리는 것**뿐이라
경로 계산 로직에는 손대지 않는다:

```python
# before
self.line_to_coord_map[idx] = final_pt
# after
self.line_to_coord_map[idx] = self.seq_to_coord_map[seq_pos] = final_pt
```

대상 지점(실측): `modal_state_map` 1곳(`:2985`), `line_to_c_rot` 5곳
(`:2992, 3305, 3399, 3404, 3429`), `line_to_tool_map` 3곳
(`:2999, 3002, 3240`), `line_to_coord_map` 7곳
(`:3310, 3323, 3505, 3533, 3585, 3603, 3612`) — 총 16곳.

새 메서드:

```python
def sequence_length(self)        # len(seq_to_line)
def set_cursor_seq(self, seq)    # set_cursor_line의 seq 버전 (실체)
def set_cursor_line(self, line)  # 줄 -> seq 해석 후 set_cursor_seq에 위임 (기존 API 유지)
def resolve_seq_for_line(self, line, near_seq=None)  # 줄 -> seq 해석 규칙(§3.5)
def line_at_seq(self, seq)       # seq -> 원본 줄번호
```

`set_cursor_seq()`는 현재 `set_cursor_line()` 본문을 그대로 옮기되
`modal_state_map`/`line_to_tool_map`/`line_to_coord_map`/`line_to_c_rot`
대신 seq 키 맵을 읽고, `seq_limit`을 `line_to_seq` 조회 없이 `seq`를
그대로 쓴다. `current_cursor_line`은 `seq_to_line[seq]`로 계속 채워
`_build_pick_cache`(`nc_viewer_widget.py:1863`) 등 기존 소비자를 유지한다.

### 3.2 재생기 — seq를 전진시킨다 (`NC_Tool_List.py`)

`App`에 재생 위치 상태를 둔다:

```python
self.playback_seq = 0     # 현재 실행 위치(seq)
self._seq_driven = False  # 커서를 우리가 옮기는 중 = source_cursor_changed 무시
```

`_playback_tick()` 재작성 (동작 골격은 유지):

```python
total = self.viewer.sequence_length()          # 0이면 기존 문서 줄 기준으로 폴백
target = min(self.playback_seq + steps, total - 1)
stop_seq = None
for seq in range(self.playback_seq + 1, target + 1):
    text = 그 seq의 줄 텍스트 (self.viewer.line_at_seq(seq) -> document)
    if line_stops_playback(text, needle, stop_text, stop_m00, stop_m01):
        stop_seq = seq; break
destination = stop_seq if stop_seq is not None else target
if destination != self.playback_seq:
    self._goto_seq(destination)                # 커서 이동 + viewer.set_cursor_seq
if stop_seq is not None or destination >= total - 1:
    self.pause_playback()
```

- **요구 1(M30 종료)은 여기서 자동 충족된다.** `_expand_subprograms`의
  본 프로그램 구간이 `main_end = m30_idx + 1`이라 시퀀스의 마지막 원소가
  바로 `M30` 줄이다. 그 뒤 서브프로그램 본문은 호출된 위치에만 존재한다.
- **요구 2·3(호출/복귀/중첩)도 자동 충족된다.** 시퀀스가 이미
  `… M98 → O0001 본문 … → M99 → M98 다음 줄 …` 순서다.

`_goto_seq(seq)`:

```python
self.playback_seq = seq
line = self.viewer.line_at_seq(seq)
self._seq_driven = True
try:
    커서를 line으로 이동 + ensureCursorVisible   # 기존 jump_to_process_line 본문
finally:
    self._seq_driven = False
self.viewer.set_cursor_seq(seq)
```

`source_cursor_changed()`는 `self._seq_driven`이면 즉시 반환하고,
아니면(사용자가 마우스로 클릭했거나 PG 매칭 밖 이동) 기존대로
`set_cursor_line(...)`을 부르면서 `playback_seq`를 §3.5 규칙으로 재동기화한다.

### 3.3 방향키 (`ProgramTextEdit`, `NC_Tool_List.py:1381`)

```python
class ProgramTextEdit(QPlainTextEdit):
    stepRequested = pyqtSignal(int)     # -1 / +1
    seq_step_enabled = False            # App이 켜고 끈다

    def keyPressEvent(self, event):
        if self.seq_step_enabled and event.key() in (Qt.Key_Down, Qt.Key_Up) \
                and not event.modifiers():
            self.stepRequested.emit(1 if event.key() == Qt.Key_Down else -1)
            return                       # 기본 줄 이동을 먹는다
        super().keyPressEvent(event)
```

`App`에서 `stepRequested → _step_seq(direction)`:
`_goto_seq(clamp(self.playback_seq + direction, 0, total - 1))`.
재생 중이었으면 재생 상태는 건드리지 않는다(기존 방향키와 동일).

`seq_step_enabled`는 **PG 매칭 모드 ON + 뷰어 모드 + 시퀀스 존재**일 때만
켠다(결정 B). PgUp/PgDn/Home/End/마우스 클릭은 기존 동작 그대로 두고
§3.5 규칙으로 seq를 되맞춘다.

### 3.4 나머지 진입점도 seq로 정렬

| 함수 | 지금 | 바꿀 것 |
|---|---|---|
| `playback_rewind()` (`:2829`) | `process_first_line[공정]` 줄로 점프 | `process_first_seq[공정]`으로 `_goto_seq` |
| `_jump_relative_tool()` F6/F8 (`:2845`) | 선택 공정 시작 **줄** 정렬 | 선택 공정 시작 **seq** 정렬 |
| `toggle_pg_match_mode()` (`:2662`) | 첫 선택 공정 줄로 커서 | 같은 공정의 seq로 `_goto_seq` |
| 필터 클릭 `process_activated` / Reset(`:1971`) | `jump_to_process_line(줄)` | 그대로 두되 §3.5로 seq 재동기화 |

뷰어에 `self.process_first_seq[공정키] = seq_pos`를 `process_first_line`
대입 옆(`nc_viewer_widget.py:2825`, `:3232`)에 나란히 채운다.

### 3.5 줄 → seq 해석 규칙 (마우스 클릭·경로 픽·필터 점프용)

같은 원본 줄이 여러 번 실행될 수 있으므로 해석이 필요하다. **규칙:
현재 `playback_seq`에서 가장 가까운 실행 시점**을 고르고, 동률이면 앞선
것. 그 줄이 한 번도 실행되지 않으면(M30 뒤 서브프로그램 본문을 호출 없이
클릭 등) **시뮬레이션 위치를 바꾸지 않는다**(커서만 그 줄에 놓인다).

- 반복 구간(`L3`) 2회차를 보는 중에 그 안의 다른 줄을 클릭하면 2회차
  맥락이 유지된다.
- 파일을 막 열었을 때(`playback_seq == 0`)는 자연히 첫 실행이 잡힌다.
- 기존 `line_to_seq`(마지막 실행)를 쓰는 `set_cursor_line`의 계약이
  바뀌는 셈이라, 기존 테스트
  `test_cursor_trim_uses_execution_order_with_subprograms`는
  `line_to_seq`를 **직접** 읽으므로 영향 없다(맵은 그대로 유지한다).

---

## 4. 결정 필요 항목 (승인 시 확정)

**A. M98이 하나도 없는 파일의 M30 처리 — 현행 유지(권장)**

`_expand_subprograms`는 파일에 `M98`이 없으면 확장을 건너뛰고 전체 줄을
그대로 돌려준다(v1.7.4 확정 A). 이 경우 시퀀스 = 문서 전체라 재생도
지금처럼 파일 끝까지 간다. 선반에서 `append_nc_programs`로 여러 프로그램을
이어붙이면 두 번째 프로그램이 첫 `M30` **뒤**에 오는데, 여기서 M30 종료를
강제하면 이어붙인 프로그램을 아예 시뮬레이션할 수 없다.
→ **`M98`이 있는 파일만 M30에서 끝난다**(요구 1의 실제 대상), 없는 파일은
현행 유지. 이어붙인 프로그램도 첫 M30에서 끊고 싶으면 알려주면 바꾼다.

**B. 방향키 seq 스텝의 적용 범위 — PG 매칭 모드에서만(권장)**

뷰어 모드 전체에 적용하면, 프로그램을 그냥 읽으려고 ↓를 눌렀을 때도
커서가 M30 뒤 서브프로그램 본문으로 튄다. 시뮬레이션 맥락인 **PG 매칭
모드에서만** seq 스텝을 켜고, 그 밖에서는 기존 문서 줄 이동을 유지한다.

**C. 서브프로그램 실행 중 표시 — 오버레이 한 줄 추가(권장)**

커서가 파일 아래쪽 본문으로 뛰면 "왜 여기로 갔지?"가 되기 쉬우므로,
좌표 오버레이에 `O0001 호출 중 (깊이 1)` 같은 한 줄을 붙인다. 본
프로그램일 때는 빈 문자열이라 기존 화면과 동일하다. 불필요하면 뺀다.

**D. 경로 클릭(픽)의 회차 정확도 — 이번엔 §3.5 규칙으로 처리(권장)**

픽 캐시(`_pick_src_lines`)에 seq를 같이 실으면 반복 구간에서 클릭한
**바로 그 회차**로 정확히 갈 수 있다. 다만 캐시 구조 변경이라 이번 범위
밖으로 두고, §3.5의 "현재 위치에서 가장 가까운 실행"으로 처리한다.
정확도를 지금 원하면 같이 넣는다.

---

## 5. 변경 파일 요약

**`nc_viewer_widget.py`**
- `__init__`(`:1425~`) / `clear()`(`:2199~`) / `process_nc_lines()` 초기화
  (`:2790~`)에 seq 키 맵 5종 + `seq_to_line` + `line_to_seq_all` +
  `process_first_seq` 추가·초기화.
- 파싱 루프(`:2977~3612`)의 16개 대입을 연쇄 대입으로 확장 (로직 무변경).
- `set_cursor_seq()` 신설, `set_cursor_line()`은 위임으로 축소,
  `sequence_length()` / `line_at_seq()` / `resolve_seq_for_line()` 추가.
- `elapsed_seconds_at_line()`의 seq 버전 `elapsed_seconds_at_seq()` 추가
  (기존 함수는 유지).

**`NC_Tool_List.py`**
- `ProgramTextEdit`에 `stepRequested` / `seq_step_enabled` 추가.
- `App`: `playback_seq` / `_seq_driven` 상태, `_goto_seq()` / `_step_seq()`
  신설, `_playback_tick()` seq 기반 재작성, `source_cursor_changed()` 가드,
  `playback_rewind()` / `_jump_relative_tool()` / `toggle_pg_match_mode()`
  seq 기준으로 전환, `sync_viewer_from_source()` 후 seq 재동기화.
- `APP_VERSION = '1.7.6'`, `NC_Tool_List.iss` `MyAppVersion`,
  `version_info.txt` 버전 갱신.

**문서**: `ABOUT_RELEASE_INSTRUCTIONS.md` 이력 추가,
`LATHE_MODE_GUIDELINES.md`에 "재생 = seq 기준" 한 줄 반영.

---

## 6. 테스트 계획 (`tests/test_nc_tool_list.py`, `SubprogramTests` 확장)

전부 기존 `_make_window_with_text()` 하니스(실제 `App` 창)로 돌린다.

1. `test_playback_enters_subprogram_and_returns` — 요구 2.
   `M6T1 / G43 / M98 P0001 / G01 X99 / M30 / O0001 / G01 X50 / M99` 에서
   1배속 틱을 반복하며 커서 줄 번호 궤적을 모으면
   `[… M98줄, O0001본문줄, M99줄, M98다음줄, M30줄]` 순서여야 한다.
2. `test_playback_stops_at_m30_not_at_file_end` — 요구 1.
   M30 줄에서 `play_timer.isActive()`가 False가 되고, 그 뒤 아무리 틱을
   돌려도 커서가 서브프로그램 본문으로 내려가지 않는다.
3. `test_playback_handles_nested_subprogram_calls` — 요구 3.
   O0001 안에서 O0002를 부르는 2단 중첩의 줄 궤적 검증.
4. `test_playback_repeat_l_shows_each_iteration_separately` — `L2` 구간에서
   같은 본문 줄이 궤적에 **두 번** 나오고, 1회차 시점의 좌표 오버레이가
   2회차 값이 아니어야 한다(§2.3 회귀).
5. `test_arrow_key_down_follows_execution_order` — 요구 4.
   `QTest.keyClick(window.src, Qt.Key_Down)` 궤적이 1번 테스트와 동일.
   ↑는 역순으로 복귀.
6. `test_arrow_key_falls_back_to_document_order_without_pg_match` — 결정 B 봉인.
7. **회귀 봉인** — `M98`이 없는 파일에서 기존 재생 테스트 4종
   (`test_playback_advances_expected_lines_per_tick` 등)이 그대로 통과.
   (seq == 줄번호라 값이 같아야 한다.)
8. `test files/1111.nc`가 있으면 스모크: 전체 시퀀스를 끝까지 재생해도
   예외 없이 M30에서 멈추고 커서 궤적 길이 == 확장 시퀀스 길이.

---

## 7. 회귀 위험과 대응

| 위험 | 대응 |
|---|---|
| `M98` 없는 기존 파일의 재생이 바뀜 | 시퀀스 == 문서 줄이라 seq == 줄번호. 테스트 7로 봉인 |
| seq 키 맵 추가로 메모리 증가 | 실측 최대 확장 19,653줄 × 4맵 = 무시 가능 |
| 커서 이동 신호 재귀(우리가 옮긴 커서가 다시 해석됨) | `_seq_driven` 가드 + 테스트 1로 확인 |
| 방향키를 가로채 텍스트 편집/선택이 막힘 | 뷰어는 읽기전용, 수정자 키 없는 ↓/↑만 가로챈다 |
| 픽/필터 클릭이 엉뚱한 회차로 감 | §3.5 규칙 + 결정 D |

## 8. 작업 순서

1. 승인 → 결정 A~D 확정을 이 문서에 반영.
2. 뷰어 seq 맵·API (§3.1) + 단위 테스트 4번.
3. 재생기 seq 전환 (§3.2, §3.4) + 테스트 1·2·3·7.
4. 방향키 (§3.3) + 테스트 5·6.
5. 결정 C/D 채택 시 추가 구현.
6. 전체 테스트 → 버전 1.7.6 → 설치본/포터블 빌드 → 이력 기록.
