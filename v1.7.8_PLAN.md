# v1.7.8 플랜 — 공구리스트 REMARK에 경보정(G41) 표기

작성: 2026-09-11 / 상태: **승인 대기** (아래 §5 결정 A~E 확인 필요)
기반: v1.7.7(`worktree-v177-lathe-spindle-feed`, PR #17 — 아직 미병합) 위에 쌓는다.
대상: 공구리스트 **REMARK 열**의 N번호 표기(화면 표 / PDF). 툴패스 계산·3D 뷰어·
가공시간 계산은 손대지 않는다.

---

## 1. 요구사항 (사용자 지시, 2026-09-11)

> "경보정 적용을 공구리스트에 추가 위치는 remark 부분에 추가 프로그램 중 n1,n3
> 이런식으로 표현 되는 부분에 해당 되는 N1공정에 경보정 G41이 입력 되어 있다면
> N1(G41), N2 이런형태로 표현 되게 N1(G41)중 (G41)은 굵은 글씨체로 표현 해줘"

정리:

1. REMARK 열의 N번호 목록(`N1, N3`)에서, **그 N 공정 안에 경보정 `G41`이 있으면**
   그 N번호 바로 뒤에 `(G41)`을 붙인다 → `N1(G41), N2`.
2. 붙인 `(G41)` 부분**만** 굵은 글씨로 보인다(N번호와 쉼표는 지금 그대로).

---

## 2. 현재 구조 (조사 결과)

| 항목 | 위치 | 비고 |
|---|---|---|
| 밀링 REMARK 생성 | `NC_Tool_List.py:1269 parse_program()` | `N_RE`(`N1(#1: Tool Change)`) 기준 N번호를 공구별로 누적. **괄호 주석 줄만 버퍼링**하고 코드 줄은 읽지 않는다 |
| 선반 REMARK 생성 | `NC_Tool_List.py:359 parse_lathe_program()` | `LATHE_N_RE`(`N1` 단독 줄) 기준, 블록 = 다음 N 줄 전까지 |
| 화면 표 셀 | `App.set_table_row()` (`:3326`) | 평문 `QTableWidgetItem` — 셀 일부만 굵게 하는 기능은 없음(delegate 없음) |
| 선반 동적 열 폭 | `App._update_lathe_dynamic_col_width()` (`:3104`) | 보통 글꼴로 실측 — 굵은 부분이 생기면 폭을 약간 더 잡아야 함 |
| 밀링 PDF / 선반 PDF | `make_pdf_table()` (`:606`) / `make_lathe_pdf_table()` (`:734`) | 셀은 평문 문자열. **본문 전체가 이미 굵은 글꼴**(`base_pdf_table_style`의 `FONTNAME (0,1)~(-1,-1) bold_font` = 맑은 고딕 Bold) |
| 클립보드 복사 | `App.copy_table()` (`:3643`) | 탭 구분 평문(엑셀 붙여넣기용) |
| REMARK를 다시 읽는 곳 | 없음 | 3D 뷰어 필터 라벨은 NO/NAME/INSERT만 사용 — REMARK 표기가 바뀌어도 영향 없음 |

---

## 3. 실측 (예제 파일 `test files/`)

| 파일 | 모드 | G41 위치 | 이 플랜 적용 후 REMARK |
|---|---|---|---|
| `5ax-A To C.nc` / 저장소 `ncdata.nc` | 밀링 | N8 블록(`G41 X17.309 F1500. D05` 2회) | T5 행 `N8, N9, N10` → **`N8(G41), N9, N10`** |
| `O1699.nc` | 선반 | N9 블록(`G41X126.57` 3회) | N9 행 → **`N9(G41)`** |
| `O4812.nc` | 선반 | N7 블록(`G41X.925C-.008F112.` 등 6회) | **`N7(G41)`** |
| `O4006.nc` | 선반 | N1 블록 2회 | **`N1(G41)`** |
| `sub_pg test.nc` | 선반 | **M30 뒤 서브프로그램 O0002/O0003 안에만** 있음. 이 둘을 `M98`로 부르는 건 **N2**(T0707), N1(T0404)은 O0001만 호출 | **`N2(G41)`**, N1은 그대로 `N1` (§4.2, 결정 D) |
| `O2222.nc`, `O4811.nc`, `TurnMill Test.nc`, `K10M41017…O1017.NC` | — | G41 없음 | 변화 없음 |

- 모든 예제에서 **G42는 0건**이다(결정 C 참고).
- 기존 테스트 픽스처(`REAL_NC_SAMPLE`, 선반 `LATHE_TOOLLIST_SOURCE` 등)에는 G41이 없어
  **기존 REMARK 기대값(`N2, N7`, `N2, N3` 등)은 하나도 바뀌지 않는다.**

---

## 4. 설계

### 4.1 검출 규칙

- N 블록의 **코드부(주석 걷어낸 뒤, `code_without_comments()`)**에서 `G41` 워드를 찾는다.
  주석 속 문구(예: 인서트 설명)는 절대 읽지 않는다(v1.7.7과 같은 원칙).
- 정규식(신설): `G0*4([12])(?![\d.])` — `G41`, `G041`, `G1G41X..`, `G41X.077F22.`는 잡고,
  `G41.1` 같은 다른 확장 코드나 `G410`류는 잡지 않는다.
- `G40`(보정 취소)은 표기하지 않는다. 블록 안에 한 번이라도 G41이 나오면 그 N은 `(G41)`.

### 4.2 N 블록 범위와 서브프로그램 귀속 (결정 D)

`sub_pg test.nc`처럼 G41이 **M30 뒤 서브프로그램 본문에만** 있는 경우가 실제로 있다.
지금 선반 파서의 블록 규칙("다음 N 줄 전까지")을 그대로 쓰면, M30 뒤 서브프로그램
전체가 **마지막 N 블록**에 붙어 버린다(이 예제에선 우연히 N2가 마지막이라 맞지만,
N1이 O0002를 불렀다면 틀린 N에 G41이 붙는다).

권장안:

1. 경보정 검출용 블록 범위 = N 헤더 다음 줄 ~ **다음 N 헤더 또는 다음 `O<번호>` 헤더 줄** 전.
   (M30 뒤 서브프로그램은 항상 `O<번호>` 헤더로 시작하므로 마지막 N에 섞이지 않는다.)
2. 블록 코드 안에 `M98 P<n>`이 있으면 `O<n>` 본문(`O<n>` 헤더 ~ `M99` 또는 다음 O 헤더)을
   **그 N 블록의 일부로 함께 스캔**한다. 서브프로그램 안의 `M98`도 재귀로 따라가되,
   방문 표시로 순환 호출(O1→O2→O1)은 한 번만 본다.
3. 이 범위 규칙은 **경보정 검출에만** 쓴다. TOOL NO / 홀더 / 인서트 / SO / SPINDL / FEED
   추출은 지금 규칙 그대로 둔다(범위 밖 변경 금지). SPINDL/FEED에도 같은 한계가
   있다는 점은 기록만 남기고 이번엔 고치지 않는다.
4. 뷰어의 `_expand_subprograms()`(`nc_viewer_widget.py`, 뷰어 클래스 메서드)는
   재사용하지 않는다 — 공구리스트는 뷰어 모듈 import가 실패해도(폴백 화면) 동작해야
   하므로, `NC_Tool_List.py`에 O번호 → 본문 줄 범위만 찾는 작은 헬퍼를 따로 둔다.

### 4.3 표기 형식 (데이터)

- 표·편집·복사·PDF가 공유하는 행 데이터는 **평문 문자열** 그대로다:
  `"N8(G41), N9, N10"`. 굵게는 **그리는 단계**에서만 입힌다(데이터에 HTML/마크업 없음).
- 같은 공구가 여러 N에 쓰이면 N별로 따로 판단한다: `N2(G41), N5`.
- 결정 C가 "G42 포함"이면: `N3(G42)`, 한 블록에 둘 다 있으면 등장 순서대로 `N3(G41/G42)`.
- 공구번호가 비는 빈 행(v1.6.6)은 지금처럼 빈칸.

### 4.4 화면 표 — `(G41)`만 굵게

- REMARK 열에만 **전용 `QStyledItemDelegate`**를 건다. 셀 텍스트에서
  `\(G4[12](?:/G4[12])?\)` 부분만 `<b>…</b>`로 감싼 뒤 `QTextDocument`로 그린다.
- 배경/선택 강조/포커스 테두리는 Qt 스타일이 그리게 두고(텍스트만 비운 채
  `CE_ItemViewItem`), 글자색은 셀 상태에 맞춰 팔레트(`Text`/`HighlightedText`)를 따른다
  — 다크/라이트 테마 QSS(`selection-color`)와 셀 좌우 패딩(`TABLE_CELL_PADDING_PX`)이
  지금과 똑같이 보이게 한다.
- `(G41)` 표기가 **없는 셀은 기존 기본 그리기를 그대로** 호출한다 → G41이 없는 프로그램은
  화면이 픽셀 단위로 지금과 같다.
- 선반 동적 열 폭(`_update_lathe_dynamic_col_width`)은 `(G41)` 부분을 **굵은 글꼴 폭으로**
  재서 셀이 잘리지 않게 한다(v1.6.6 "선반 표 말줄임 금지" 유지). 밀링 표는 기존처럼 패널
  폭에 맞춰 축소되는 방식 그대로다.
- 행 편집 대화상자(`show_row_editor`)는 평문 입력 그대로 — 사용자가 직접 `(G41)`을
  적어도 저장하면 똑같이 굵게 보인다.

### 4.5 PDF (결정 B)

PDF는 **본문 전체가 이미 굵은 글꼴**이라 "(G41)만 굵게"가 그대로는 구분되지 않는다.
권장안: REMARK 셀에 `(G41)`이 있을 때만 그 셀을 `Paragraph`로 바꿔 **N번호 부분은 보통
글꼴(맑은 고딕), `(G41)`만 굵은 글꼴**로 찍는다(글자색/크기/정렬은 기존 REMARK 셀과
동일: 파란색 6.5pt, 왼쪽 정렬). 표기가 없는 REMARK 셀은 지금처럼 평문 문자열 —
G41 없는 프로그램의 PDF는 지금과 완전히 같다. 밀링/선반 PDF 둘 다 같은 헬퍼를 쓴다.

### 4.6 클립보드 복사 (결정 E)

권장안: 지금처럼 **탭 구분 평문**(`N8(G41), N9`) — 엑셀 붙여넣기 동작 무변화.

---

## 5. 결정 사항 (승인 시 확인 부탁)

| # | 결정 | 권장안 | 대안 |
|---|---|---|---|
| **A** | 적용 대상 | **밀링 + 선반 둘 다**(두 툴리스트 모두 REMARK가 `N1, N3` 형식) | 선반만 / 밀링만 |
| **B** | PDF에서 굵게 구분 | **REMARK 셀만 N번호 보통 글꼴 + `(G41)` 굵게** | 전부 굵게 유지 + `(G41)`만 빨간색 / PDF는 서식 없이 표기만 |
| **C** | G42(우측 보정)도 표기 | **G41·G42 모두**(`N3(G42)`, 둘 다면 `N3(G41/G42)`) — 예제엔 G42 0건 | G41만 |
| **D** | 서브프로그램 안의 G41 | **`M98`로 부른 N에 귀속**(§4.2, 경보정 검출에만 적용) | 단순 "다음 N 전까지"(v1.7.7 SPINDL/FEED와 동일, 마지막 N에 잘못 붙을 수 있음) |
| **E** | 클립보드 복사 | **평문 유지** | HTML 서식도 함께 복사(엑셀에 붙이면 `(G41)` 굵게 유지) |

---

## 6. 변경 파일 / 작업 목록

1. `NC_Tool_List.py`
   - `RADIUS_COMP_RE`(`G0*4([12])(?![\d.])`) 신설, 서브프로그램 본문 찾기용
     `SUBPROGRAM_HEADER_RE`(`^\s*O(\d+)`) / `M98 P<n>` 정규식 신설
   - `_subprogram_bodies(lines)` — O번호 → 본문 줄 범위 헬퍼
   - `_radius_comp_by_n(lines, n_header_re)` — N번호 → 경보정 코드 목록
     (§4.1 + §4.2 규칙). 밀링은 `N_RE`, 선반은 `LATHE_N_RE`를 넘긴다 — 기존 밀링/선반
     정규식·파서 로직은 고치지 않고 **새 함수만 추가**(지침 §0-2, §0-4)
   - `_format_remark(n_labels, comp_by_n)` — `N8(G41), N9` 문자열 생성
   - `parse_program()`(밀링) / `parse_lathe_program()`(선반): REMARK 조립만
     `', '.join(...)` → `_format_remark(...)`로 교체
   - `RemarkCompDelegate(QStyledItemDelegate)` — §4.4, `_configure_table_columns()`에서
     REMARK 열에만 설정
   - `_update_lathe_dynamic_col_width()` — `(G41)` 부분 굵은 글꼴 폭 반영
   - `_pdf_remark_cell(text, fonts)` — §4.5, `make_pdf_table()` / `make_lathe_pdf_table()`의
     REMARK 셀에만 적용
   - `APP_VERSION = '1.7.8'`, `APP_BUILD_DATE` (**준비 단계에서 이미 반영됨**)
2. `NC_Tool_List.iss`, `version_info.txt` — 1.7.8 (**준비 단계에서 이미 반영됨**)
3. `tests/test_nc_tool_list.py` — 신규 테스트(§7)
4. `LATHE_MODE_GUIDELINES.md` §7 — REMARK 경보정 표기 규칙 추가
5. `ABOUT_RELEASE_INSTRUCTIONS.md` — v1.7.8 이력 추가
6. 회귀 확인: `pytest tests/test_nc_tool_list.py` 전체, 미수정 v1.7.7과 실패 집합 비교
   (공유 QSettings 때문에 생기는 환경 의존 실패는 기존과 동일해야 함)

---

## 7. 테스트 (신규)

1. 밀링: N 블록 코드에 `G41 X.. D05`가 있으면 그 공구 REMARK가 `N8(G41), N9, N10`
2. 선반: `G41X126.57`이 있는 N 블록 → `N9(G41)`, 없는 블록은 `N`번호만
3. 같은 공구가 G41 있는 N / 없는 N에 쓰일 때 N별로 따로 표기(`N2(G41), N5`)
4. 주석 속 `G41` 문구(예: `( G41 COMP )`)는 무시
5. `G40`만 있는 블록, `G41.1`류는 표기 안 함
6. 서브프로그램 귀속: N2가 `M98 P0002`로 부르는 O0002 안의 G41 → `N2(G41)`, O0002를
   부르지 않는 N1은 `N1` 그대로, 그리고 M30 뒤 본문이 마지막 N에 섞이지 않음(결정 D)
7. 순환 호출(O1 → O2 → O1)에서도 멈추지 않음
8. (결정 C 채택 시) G42 → `N3(G42)`, 둘 다 → `N3(G41/G42)`
9. 화면: REMARK delegate가 `(G41)` 셀에서만 굵은 서식 HTML을 만들고, 표기 없는 셀은
   기본 그리기로 넘김
10. PDF: `(G41)` 있는 REMARK 셀만 `Paragraph`(굵은 글꼴 구간 포함), 없는 셀은 평문
    문자열 그대로 / 밀링·선반 PDF 모두 생성 성공
11. 실제 예제 스모크: `test files/O1699.nc`, `sub_pg test.nc`(파일 없으면 스킵)

---

## 8. 준비 상태 (2026-09-11)

- 작업 브랜치: `worktree-v178-prep` (v1.7.7 `2e3f2a5` 기반, worktree
  `.claude/worktrees/v178-prep`)
- 버전 표기 3곳(`APP_VERSION`/`APP_BUILD_DATE`, `NC_Tool_List.iss`, `version_info.txt`)은
  1.7.8로 이미 올려 두었다.
- v1.7.7(PR #17)이 먼저 병합돼야 이 브랜치를 메인에 올릴 수 있다.
- 승인 후 §6 순서대로 구현한다. 빌드(PyInstaller/Inno Setup)는 사용자 지시가 있을 때만.
