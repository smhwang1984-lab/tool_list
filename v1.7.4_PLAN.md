# v1.7.4 업데이트 플랜 (2차 — 결정사항 확정 반영, 승인 대기)

원본 요구: 사용자 지시 (2026-09-07)

> 1. M98(서브프로그램 호출) M99(호출 및 서버 프로그램 호출) M30 % 이후에 O번호로
>    서브프로그램 배치 됨. 배치 순서는 O0001 다음 O0002 O0003 순으로 배치 됨.
> 2. M98 P0001 경우 현재 위치에서 서브프로그램 O0001을 해서 가공,
>    M99에서 다시 M98 P0001 바로 다음으로 시작해야 함. 그리고 다시 만났을 때 그대로 적용 필요.
> 3. 서브프로그램 호출은 **선반 및 밀링 2군데 다** 적용이 필요함.
> 4. 만약 본 프로그램에서 M99 P1 또는 P2일 시 P1 = N1, P2 = N2번 시퀀스에서
>    시작하는 명령어임.

**작업 기준 브랜치**: `agent/drag-drop-installer` (v1.7.3). `master`는 v1.4.2로 오래된
상태이므로 기준으로 쓰지 않는다.

**확정 사항 (2026-09-07 사용자 승인)** — 1차 플랜의 승인 항목 A~E 회신:

| # | 결정 |
|---|---|
| **A** | M98이 **없는** 파일은 확장하지 않고 M30 뒤까지 **그대로 그린다** (프로그램 이어붙이기 보존) |
| **B** | `%` 뒤 O헤더를 별도 프로그램으로 보는 보조 규칙은 **넣지 않는다** |
| **C** | 커서·재생 트리밍(및 시간 표시)을 **실행 순서(seq) 기준으로 옮긴다** |
| **D** | 본 프로그램의 `M99 P<n>`은 **`N<n>`을 찾아가는 점프**다. `M99 P3`이면 N1·N2를 건너뛰고 **N3부터 시작**한다 |
| **E** | 반복은 **`M98 P0001 L2` 형태일 때만**이다. `P51002`류 축약형은 반복으로 보지 않는다 |

검증용 예제 프로그램: `test files/1111.nc` (사용자 제공, 부록 A 참조).

**전제**: `LATHE_MODE_GUIDELINES.md` §0 완전 분리 원칙. 이번 변경은 "선반 때문에 밀링을
고치는" 것이 아니라 **사용자가 밀링에도 요구한 신규 기능**이므로, 확장기(순수 텍스트 처리)만
공용으로 올리고 선반/밀링의 **경로 계산 분기는 한 줄도 서로 섞지 않는다.**

---

## 0. 현재 상태 요약 (v1.7.3)

| 항목 | 현재 |
|---|---|
| `_expand_lathe_subprograms()` (`nc_viewer_widget.py:2515`) | **선반 전용**. M30 뒤 `O<번호>` 헤더를 서브프로그램으로 등록하고 `M98 P<번호> [L<반복>]` 자리에 펼침 |
| 서브프로그램 본문 범위 | 헤더 다음 줄 ~ **다음 O헤더(또는 EOF)**. **M99를 본문 끝으로 보지 않음** |
| `M99` | **전혀 해석하지 않음** (그냥 지나가는 줄) |
| `M99 P####` | 미지원 |
| 밀링 | `list(enumerate(lines))` 그대로 (`:2831`). 확장도, M30 경계도 없음 → **M30 뒤 서브프로그램 본문이 본 프로그램에 이어붙은 것처럼 그려짐** |
| 경로 노드 키 | `src_line`(원본 줄번호)만 보관. `seq`(실행 순서) 없음 |
| 커서/재생 트리밍 | `_render_segments()` (`:3660`) 가 `node["src_line"] > line_limit` 에서 `break` |

### 이미 존재하는 결함 (이번에 같이 고쳐야 기능이 화면에 제대로 보임)

서브프로그램은 M30 **뒤**에 있으므로 원본 줄번호가 본 프로그램보다 **크다**. 확장 시퀀스의
`src_line`은 `… 20, 21, [100, 101, 102], 22, 23 …` 처럼 **단조 증가가 아니다.**
그런데 `_render_segments()`는 `src_line > line_limit`에서 `break`한다.
→ 커서를 22번 줄에 올리면 첫 M98(= src_line 100)에서 그리기가 끊긴다.
**선반에서 이미 이 증상이 있고**, 밀링까지 확장하면 그대로 따라온다. (항목 3에서 처리)

---

## 항목 1. 서브프로그램 확장기를 "호출 스택 인터프리터"로 교체

### 왜 교체가 필요한가

현재 확장기는 `expand(index_range, depth)` **재귀 제너레이터**로, "범위를 통째로 흘린다"는
구조다. 여기에는

- M99에서 **본문을 중간에 끝내기**,
- M99 P####로 **다른 줄(N 라벨)로 점프하기**

를 표현할 자리가 없다. 범위 반복이 아니라 **프로그램 카운터(pc) + 호출 스택**이 필요하다.

### 새 구조 (`nc_viewer_widget.py`)

`_expand_lathe_subprograms()` → **`_expand_subprograms()`** 로 이름을 바꾸고
(선반 전용이 아니게 되므로) 다음으로 다시 쓴다. 반환 계약은 **그대로 유지**한다 —
`[(원본 줄번호, 줄 텍스트), ...]`.

```python
_M99_RE     = re.compile(r"M99(?!\d)")
_M99_P_RE   = re.compile(r"P0*(\d+)(?!\d)")     # M98의 P와 같은 형식
_N_LABEL_RE = re.compile(r"^\s*N0*(\d+)")       # 라벨용 — 뒤에 지령이 붙어도 잡는다
                                                # (LATHE_N_LINE_RE는 "N1"만 잡는
                                                #  공정 경계용이라 목적이 다르다)
_SUBPROGRAM_MAX_DEPTH = 10                      # 기존 _LATHE_SUBPROGRAM_MAX_DEPTH 승격
_SUBPROGRAM_MAX_STEPS = 200_000                 # 무한 루프 방지 상한 (신설)
```

1. **프로그램 테이블 만들기** — 첫 M30을 찾아 `main = (0, m30_idx+1)`,
   그 뒤의 `O<번호>` 헤더마다 `sub[번호] = (헤더+1, 다음 헤더 또는 EOF)`.
   (요구 1의 "O0001 → O0002 → O0003 배치 순서"가 이 스캔이다. 배치 순서 자체는
   실행에 영향이 없고 **번호로 찾는다** — `M98 P0002`는 O0002를 부른다.)
2. **N 라벨 색인** — 프로그램 구간마다 `{N번호: 줄번호}`를 미리 만든다. 같은 N이 두 번
   나오면 **먼저 나온 것**을 쓴다(Fanuc 관례).
3. **실행 루프** — 프레임 스택으로 돈다.

```python
Frame = (start, end, pc, ret_pc, repeat_left)   # ret_pc: 복귀할 호출자 pc

frames = [Frame(main_start, main_end, pc=main_start, ret_pc=None, repeat_left=1)]
steps = 0
while frames and steps < _SUBPROGRAM_MAX_STEPS:
    f = frames[-1]
    if f.pc >= f.end:                    # 본문 끝 = 암묵적 M99
        _finish_frame(); continue
    raw = lines[f.pc]; code = 주석제거·대문자
    yield (f.pc, raw); f.pc += 1; steps += 1

    if M99 in code:
        p = M99의 P번호
        if p is not None:                # 항목 4
            ...N라벨로 점프...
        _finish_frame(); continue

    if M98 in code and (p := M98의 P번호) is not None and len(frames) < MAX_DEPTH:
        body = sub.get(p)
        if body:
            frames.append(Frame(body.start, body.end, pc=body.start,
                                ret_pc=f.pc, repeat_left=L or 1))
```

`_finish_frame()`:

- `repeat_left -= 1`; 남아 있으면 **pc를 본문 시작으로 되돌리고 프레임을 유지**
  (= `M98 P… L3` 반복. 기존 동작과 같은 결과).
- 다 썼으면 **pop** 하고, 호출자의 pc는 이미 `ret_pc`(= M98 다음 줄)를 가리키고 있다.
  → **요구 2의 "M99에서 M98 P0001 바로 다음으로 복귀"** 가 정확히 이것이다.
- 스택이 비면(본 프로그램의 M99 / 본문 끝) 종료.

### 요구별 대응

| 요구 | 처리 |
|---|---|
| M98 P0001 → O0001 실행 | 프로그램 테이블에서 1번 조회. `P0*(\d+)`이므로 `P1` / `P0001` / 공백 없는 `M98P0001` 모두 같다 (예제 `1111.nc`가 공백 없는 형태 — 부록 A) |
| 반복 | **확정 E**: `L` 워드가 있을 때만(`M98 P0001 L2` → 2회). `L`이 없으면 무조건 1회이며, `P51002`처럼 P 앞자리를 반복수로 해석하는 Fanuc 축약형은 **쓰지 않는다** |
| M99 → 호출 다음 줄 복귀 | `ret_pc`가 이미 M98 **다음** 줄 |
| "다시 만났을 때 그대로 적용" | pc가 다시 그 M98에 오면 프레임을 또 push한다. 반복문(M99 P로 되돌아오는 루프) 안이어도 매번 펼쳐진다 |
| 선반 + 밀링 둘 다 | 항목 2 |
| 본 프로그램 M99 P1 → N1 | 항목 4 |

### 기존 동작 유지 (회귀 방지)

- 정의되지 않은 P번호, 깊이 초과 → **조용히 무시**하고 다음 줄 진행 (기존과 동일).
- 호출되지 않은 서브프로그램(예 O9001)은 시퀀스에 **전혀 안 나온다** (기존과 동일).
- 원본 줄번호를 그대로 들고 나오므로 `src_line` 기반 커서 동기화 계약 불변.

### 달라지는 점 (의도된 수정)

- **본문 끝이 M99가 된다.** 지금은 다음 O헤더 직전까지 전부 실행하므로,
  `M99` 뒤에 남은 줄이 있으면 잘못 실행됐다. 이제 M99에서 끊는다.

---

## 항목 2. 밀링에도 확장 적용 — 단, "M98이 있을 때만"

### 변경 지점

`nc_viewer_widget.py:2831`

```python
# 현재
lathe_sequence = self._expand_lathe_subprograms(lines) if is_lathe else list(enumerate(lines))
# 변경
line_sequence = self._expand_subprograms(lines)          # 선반/밀링 공통
```

`_lathe_n_blocks(line_sequence)` 호출(`:2835`)은 지금처럼 `is_lathe`일 때만 돈다.
**선반 공정 판정·밀링 M6 공정 판정 로직은 전혀 건드리지 않는다.**

### ⚠ 회귀 위험과 방어 장치 (중요)

M30 뒤를 서브프로그램으로 보고 본 프로그램을 M30에서 끊으면,
**NC_Tool_List의 "프로그램 이어붙이기"(`append_nc_programs`, `NC_Tool_List.py:1178`)로 만든
파일이 뷰어에서 두 번째 프로그램부터 사라진다.** 이어붙인 형태가

```
%
O1001
 …
M30
%

%
O1002        ← 이것도 "M30 뒤의 O헤더"라서 서브프로그램으로 오인된다
 …
M30
%
```

이기 때문이다. (이 위험은 선반에서 이미 존재한다 — 현재 확장기가 M30에서 무조건 자른다.)

**방어 규칙 (신설)**: 파일 전체에 **M98이 하나도 없으면 확장을 아예 하지 않고
`list(enumerate(lines))`를 그대로 돌려준다.**

- 밀링: M98 없는 기존 모든 파일이 **지금과 완전히 동일한 시퀀스** → 회귀 0.
- 선반: M98 없는 파일에서 M30 뒤 내용이 다시 보이게 된다 → **동작 변경.
  확정 A로 이렇게 간다** (이어붙이기 보존).
- M98이 있으면서 동시에 이어붙인 파일은 여전히 두 번째 프로그램이 잘린다.
  `%` 뒤 O헤더를 별도 프로그램으로 보는 보조 규칙은 **확정 B로 넣지 않는다.**
  (예제 `1111.nc`도 서브프로그램 앞에 `%`가 없어 이 규칙이 필요 없다 — 부록 A.)

### 밀링 부수 영향

- 서브프로그램 본문에 `M6`가 있고 그 서브프로그램이 2회 호출되면 **공정이 2개 열린다.**
  실무에서 흔한 형태는 아니고, "실행 그대로"라는 원칙에는 맞다. 문서에만 남긴다.
- G43 공구길이보정, G68.2/G53.1(5축), 4축 B 회전, 고정 사이클 상태값은 전부 모달 변수로
  루프가 이어 받으므로 서브프로그램 진입/복귀에서 **따로 손댈 것이 없다**(실기와 동일).

---

## 항목 3. 커서·재생 트리밍을 "실행 순서(seq)" 기준으로

서브프로그램이 들어오면 `src_line`이 단조 증가하지 않아, 지금의
`_render_segments()`(`:3660`) `break`가 **첫 M98에서 그리기를 끊는다.**
(선반에서 이미 발생 중인 결함이고, 밀링 확장 시 그대로 따라온다.)

### 변경

1. `process_nc_lines()`의 모든 `tool_paths[...].append({...})` 지점에 **`"seq": seq_pos`** 를
   추가한다 (값만 추가, 기존 키는 불변).
2. `line_to_seq[idx] = seq_pos` 맵을 새로 만든다 — 같은 줄이 여러 번 실행되면
   **마지막 실행**이 남는다 (v1.6.6 docstring이 정한 "마지막으로 실행된 위치" 계약과 일치).
3. `_render_segments(path_data, line_limit=None)`의 비교 기준을 `seq_limit`으로 바꾼다.
   호출부(`set_cursor_line` `:3765`, `_collect_pick_segments` `:1807`)에서
   `seq_limit = line_to_seq.get(line_index)`로 변환해 넘긴다.
   `line_to_seq`가 비어 있으면(= 확장이 없었던 파일) `seq_pos == idx`라 **결과가 동일**하다.
4. **확정 C**: 시간 표시도 실행 순서 기준으로 옮긴다. `_compute_machining_times()`(`:3486`)의
   누적 시간을 `src_line`이 아니라 **`seq` 키로** 쌓고(`seq_to_elapsed_sec`),
   `elapsed_seconds_at_line()`은 `line_to_seq`로 변환해 조회한다. 그래야 서브프로그램
   반복 구간에서 시간이 되감기는 것처럼 보이지 않는다.
   합계(`total_time_sec`)는 노드 전체를 더하므로 지금도 정확하고, 변경 후에도 같다.
   `_elapsed_line_keys` 이분 탐색은 seq 키 정렬로 그대로 유지한다.

---

## 항목 4. 본 프로그램의 `M99 P<n>` = `N<n>` 시퀀스로 점프

확정 D: **`M99 P<n>`은 `N<n>`을 찾아가는 점프다.** `M99 P3`이면 N1·N2를 건너뛰고
**N3 블록부터 이어서 실행**한다. 세 경우를 같은 라벨 색인으로 처리한다.

| 위치 | 동작 |
|---|---|
| 본 프로그램의 `M99 P3` | 본 프로그램의 `N3` 라벨로 pc를 옮긴다 (N1·N2 건너뜀). 라벨이 없으면 프로그램 종료 |
| 서브프로그램 안의 `M99 P2` | 프레임을 pop하고 **호출자**의 `N2` 라벨로 pc를 옮긴다. 라벨이 없으면 M98 다음 줄로 폴백(= 일반 M99) |
| 본 프로그램의 `M99` (P 없음) | **무시하고 다음 줄로 진행한다.** ※ 아래 설명 |

> **P 없는 M99를 "종료"가 아니라 "무시"로 바꾼 이유** — 1차 플랜에서는 종료를 제안했으나,
> 확정 D가 "본 프로그램의 M99 = **N번호를 찾는 호출**"이므로 **찾을 N이 없는 M99에는
> 지시할 동작 자체가 없다.** 여기서 프로그램을 끊어 버리면 뒤에 남은 본 프로그램이
> 조용히 안 그려져 **그림이 사라지는 쪽으로 틀린다.** 무시하면 최악이라도 여분의 줄을
> 더 그릴 뿐이라 손실이 없다. (Fanuc 실기의 "선두로 무한 반복"은 뷰어에서 상한까지
> 그리다 잘린 그림이 되어 무의미하므로 채택하지 않는다.)

**라벨 색인 규칙**: 프로그램 구간마다 `{N번호: 줄번호}`. 같은 N이 두 번 나오면 먼저 나온
것을 쓴다. `_N_LABEL_RE = ^\s*N0*(\d+)` — `1111.nc`의 `N1 `(뒤 공백), `N2 `처럼 뒤에
공백·주석·지령이 붙어도 잡는다. (공정 경계용 `LATHE_N_LINE_RE`는 `^\s*N(\d+)\s*$`로
"N만 있는 줄"만 잡는 별개 목적이므로 **재사용하지 않고 새 상수를 둔다.**)

**무한 루프 방어**: `_SUBPROGRAM_MAX_STEPS`(20만 줄) 상한에 걸리면 그 자리에서 확장을
멈춘다. `M99 P1`이 자기 앞으로 되돌아오는 프로그램은 상한까지만 그려지고 앱은 죽지 않는다.
(`1111.nc` 확장 결과가 19,653줄이므로 20만은 실사용의 10배 여유다 — 부록 A.)

---

## 항목 5. 손대지 않는 것 (명시)

- **공구리스트 파서** (`NC_Tool_List.py` `parse_program` `:1071`, `parse_lathe_program` `:171`).
  공구 정보는 본 프로그램의 N블록 주석에서 읽으며 서브프로그램에는 공구 주석이 없다.
  여기에 M98 확장을 넣으면 반복 호출 때 같은 공구가 중복 등록된다. **범위 밖으로 둔다.**
- 선반 좌표계·지름 처리·G12.1 극좌표·선반 고정 사이클 (`LATHE_MODE_GUIDELINES.md` §1~§7).
- 밀링 회전행렬(G68.2/G53.1/4축 B), G17/G18/G19 원호, G43 유효성.

---

## 테스트 계획 (`tests/test_nc_tool_list.py`)

신규:

1. `test_milling_m98_expands_subprogram_and_returns_to_next_line`
   — 밀링 3축에서 `M98 P1` → O0001 본문 → **M98 다음 줄**로 복귀한 좌표까지 확인.
2. `test_milling_without_m98_keeps_flat_sequence`
   — **M98이 없는 밀링 프로그램의 시퀀스가 `list(enumerate(lines))`와 완전히 동일**(회귀 봉인).
3. `test_subprogram_body_ends_at_m99_not_next_o_header`
   — `M99` 뒤·다음 O헤더 앞의 줄이 경로에 **없어야** 한다.
4. `test_same_m98_called_twice_expands_twice`
   — 같은 M98을 두 번 만나면 본문이 두 번 그려진다(요구 2 후단).
5. `test_multiple_subprograms_dispatch_by_number`
   — O0001/O0002/O0003을 섞어 부르고 각각의 좌표가 정확한 순서로 나오는지.
6. `test_m99_p_returns_to_caller_n_sequence`
   — 서브프로그램 `M99 P2` → 호출자 `N2` 블록부터 재개.
7. `test_main_program_m99_p1_jumps_to_n1_and_stops_at_step_limit`
   — 요구 4 + 무한 루프 상한에 걸려도 예외 없이 끝나는지.
8. `test_cursor_trim_uses_execution_order_with_subprograms`
   — M98 뒤 줄에 커서를 놓아도 경로가 M98에서 끊기지 않는지(항목 3).

회귀 (기존, 전부 통과 확인):

- `test_lathe_m98_expands_subprogram_after_m30_and_ignores_uncalled_ones` (v1.6.6)
- 선반 공정/시간/극좌표 테스트 전부, 밀링 5축/4축/고정사이클/재생 테스트 전부
- `pytest tests/test_nc_tool_list.py` 전량 (가이드라인 §0-3)

---

## 릴리스 작업 (구현 승인 후)

- `NC_Tool_List.py` `APP_VERSION` `1.7.3` → `1.7.4`, `version_info.txt` 갱신
- `LATHE_MODE_GUIDELINES.md` §8에 "M98/M99는 v1.7.4에서 선반·밀링 공용 확장기로 이동" 기록
- `ABOUT_RELEASE_INSTRUCTIONS.md` 버전 히스토리 추가
- 설치본/포터블 빌드는 **별도 지시 후** 수행

---

## 2차 승인 요청 — 1차에서 달라진 곳만

A~E는 확정되어 위(머리말)와 각 항목에 반영했다. 아래 두 가지는 **1차 플랜에 없던
새 판단**이라 승인을 받고 착수한다.

| # | 내용 | 근거 |
|---|---|---|
| **F** | P 없는 `M99`(본 프로그램)를 **종료 → 무시**로 바꿈 | 확정 D가 "M99 = N번호를 찾는 호출"이므로 찾을 N이 없으면 지시가 없는 것. 종료는 남은 본 프로그램을 조용히 지워 **그림이 사라지는 쪽으로 틀린다** (항목 4) |
| **G** | 항목 3(seq 트리밍)을 **필수 작업으로 승격** | `1111.nc` 실측 — 커서·재생 트레이스가 **첫 M98에서 죽어 점 8개만** 그린다(전체 38,926). 확정 C 범위 안이지만, 선택이 아니라 이 기능이 화면에 보이기 위한 전제다 (부록 A) |

---

## 부록 A. 실제 예제 검증 — `test files/1111.nc` (v1.7.3 현재 동작 실측)

사용자 제공 예제. **선반(턴밀) 프로그램**이다 — `M35` 구동공구, `G28U0.V0.`, `T0404`,
`G18/G19`, `M89/M90` C축 클램프. 구조:

```
1      %
2      O1111 ( 2 )
16     N1                      ← 공정 1 (T0404)
31     M98P0001 (SUB-CALL)     ┐ O0001을 2회 호출
34     M98P0001 (SUB-CALL)     ┘
42     N2                      ← 공정 2 (T0707)
55,65  M98P0002 (SUB-CALL)     ← O0002를 2회 호출
60,70  M98P0003 (SUB-CALL)     ← O0003을 2회 호출
79     M30
81     O0001(2, SUB-PGM)  …  9811 M99   (본문 9,730줄)
9813   O0002(2, SUB-PGM)  …  9840 M99   (본문   27줄)
9842   O0003(2, SUB-PGM)  …  9869 M99   (본문   27줄)
9870   %
```

요구 1의 "O0001 → O0002 → O0003 배치"와 요구 2의 "다시 만났을 때 그대로 적용"
(같은 M98이 각각 2번씩)이 이 파일 하나에 전부 들어 있다.

### 실측 결과 (v1.7.3 그대로, 선반 모드)

| 측정 | 값 |
|---|---|
| 원본 줄 수 | 9,870 |
| 확장 후 시퀀스 | **19,653** (≒ 2배) |
| 확장 시퀀스의 `src_line` 단조 증가 | **False** — 첫 M98은 `seq_pos=30, src_line=30`인데 바로 다음이 `81, 82, 83` |
| 공정 인식 | 정상 — `P001_T04`, `P002_T07` 2개 |
| 경로 노드 수 | 19,464 |
| 정적 전체 경로 점 수 | 38,926 |
| **커서 트리밍 `line_limit=30`** | **8점** |
| **커서 트리밍 `line_limit=40`** | **8점** |
| **커서 트리밍 `line_limit=78`** | **8점** |

### 확인된 것

1. **확장·공정 인식은 이미 맞게 동작한다.** 선반에서 M98 P0001/P0002/P0003이 번호로
   정확히 분기되고, 2회 호출도 2번 펼쳐진다. `M98P0001`처럼 **공백 없이 붙은 형태**도
   현행 정규식(`M98(?!\d)` + `P0*(\d+)`)이 그대로 잡는다.
2. **커서·재생 트레이스는 사실상 죽어 있다.** `line_limit`을 30이든 78이든 뭘 주든
   **8점**만 나온다 — `_render_segments()`가 첫 M98 자리(`src_line`이 30 → 81로 튀는 지점)에서
   `break`하기 때문이다. 항목 3이 없으면 M98이 있는 모든 프로그램에서 재생 기능이
   무의미하다. → **승인 항목 G**
3. **정적 전체 경로(`plot_items`)는 멀쩡하다.** `_build_path_items()`는 `line_limit` 없이
   부르므로(`:3626`) 38,926점이 다 그려진다. 그래서 "그림은 나오는데 재생만 안 되는"
   증상으로 보인다.
4. **M99를 본문 끝으로 바꾸는 변경은 이 파일에서 무해하다.** 세 서브프로그램 모두
   `M99` 다음이 공백 한 줄(9812번은 `" "`)이고 바로 다음 O헤더라, 실행 결과가 같다.
5. 서브프로그램 본문에 **N 라인이 하나도 없다**(0개). 따라서 선반 N블록 공정 판정
   (`_lathe_n_blocks`)이 서브프로그램 반복 때문에 공정을 더 만들 걱정은 이 파일엔 없다.
   다만 일반적으로는 가능한 형태이므로 회귀 테스트에 넣어 둔다(테스트 9).

### 추가 회귀 테스트 (예제 기반)

9. `test_subprogram_body_with_n_lines_does_not_split_extra_lathe_processes`
   — 서브프로그램 본문에 `N` 라인이 있고 2회 호출될 때 공정이 몇 개로 잡히는지 고정한다.
10. `test_sample_1111_nc_expands_to_expected_sequence_and_processes`
   — `test files/1111.nc`가 있을 때만 도는 스모크 테스트(`skipUnless`).
   확장 19,653줄 / 공정 2개 / `M98P0001` 공백 없는 형태 인식 / 커서 트리밍이
   **8점이 아니라 전체에 가깝게** 나오는지(항목 3 수정 확인).
   ※ 예제 파일은 리포지터리에 커밋되어 있지 않으므로 **파일이 없으면 skip** 한다.

---

## 부록 B. 착수 전 베이스라인 (2026-09-07, 손대지 않은 v1.7.3 워크트리)

`python -m pytest tests/test_nc_tool_list.py -q` → **191 passed, 12 failed (160s)**.
아래 12건은 **이번 작업 전부터 실패하던 것**이며(오프스크린 Qt 렌더링 / 폰트 메트릭 /
단일 인스턴스 소켓 등 환경 의존), v1.7.4 회귀 판정 기준에서 제외한다.

```
test_app_process_filter_click_moves_program_cursor_to_process_start
test_left_click_only_activates_line_when_magnifier_active
test_pg_match_mode_traces_only_cursor_process
test_pick_source_line_finds_nearest_segment_and_respects_radius
test_pick_source_line_pg_match_mode_scoped_to_progressed_current_tool
test_playback_prev_next_tool_jump_between_process_start_lines
test_playback_rewind_returns_to_current_process_start_line
test_projection_buttons_recenter_and_zoom_to_fit_all_views
test_scene_radius_set_from_loaded_path
test_tool_list_table_cells_and_font_scaled_1_6x_then_shrunk_15pct
test_tool_table_autofits_without_horizontal_scrollbar
test_handoff_returns_false_when_no_instance_is_running
```

`test_lathe_m98_expands_subprogram_after_m30_and_ignores_uncalled_ones`(v1.6.6 서브프로그램
회귀 테스트)는 **통과** 상태이므로, 항목 1 교체 후에도 반드시 통과해야 한다.

> 참고: 위 실패 중 `test_playback_*` / `test_pick_source_line_*` / `test_pg_match_mode_*` 는
> 항목 3(seq 기반 트리밍)이 건드리는 코드와 같은 영역이다. 구현 시 **이 실패들이
> 늘어나지 않는지**(개수·이름 동일) 확인한다.
