# v1.8.1 플랜 — 30일 체험판 + 발급 기간(1년 / 3년 / 영구)

작성: 2026-09-12 / 상태: **승인 완료**(§4 결정 A~F 전부 권장안대로 확정, 2026-09-12).
구현·테스트 완료, 빌드는 사용자 지시 대기.
기반: v1.8.0 (메인 `agent/drag-drop-installer` `a7aafc2`, PR #20 병합 완료).
대상: `sumpath_license.py`(공용 모듈), `NC_Tool_List.py`의 라이선스 게이트·About·제목 표시줄,
`SumPath_License_Maker.py`(발급 프로그램). 파서·툴패스·3D 뷰어·PDF·공구리스트 산출은 손대지 않는다.

---

## 1. 요구사항 (사용자 지시, 2026-09-12)

> "v1.8.1 기본 설치 시 30일 체험판으로 작동 후 라이센스 발급 프로그램에서는 1년 3년 영구
> 라이센스로 설정 바꿔줘 플랜 구성 후 승인대기"

정리:

1. 라이선스가 없어도 설치 후 **30일 동안은 체험판으로 그대로 사용**할 수 있다. 30일이 지나면
   v1.8.0과 같은 라이선스 등록 창이 뜬다.
2. 발급 프로그램에서 고를 수 있는 기간을 **1년 / 3년 / 영구**로 바꾼다(7일·30일 제거, 영구 신설).

v1.8.0 결정 B("유예 없음 — 첫 실행부터 라이선스 필요")를 **30일 체험**으로 뒤집는 변경이다.

---

## 2. 현재 구조 (v1.8.0 기준 줄 번호)

| 항목 | 위치 | 현재 동작 |
|---|---|---|
| 기간 목록 | `sumpath_license.py:29 LICENSE_PLANS` | 7일/30일/1년/3년 — 발급 프로그램 라디오 버튼과 `plan_label()`이 같이 씀 |
| 상태 값 | `:60 LicenseStatus` | `ok reason message license days_left` (필드 5개) |
| 내용 검증 | `:243 verify_license()` | `valid_until`이 반드시 날짜, `plan_days`와 어긋나면 형식 오류 |
| 시작 검사 | `:401 ensure_license()` | 파일 없으면 `missing` → 등록 창 |
| 만료 임박 | `:305 is_expiry_notice_due()` | 남은 7일 이하(7일짜리는 2일 이하) |
| 상태 파일 | `:372 load_license_state()` | `C:\ProgramData\NC Tool List\license_state.json` (`last_seen`, `last_notice_date`) |
| 앱 게이트 | `NC_Tool_List.py:4110 run_license_gate()`, `:4123 main()` | 핸드오프 뒤·`App()` 앞 |
| 등록 창 | `:4057 show_license_registration_dialog()` | 상태 문구 + PC 코드 + 등록/종료 |
| 실행 중 재확인 | `App._recheck_license()` `:1987`, 만료 안내 `:1995` | 1시간 주기 |
| About | `App.show_about()` `:2572~2617` | 사용자/기간/사용 기한/남은 일수/PC 코드/`[라이선스 파일 교체...]` |
| 창 제목 | `App.__init__` `:1958` | `Sum Path v1.8.0` |
| 발급 기간 선택 | `SumPath_License_Maker.py:159~237` | 라디오 4개, 기본 30일(`:193`) |
| 발급 데이터 | `:97 build_license()`, `:121 default_license_filename()` | `valid_until`은 항상 날짜 |

---

## 3. 설계

### 3.1 30일 체험판

**흐름** — `ensure_license()`를 다음 순서로 바꾼다:

```
1. 라이선스 파일이 있고 유효하면           → 정식 라이선스로 통과 (지금과 같음)
2. 아니면 체험 기록을 본다
   - 기록이 없으면 지금을 체험 시작으로 기록 → 체험판 통과 (첫 실행)
   - 기록이 있고 30일 안이면                → 체험판 통과
3. 둘 다 아니면 실패 → 등록 창
   - 라이선스 파일이 있었으면 그 파일의 실패 사유(만료/다른 PC/변조)를,
   - 없었으면 "30일 체험 기간이 끝났습니다. (체험 종료일: 2026-10-11)"를 보여 준다
```

- 기간 규칙은 라이선스와 같다: 첫 실행일 = 1일째, **꽉 채워 30일**(예: 2026-09-12 시작 →
  2026-10-11까지 사용 가능, 10-12부터 등록 필요).
- 체험 중에 정식 라이선스를 등록하면 그 즉시 정식으로 바뀐다(About의 등록 버튼, §3.4).
- 설치본·포터블 모두 같은 규칙(체험 시작은 앱 첫 실행 기준 — 결정 A).

**체험 기록 저장**(결정 B) — 두 곳에 같은 값을 적고, 둘 중 **더 이른 날짜**를 쓴다:

1. `C:\ProgramData\NC Tool List\license_state.json`의 `trial_started`
2. 레지스트리 `HKCU\Software\NC Tool List\License` 값 `TrialStarted`

- 한쪽이 지워져 있으면 남은 쪽 값으로 다시 채운다. 재설치·업데이트·제거로는 둘 다 지워지지 않는다
  (공용 폴더는 제거 시 남기도록 v1.8.0에서 이미 설정).
- PC 시계 되돌림 방지도 체험에 똑같이 적용: 현재 시각이 `trial_started` 또는 `last_seen`보다
  24시간 넘게 과거면 "PC 날짜가 올바르지 않습니다"로 거부.

**상태 값** — `LicenseStatus`에 `trial` 필드를 **기본값 False로** 추가한다
(`namedtuple(..., defaults=(False,))`). 지금처럼 인자 5개로 만드는 코드와 테스트는 그대로 동작한다.

| reason | ok | 뜻 |
|---|---|---|
| `ok` | True | 정식 라이선스 |
| `trial_started` | True | 이번 실행에서 체험 시작(첫 실행) |
| `trial` | True | 체험 중 |
| `trial_expired` | False | 체험 끝, 라이선스 없음 |
| (기존 `expired`/`wrong_machine`/`tampered`/`clock_rollback` 등) | False | v1.8.0과 같음 |

### 3.2 영구 라이선스 (결정 C)

- 형식 번호(`format: 1`)는 그대로 두고, **`plan_days: 0` + `valid_until: null`** 을 영구로 약속한다.
- `verify_license()`: `plan_days == 0`이면 `valid_until`이 `null`이어야 하고(아니면 형식 오류),
  만료 검사를 건너뛴다. `days_left`는 `None`. 서명·PC 고정·시작일 검사는 기간제와 똑같이 한다.
- 만료 임박 안내 없음, About에는 "사용 기한: 영구".
- `plan_label(0)` = `'영구'`.

### 3.3 발급 프로그램 기간 변경

- 기간 목록을 둘로 나눈다:
  - `ISSUABLE_PLANS = ((365, '1년 (365일)'), (1095, '3년'), (0, '영구'))` — 발급 프로그램 라디오 버튼용
  - `plan_label()`은 **7일·30일도 계속 인식**(v1.8.0으로 이미 발급된 파일 표시용, 결정 F)
- 라디오 버튼 3개, 기본 선택 **1년**.
- 영구 선택 시 사용 기한 미리보기는 "사용 기한: 영구", 시작일 입력은 그대로(발급일 기록용).
- 기본 파일명: `SumPath_<사용자>_<PC코드 앞 4자리>_영구.lic`.
- 발급 이력 CSV의 `valid_until` 칸은 `영구`.
- [확인] 탭: 영구면 "남은 일수: 영구".
- 발급 프로그램 버전 `1.0.0` → `1.1.0`.

### 3.4 Sum Path 화면

- **첫 실행 안내**(결정 E) — 체험이 막 시작된 실행(`trial_started`)에서 한 번만:
  "30일 체험판으로 시작합니다. 체험 종료일: 2026-10-11. 계속 사용하려면 About에서 라이선스를
  등록하세요." (확인 버튼만, 차단 아님)
- **제목 표시줄**(결정 D) — 체험 중에는 `Sum Path v1.8.1 — 체험판 (남은 12일)`. 정식이면
  `Sum Path v1.8.1` 그대로. `main()`과 1시간 주기 재확인에서 갱신한다(`App.__init__`의 기본 제목은
  그대로 둬 기존 테스트 영향 없음).
- **만료 임박 안내** — 체험도 남은 7일 이하면 하루 한 번 "체험 기간이 N일 남았습니다" 안내.
- **About 라이선스 그룹**
  - 정식: 사용자 / 기간(1년·3년·영구) / 사용 기한(영구면 "영구") + 남은 일수
  - 체험: "체험판 — 남은 N일 (체험 종료일: …)"
  - 버튼 이름 `[라이선스 파일 교체...]` → `[라이선스 파일 등록/교체...]`(체험 중 등록에도 쓰므로)
- **등록 창** — 체험이 끝났을 때 위 §3.1의 문구로 뜬다. 나머지는 v1.8.0과 같다.

### 3.5 호환성

| 경우 | 결과 |
|---|---|
| v1.8.0 발급 7일/30일/1년/3년 파일 → v1.8.1 앱 | 그대로 인정(결정 F) |
| v1.8.1 발급 **영구** 파일 → v1.8.0 앱 | "형식 오류"로 거부(v1.8.0은 `valid_until: null`을 모름) — v1.8.0은 아직 배포 전이므로 **v1.8.0을 건너뛰고 v1.8.1로 배포**하면 문제없음 |
| 이미 v1.8.0을 깐 PC(라이선스 없음) | v1.8.1 첫 실행일부터 30일 체험 시작 |
| 이 개발 PC | 빌드 검증으로 v1.8.1을 한 번 띄우면 이 PC에서도 체험이 시작된다(공용 폴더·HKCU에 기록) — 발급 PC라 영구 라이선스를 스스로 발급해 두면 됨 |

### 3.6 보안 한계 (미리 알려 둠)

- 체험 기록은 서명할 수 없다(앱에는 개인키가 없음). **두 곳(공용 폴더 + 레지스트리)을 모두 지우면**
  체험이 다시 시작된다. 이 PC를 쓴 모든 Windows 계정의 레지스트리 값까지 지워야 하므로 일반 사용자가
  우연히 되는 일은 없지만, 작정하면 가능하다 — 목표는 v1.8.0과 같이 "일상적인 우회 차단"이다.
- 정식 라이선스(기간제·영구)의 위조 불가·PC 고정은 v1.8.0과 같다.

### 3.7 바꾸지 않는 것

- 서명 방식(Ed25519, 순수 파이썬 검증), PC 코드 규칙, 저장 위치, 1시간 재확인, 단일 실행 핸드오프.
- 파서 / 툴패스 / 3D 뷰어 / PDF / 공구리스트 산출 / 검색 / 파일 연결 / 업데이트 기능.

---

## 4. 결정 사항 (승인 시 확인 부탁)

| # | 결정 | 권장안 | 대안 |
|---|---|---|---|
| **A** | 체험 시작 시점 | **앱 첫 실행** — 설치본·포터블 공통, 설치만 하고 안 쓴 기간은 차감 안 됨 | 설치 시점(설치 스크립트가 기록 — 포터블은 적용 불가) |
| **B** | 체험 기록 저장 | **공용 폴더 + 레지스트리(HKCU) 이중**, 더 이른 날짜 우선·지워진 쪽 복구 | 공용 폴더만(파일 하나 지우면 체험 재시작) |
| **C** | 영구 라이선스 형식 | **`plan_days: 0` + `valid_until: null`, 형식 번호 1 유지** | 형식 번호 2로 올림(구분은 명확하나 검증 분기가 늘어남) |
| **D** | 체험 중 제목 표시줄 | **"— 체험판 (남은 N일)" 표시** | 제목은 그대로, About에서만 확인 |
| **E** | 첫 실행 체험 안내 | **체험 시작 시 한 번 안내 창** | 안내 없음(제목 표시줄로만) |
| **F** | 이미 발급된 7일/30일 파일 | **앱은 계속 인정**(발급만 불가) | 거부(7일/30일 폐지) |

---

## 5. 변경 파일 / 작업 목록

1. `sumpath_license.py`
   - `LicenseStatus`에 `trial` 필드(기본 False), `ISSUABLE_PLANS`, `PERPETUAL_PLAN_DAYS = 0`, `TRIAL_DAYS = 30`
   - `verify_license()` 영구 분기, `plan_label(0)`
   - 체험 기록 읽기/쓰기(공용 폴더 + HKCU, 테스트에서 바꿔 끼울 수 있게 함수로 분리),
     `evaluate_trial()`, `ensure_license()` 순서 변경(§3.1)
2. `NC_Tool_List.py`
   - `main()`: 체험 시작 안내(결정 E), 제목 표시줄 갱신(결정 D)
   - `App._recheck_license()` / `_maybe_show_license_expiry_notice()`: 체험 문구, 제목 갱신
   - `show_about()`: 체험/영구 표시, 버튼 이름
   - `APP_VERSION = '1.8.1'`, `APP_BUILD_DATE`
3. `SumPath_License_Maker.py` — `ISSUABLE_PLANS` 라디오(기본 1년), 영구 발급/미리보기/파일명/CSV/확인 탭,
   `MAKER_VERSION = '1.1.0'`
4. `NC_Tool_List.iss` `MyAppVersion`, `version_info.txt` — 1.8.1
5. `tests/test_license.py` — §6 신규 테스트, 기존 `missing` 기대 테스트를 체험 흐름에 맞게 수정
6. `ABOUT_RELEASE_INSTRUCTIONS.md` — v1.8.1 이력(v1.8.0 결정 B 변경 명시)
7. 회귀 확인: 전체 스위트를 v1.8.0과 같은 범위로 돌려 실패 집합 비교
   (기존 `ToolListModeComboTests` 1건은 이 PC 레지스트리 상태 문제로 v1.7.9부터 동일 — [[project_tests_share_real_qsettings]])

---

## 6. 테스트 (신규/수정)

모든 테스트는 공용 폴더·레지스트리 읽기/쓰기 함수를 임시 저장소로 바꿔 끼워, **실제 ProgramData와
HKCU를 절대 건드리지 않는다.**

1. **체험 시작** — 라이선스·기록 없음 → `trial_started`, 남은 29일(시작일 포함 30일), 두 저장소 모두에 기록.
2. **체험 진행/종료** — 30일째(마지막 날) 통과, 31일째 `trial_expired`(ok=False, 종료일 문구).
3. **이중 저장** — 공용 폴더만 지워도/레지스트리만 지워도 체험 이어짐(남은 쪽으로 복구),
   두 값이 다르면 더 이른 날짜 사용.
4. **시계 되돌림** — `trial_started`·`last_seen`보다 25시간 과거 → `clock_rollback`, 23시간은 통과.
5. **우선순위** — 체험 중이라도 유효한 라이선스가 있으면 `ok`(trial=False). 라이선스 파일이 무효(만료/다른 PC)여도
   체험 기간 안이면 체험판 통과, 체험도 끝났으면 **라이선스 쪽 실패 사유**를 보여 줌.
6. **영구 라이선스** — 서명 통과·`days_left None`·먼 미래 날짜(2099)에도 통과, `valid_until`에 날짜를 넣은
   `plan_days 0`은 형식 오류, 영구 파일도 PC·서명 변조 거부.
7. **발급 프로그램** — 라디오가 1년/3년/영구 3개·기본 1년, 영구 발급 → 앱 검증기 통과, 파일명 `_영구.lic`,
   CSV `영구`.
8. **하위 호환** — 7일/30일 픽스처가 계속 통과, `plan_label(7)`/`(30)`/`(0)`.
9. **`LicenseStatus` 호환** — 인자 5개로 만들면 `trial=False`.
10. **UI** — 체험 종료 상태의 등록 창 문구, About 소스에 버튼 이름 `라이선스 파일 등록/교체`,
    `main()` 소스에서 체험 안내·제목 갱신이 `window.show()` 뒤에 있음.
11. 기존 v1.8.0 테스트 51개는 `missing` 기대 1건만 체험 흐름에 맞게 고치고 나머지는 그대로 통과.

---

## 7. 회귀 위험과 대응

| 위험 | 대응 |
|---|---|
| 테스트가 실제 레지스트리/ProgramData에 체험 기록을 남김 | 저장 함수 바꿔치기, 봉인 테스트로 확인 |
| 체험이 끝났는데 정식 라이선스 실패 사유가 가려짐 | §3.1 3번 규칙, 테스트 5 |
| 영구 라이선스가 1시간 재확인에서 잘못 만료 처리 | `days_left None` 경로 테스트 6 |
| 제목 표시줄 변경이 기존 테스트에 영향 | `App.__init__` 제목은 그대로, `main()`/재확인에서만 변경 |
| v1.8.0 앱에 영구 파일을 줌 | v1.8.0은 배포하지 않고 v1.8.1로 바로 배포(§3.5) |

---

## 8. 배포 순서 (v1.8.0 §8 대체)

체험판 덕분에 **배포 순서 제약이 풀린다**: v1.8.1을 업데이트 공유 폴더에 올려도 각 PC는 첫 실행부터
30일 동안 그대로 쓸 수 있다. 그 30일 안에:

1. 각 PC의 About → 라이선스 그룹에서 **PC 코드**를 받아
2. 발급 프로그램(v1.1.0)으로 1년/3년/영구 라이선스 발급 → 전달
3. 각 PC에서 About → `[라이선스 파일 등록/교체...]`

v1.8.0 설치본/포터블은 배포하지 않는다(체험판이 없어 즉시 등록을 요구함).

---

## 9. 준비 상태 (2026-09-12)

- 작업 브랜치: `worktree-v181-trial` (메인 `a7aafc2` 기반, worktree `.claude/worktrees/v181-trial`)
- **구현 완료** — §5 전 항목 반영. `APP_VERSION`/`MAKER_VERSION`/`NC_Tool_List.iss`/
  `version_info.txt` 모두 1.8.1 계열로 갱신.
- **테스트 완료** — 신규 `tests/test_license.py` 75개(v1.8.0의 51개 + 신규 24개) 전부
  통과(27.55초). 기존 `tests/test_nc_tool_list.py` 전체 재실행 결과 328 passed, 1 skipped,
  1 failed — 그 1건(`ToolListModeComboTests`)은 이 개발 PC의 실제 레지스트리 상태
  때문에 v1.7.9부터 동일하게 재현되는 기존 문제로, v1.8.0 릴리스 때 이미
  `git archive`로 확인했다(회귀 아님, [[project_tests_share_real_qsettings]]).
- **빌드 완료**(사용자 지시) — PyInstaller onedir + Inno Setup 6 설치본 +
  포터블 zip(본 앱, 1.8.1), 그리고 별도 배포물인 발급 프로그램(SumPath
  License Maker, 1.1.0) zip까지 빌드하고 각각 짧게 실행해 정상 기동을
  확인했다. SHA-256 등 상세는 `ABOUT_RELEASE_INSTRUCTIONS.md` v1.8.1
  항목 참고. §8 배포 순서대로 진행하면 된다.
