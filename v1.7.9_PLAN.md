# v1.7.9 플랜 — 이전공구검색 버튼 + 파일을 넘겨받아도 최대화 유지

작성: 2026-09-11 / 상태: **승인 완료** (결정 A·B 전부 권장안대로 확정, 2026-09-11.
구현·테스트·설치본/포터블 빌드 완료.)
기반: v1.7.8 (메인 `agent/drag-drop-installer` `f8c6f67`, PR #18 병합 완료).
대상: 프로그램 입력 패널의 검색줄, 단일 실행(v1.6.7) 핸들러. 파서·툴패스·3D 뷰어·
PDF는 손대지 않는다.

---

## 1. 요구사항 (사용자 지시, 2026-09-11)

> "툴리스트 모드에서 다음공구검색 앞에 이전공구 검색 추가 열린 상태에서 다른 파일
> 기본프로그램으로 열 떄 최대화 풀림 최대화 유지 되게 설정"

정리:

1. 검색줄의 `다음공구검색` 버튼 **바로 앞(왼쪽)**에 `이전공구검색` 버튼을 추가한다.
   커서 위치에서 **위쪽으로** 가장 가까운 공구 교체 지점(밀링 `M6T`, 선반 `Tnn00`)을
   선택한다.
2. 프로그램이 이미 떠 있는 상태에서 탐색기 더블클릭(연결 프로그램)으로 다른 NC 파일을
   열면 **창 최대화가 풀린다** → 최대화를 유지한다.

---

## 2. 현재 구조와 원인 (조사 결과, v1.7.8 기준 줄 번호)

### 2.1 다음공구검색

| 항목 | 위치 | 비고 |
|---|---|---|
| 앞으로 찾기 공용 함수 | `NC_Tool_List.py:978 find_next_regex_span()` | 커서 뒤에서 찾고, 없으면 처음부터 다시(`wrapped=True`) |
| 공구 교체 검색 | `:996 find_next_tool_change_span(text, start, lathe)` | 밀링 `M6_SEARCH_RE`(`:121`), 선반 `LATHE_T_SEARCH_RE`(`:131`) |
| 버튼 동작 | `App.find_next_tool_change()` (`:3427`) | 기준 = `textCursor().position()`, 찾으면 매치를 선택(커서 = 매치 **끝**) |
| 검색줄 UI | `App._build_ui()` (`:2206~2216`) | `[다음공구검색] 문자 검색 [입력칸(stretch)] [검색] 상태` |
| 도움말 | `HELP_TEXT` (`:2396`) | "다음공구검색: 공구 교체 지점을 순서대로 찾습니다…" |

- 검색줄은 왼쪽 프로그램 패널에 있어서 **"툴리스트 산출 모드"와 "Viewer 모드"에 공통**으로
  보인다(`set_mode()` `:2950`은 필터/장비설정 패널만 숨긴다). 따라서 새 버튼도 두 모드에
  같이 보인다.
- 이름이 비슷한 **F6/F8(재생 중 이전/다음 공구)**은 선택 공정 기준으로 재생 커서를 옮기는
  별개 기능이다. 이번 변경과 무관하며 그대로 둔다.

### 2.2 최대화가 풀리는 원인

두 번째 파일을 열면 새로 뜬 프로세스가 `send_to_running_instance()`로 경로만 넘기고 종료하고,
떠 있던 창의 `start_single_instance_server()` → `read_path()` (`:1518~1535`)가 받는다:

```python
window.setWindowState(
    (window.windowState() & ~Qt.WindowMinimized) | Qt.WindowActive
)
window.showNormal()      # ← 여기서 최대화가 풀린다
window.raise_()
window.activateWindow()
```

- `showNormal()`은 v1.6.7에서 "최소화된 창을 앞으로 끌어내기" 용도로 넣은 것인데, v1.7.3
  "항상 최대화" 이후에는 **최대화 상태의 창까지 일반 크기로 되돌린다.**
- 최소화 해제는 첫 줄의 상태 마스크(`& ~Qt.WindowMinimized`)만으로 이미 된다. Qt는 최대화
  창을 최소화하면 `WindowMaximized` 비트를 남겨 두므로, 이 마스크만 쓰면 최대화로 복원된다
  — 뒤따르는 `showNormal()`이 그 비트를 지우는 게 문제다.
- 첫 실행(창이 없을 때)은 `App.__init__`의 `QTimer.singleShot(0, self.showMaximized)`
  (`:1920`)로 정상 최대화되며 이번 문제와 무관하다.
- `load_file()`(`:3648`) / `run()` / 뷰어 모듈(`nc_viewer_widget.py`)에는 창 상태를 바꾸는
  코드가 없다(확인 완료). 원인은 위 `showNormal()` 한 곳뿐이다.

---

## 3. 설계

### 3.1 이전공구검색

**모듈 함수 2개 신설** (`find_next_*` 바로 옆, 기존 함수는 고치지 않음):

```python
def find_prev_regex_span(text, pattern, start=0):
    """start보다 앞에서 시작하는 마지막 매치. 없으면 끝에서부터 다시(wrapped=True)."""
    text = text or ''
    if not text:
        return None
    try:
        start = int(start)
    except (TypeError, ValueError):
        start = 0
    start = max(0, min(start, len(text)))
    before = last = None
    for match in pattern.finditer(text):
        if match.start() < start:
            before = match
        last = match
    if before is not None:
        return before.start(), before.end(), False
    if last is not None:
        return last.start(), last.end(), True
    return None


def find_prev_tool_change_span(text, start=0, lathe=False):
    """'이전공구검색' — 다음공구검색과 같은 기준(밀링 M6T, 선반 Tnn00)으로 위쪽을 찾는다."""
    return find_prev_regex_span(
        text, LATHE_T_SEARCH_RE if lathe else M6_SEARCH_RE, start
    )
```

- 정규식은 `find_next_tool_change_span`과 **같은 것을 그대로** 쓴다. 선반 판별도 기존
  `lathe` 플래그(`is_lathe_program()`)를 재사용 — 선반/밀링 분기를 새로 만들지 않는다
  (`LATHE_MODE_GUIDELINES.md` 분리 원칙 유지).
- `M6_SEARCH_RE`는 `re.M`의 `^` 앵커를 쓰므로 잘라낸 부분 문자열이 아니라 **전체 텍스트에
  `finditer`**를 돌려 줄 머리 판정이 바뀌지 않게 한다. 실측 최대 파일(확장 약 2만 줄)에서도
  한 번 훑는 비용은 무시할 수준.

**`App.find_prev_tool_change()` 신설** (`find_next_tool_change` 옆):

- 기준 위치는 `textCursor().selectionStart()`. 다음공구검색이 매치를 선택해 둔 상태(커서 =
  매치 끝)에서 누르면, 방금 선택한 **그 매치를 다시 잡지 않고 바로 앞 공구로** 간다.
- 이전 ↔ 다음을 번갈아 누르면 두 지점 사이를 정확히 왕복한다(다음 = 선택 끝 기준,
  이전 = 선택 시작 기준).
- 상태 문구: 정상 `공구 위치 선택`, 맨 위를 넘어가면 `끝에서부터 검색`, 없으면 다음공구검색과
  같은 `M6T 항목 없음` / `Tnn00 항목 없음`.
- 커서를 `M6 T3` 줄 **중간**에 두고 누르면 그 줄의 매치(= 커서 위쪽에서 가장 가까운 공구
  교체 시작점)를 잡는다. 다음공구검색이 커서 뒤부터 찾는 것과 대칭이다.

**UI** — 검색줄에 버튼 한 개:

```python
search_bar = QHBoxLayout()
self._add_button(search_bar, '이전공구검색', self.find_prev_tool_change, kfont)
self._add_button(search_bar, '다음공구검색', self.find_next_tool_change, kfont)
search_bar.addWidget(QLabel('문자 검색'))
...
```

- 문자 검색 입력칸이 `stretch=1`이라 버튼이 늘어난 만큼 입력칸만 좁아진다(다른 행 영향 없음).
- `HELP_TEXT` 검색 항목:
  `이전공구검색 / 다음공구검색: 공구 교체 지점을 위/아래 방향으로 찾습니다(밀링 M6T, 선반
  Tnn00). 끝에 닿으면 반대쪽 끝에서 이어서 찾습니다.`

### 3.2 파일을 넘겨받을 때 최대화 유지

**모듈 함수 `bring_window_to_front(window)` 신설**, `read_path()`의 창 조작 블록을 이
호출 하나로 바꾼다:

```python
def bring_window_to_front(window):
    """넘겨받은 파일을 열기 전에 창을 앞으로 끌어낸다(v1.7.9).
    최소화돼 있을 때만 되살리고, 최대화 상태는 절대 풀지 않는다."""
    state = window.windowState()
    if state & Qt.WindowMinimized:
        maximized = bool(state & Qt.WindowMaximized) or getattr(
            window, '_restore_maximized', False
        )
        window.setWindowState((state & ~Qt.WindowMinimized) | Qt.WindowActive)
        if maximized:
            window.showMaximized()
        else:
            window.showNormal()
    window.raise_()
    window.activateWindow()
```

- 최소화가 아니면(최대화 상태로 떠 있거나 다른 창에 가려 있을 때) **창 상태를 건드리지 않고**
  앞으로만 끌어낸다 → 최대화 그대로.
- 최소화돼 있었으면 최대화 이력대로 복원한다.
- **보강** — `App.changeEvent()`를 신설해 `QEvent.WindowStateChange`마다, 최소화가 아닌
  상태일 때 `self._restore_maximized = self.isMaximized()`를 기록한다. Qt5는 최소화 시
  `WindowMaximized` 비트를 남기지만, 작업표시줄/Win+D 등 경로에 따라 비트가 사라지는 경우를
  대비한 이중 안전장치다. 기존 `resizeEvent` 등과 충돌 없음(`super().changeEvent()` 호출 유지).
- 순서는 지금과 같다: 창 복원 → `load_file(payload)`.

### 3.3 바꾸지 않는 것

- 파서 / 툴패스 / 3D 뷰어 / PDF / 문자 검색(`find_next_text`).
- 첫 실행 최대화 경로(`App.__init__`), 드래그&드롭·`파일 열기` 버튼 경로(창 상태 무관).
- F5~F8 단축키(재생 전용). 이전/다음공구검색에는 단축키를 두지 않는다(현행 다음공구검색과 동일).

---

## 4. 결정 사항 (승인 시 확인 부탁)

| # | 결정 | 권장안 | 대안 |
|---|---|---|---|
| **A** | 사용자가 최대화를 **직접 풀어 둔**(일반 크기) 창에 파일을 넘겨받을 때 | **그 상태 유지** — 최대화였으면 최대화, 일반 크기였으면 일반 크기(사용자 조작 존중) | 파일을 넘겨받을 때마다 **항상 최대화**로 되돌림(v1.7.3 "항상 최대화" 정책을 여기까지 확장) |
| **B** | 문자 검색에도 '이전' 기능 | **이번엔 안 함**(요청 범위 = 공구 검색) | 문자 검색 `[검색]` 앞에 `[이전]` 버튼 + 입력칸 `Shift+Enter` = 이전 검색 |

---

## 5. 변경 파일 / 작업 목록

1. `NC_Tool_List.py`
   - `find_prev_regex_span()` / `find_prev_tool_change_span()` 신설 (§3.1)
   - `bring_window_to_front()` 신설, `start_single_instance_server()`의 `read_path()`에서
     사용 — `showNormal()` 직접 호출 제거 (§3.2)
   - `App.changeEvent()` 신설 — `_restore_maximized` 기록
   - `App.find_prev_tool_change()` 신설, 검색줄에 `이전공구검색` 버튼, `HELP_TEXT` 갱신
   - `APP_VERSION = '1.7.9'`, `APP_BUILD_DATE`
2. `NC_Tool_List.iss` `MyAppVersion`, `version_info.txt` — 1.7.9
3. `tests/test_nc_tool_list.py` — 신규 테스트(§6)
4. `LATHE_MODE_GUIDELINES.md` (`:250` 등) — "다음공구검색" → "이전/다음공구검색" (기준 동일)
5. `ABOUT_RELEASE_INSTRUCTIONS.md` — v1.7.9 이력 추가
6. 회귀 확인: `pytest tests/test_nc_tool_list.py` 전체, 미수정 v1.7.8과 실패 집합 비교
   (공유 QSettings 때문에 생기는 환경 의존 실패는 기존과 동일해야 함)

---

## 6. 테스트 (신규)

1. `test_prev_tool_change_search_supports_spacing_and_wraps` — 밀링
   `'M6 T2\nG0 X0\nM06T01'`: 끝에서 → 두 번째 매치, 두 번째 매치 시작에서 → 첫 매치,
   0에서 → 마지막 매치(`wrapped=True`), 매치 없음 → `None`.
   (기존 `test_next_tool_change_search_supports_spacing_and_wraps`의 거울상)
2. 선반: `lathe=True`에서 `Tnn00`만 짚고 `T0101`류 옵셋 호출·`T0000`은 건너뜀,
   밀링 원문은 선반 기준으로 `None` (기존 `:3147` 선반 테스트의 거울상).
3. 창 하니스: 다음공구검색 2회 → 이전공구검색 1회 → 선택된 텍스트가 **첫 번째** 공구 교체
   지점 (selectionStart 기준 왕복 확인). 없을 때 상태 라벨 문구 확인.
4. UI: 검색줄에서 `이전공구검색` 버튼이 `다음공구검색` 바로 앞에 있음.
5. `bring_window_to_front()` — 호출을 기록하는 가짜 창 객체로:
   - 최대화(최소화 아님) → `showNormal`/`showMaximized` 호출 없음, `raise_`/`activateWindow`만
   - 최소화+최대화 비트 → `showMaximized`, `showNormal` 없음
   - 최소화만 + `_restore_maximized=True` → `showMaximized`
   - 최소화만 + 이력 없음 → `showNormal`
   (offscreen QPA에서는 실제 최대화 여부를 신뢰할 수 없어 기존 v1.7.3 테스트처럼 실물 창
   대신 호출 기록으로 검증한다.)
6. 소스 봉인: `inspect.getsource(start_single_instance_server)`에 `showNormal`이 없고
   `bring_window_to_front`가 있음.
7. 기존 `SingleInstanceTests` 3종, v1.7.3 `test_window_is_always_maximized_even_with_saved_geometry`
   그대로 통과.

---

## 7. 회귀 위험과 대응

| 위험 | 대응 |
|---|---|
| 최소화된 창이 복원되지 않음 | 상태 마스크 + `showMaximized/showNormal` 유지, 테스트 5 |
| 이전/다음 왕복 시 같은 지점을 두 번 잡음 | `selectionStart` 기준, 테스트 3 |
| 좁은 화면에서 검색줄이 넘침 | 입력칸 stretch가 흡수. 1280px 폭에서 육안 확인 |
| 백그라운드 창을 앞으로 끌어낼 때 Windows 포그라운드 제한(작업표시줄 깜빡임만) | 기존 동작과 동일 — 이번 변경 범위 밖. 문제 되면 별도 과제 |

---

## 8. 준비 상태 (2026-09-11)

- 작업 브랜치: `worktree-v179-plan` (메인 `f8c6f67` 기반, worktree
  `.claude/worktrees/v179-plan`)
- **구현 완료** — §5 전 항목 반영, `APP_VERSION`/`NC_Tool_List.iss`/`version_info.txt`
  1.7.9로 갱신.
- **테스트 완료** — §6 신규 테스트 전부 통과, 전체 스위트 254 passed / 1 skipped
  (회귀 없음). 자세한 내역은 `ABOUT_RELEASE_INSTRUCTIONS.md` v1.7.9 항목 참고.
- **빌드 완료**(사용자 지시) — PyInstaller onedir + Inno Setup 6 설치본 +
  포터블 zip. SHA-256 등 상세는 `ABOUT_RELEASE_INSTRUCTIONS.md` v1.7.9 항목 참고.
