# v1.7.5 플랜 — 선반 툴리스트, 신규 포스트 `(Tnnnn)` 주석 대응 (승인 대기)

작성: 2026-09-07 / 상태: **승인 대기**
대상: 선반 모드 전용 (`parse_lathe_program()`). 밀링 툴리스트는 손대지 않음
(LATHE_MODE_GUIDELINES §0 완전 분리 원칙).

---

## 1. 문제 — 예전 포스트 기준으로 굳어 있음

현재 `parse_lathe_program()`(v1.6.5)은 **예전 포스트** 출력(`O1699.nc`,
`O2222.nc`, `O4811.nc`)만 기준으로 만들어졌다.

```
N1
( T06 - SLEEVE )               <- 1번째 통짜 주석 = 홀더
( T06 - D50.0 X H103 T-DRILL ) <- 2번째 통짜 주석 = 인서트(공구)
G0X400.Z200.
```

그런데 **현재 포스트**는 N 시퀀스 바로 아래에 옵셋까지 붙은 공구번호
주석 `( Tnnnn )` 을 한 줄 더 찍는다 (`test files/1111.nc`, `O4006.nc` 실측):

```
N1
( T0404 )                                     <- 공구번호 주석 (신규)
( T04- BMT STRAIGHT )                         <- 홀더
( T04-10.0 R0.5 E/M / D-10. / R-0.5)          <- 인서트(공구)
( MAX : Z100. /  MIN : Z0. )                  <- 가공범위 주석
M35
```

파서가 앞 두 줄만 보므로 **한 칸씩 밀린다.** 실제 산출 결과(현재 코드로 실행):

| 파일 | NO | INSERT | HOLDER |
|---|---|---|---|
| 1111.nc N1 | T0404 | `BMT STRAIGHT` (실제로는 홀더) | `T0404` (주석 쓰레기) |
| 1111.nc N2 | T0707 | `BMT STRAIGHT` | `T0707` |
| O4006.nc N1 | T1010 | `BMT ANGLE` | `T1010` |

→ **홀더 칸에 `T0404`가 들어가고, 진짜 인서트 문구는 통째로 유실된다.**
(예전 포스트 `O1699.nc`는 지금도 정상 — 9행 전부 홀더/인서트가 맞게 나온다.)

---

## 2. 요구사항 (사용자 지시, 2026-09-07)

> "N 시퀀스 번호 아래 `(Tnnnn)` 다음 주석이 홀더, 그다음 주석이 공구임.
> 2가지 경우로 해줬으면 좋겠음. 시퀀스 다음 `(T0404)` 같은 주석이 나오면
> 이것은 무시되고 다음 2가지 주석으로 인서트와 홀더를 결정하게끔.
> 그래야 예전 프로그램과 신규 프로그램 같이 사용 가능함."

정리하면 **한 파서가 두 양식을 모두 받는다**:

- **경우 A (예전 포스트)** — N 아래 첫 통짜 주석 = 홀더, 둘째 = 인서트. **현행 유지.**
- **경우 B (신규 포스트)** — N 아래 첫 통짜 주석이 `(Tnnnn)` 꼴이면 **버리고**,
  그 다음 통짜 주석 = 홀더, 그 다음 = 인서트.

홀더가 먼저, 인서트가 나중 — 순서는 두 경우 모두 동일(기존 규약 유지).

---

## 3. 구현 계획

### 3-1. 새 상수 (`NC_Tool_List.py`, 선반 상수 블록 안)

```python
# v1.7.5: 신규 포스트는 N 시퀀스 바로 아래에 옵셋 포함 공구번호만 든
# 주석 한 줄("( T0404 )")을 먼저 찍는다. 이 줄은 홀더/인서트가 아니므로
# 건너뛴다 — 예전 포스트(O1699.nc)에는 이 줄이 없어 그대로 동작한다.
LATHE_TOOL_NO_COMMENT_RE = re.compile(r'^T\s*\d{2}(?:\d{2})?$', re.I)
```

- 판정은 **접두어 제거(`_strip_lathe_tool_prefix`) 이전의 원문**에 대해 한다.
- 매치 조건은 "괄호 안이 `T` + 숫자 2자리 또는 4자리, 그것뿐"이다.
  예전 포스트의 홀더 주석은 항상 `T06 - SLEEVE`처럼 `-` 뒤에 문구가 붙으므로
  **절대 매치되지 않는다** (회귀 없음).

### 3-2. `parse_lathe_program()` 주석 수집 루프 수정

현재:

```python
for line in block:
    if not line.strip():
        continue
    comment_match = LATHE_FULL_COMMENT_LINE_RE.match(line)
    if comment_match is None:
        break
    comments.append(_strip_lathe_tool_prefix(comment_match.group(1)))
    if len(comments) >= 2:
        break
```

변경 후:

```python
for line in block:
    if not line.strip():
        continue
    comment_match = LATHE_FULL_COMMENT_LINE_RE.match(line)
    if comment_match is None:
        break
    raw_comment = comment_match.group(1).strip()
    # v1.7.5: 홀더/인서트를 아직 하나도 못 모은 상태에서 나오는
    # "( T0404 )" 꼴 공구번호 주석은 신규 포스트의 머리줄이므로 버린다.
    if not comments and LATHE_TOOL_NO_COMMENT_RE.match(raw_comment):
        tool_no_from_comment = raw_comment          # 3-3에서 폴백으로 사용
        continue
    comments.append(_strip_lathe_tool_prefix(raw_comment))
    if len(comments) >= 2:
        break
```

- `not comments` 조건 덕에 **맨 앞에 나올 때만** 건너뛴다. 홀더/인서트를
  이미 읽은 뒤의 주석(`( MAX : Z100. / MIN : Z0. )`)은 지금처럼 2줄에서
  끊기므로 영향 없음.
- 두 줄을 다 못 모으면 지금과 같이 빈 문자열로 남는다.
- `[SO nn]` 추출(v1.7.2)은 수집이 끝난 뒤 `comments`(= 홀더/인서트)에만
  돌므로, 버려진 `(T0404)` 줄은 SO 스캔 대상이 아니다 — 자동으로 맞다.

### 3-3. TOOL NO 폴백 (신규 판단 — **결정 A**)

TOOL NO는 지금처럼 **블록 안 코드의 T워드**에서 뽑는 것을 1순위로 유지한다
(`code_without_comments`로 주석을 걷어내므로 `(T0404)`는 원래 안 잡힌다).
1111.nc / O4006.nc는 코드에 `T0404`, `T1010`이 실제로 있어 지금도 NO는 맞다.

다만 **블록 코드에 T워드가 하나도 없을 때에 한해** 3-2에서 챙겨 둔
`(Tnnnn)` 주석 값을 TOOL NO로 쓰는 폴백을 추가할지 결정이 필요하다.

- **제안: 추가한다.** 기존 경로(코드에 T워드가 있는 모든 프로그램)의
  결과는 한 글자도 바뀌지 않고, T워드가 없어 `__N<줄번호>__` 익명 키로
  빠지던 블록만 구제된다. 4자리(`T0404`)면 그대로 쓰고, 2자리(`T04`)는
  옵셋을 알 수 없으므로 폴백에서 제외한다.
- 원하지 않으시면 이 항목만 빼고 진행 가능(3-2만으로 요구는 충족됨).

### 3-4. 3D 뷰어 — 변경 없음 (확인 완료)

- `nc_viewer_widget._lathe_n_blocks()`는 `_code_without_comments()`로 주석을
  지운 뒤 T워드를 찾으므로 `(T0404)` 줄의 영향을 받지 않는다.
- `process_nc_lines()`의 `line_upper_with_comments` 기반 T 감지는 원래
  주석 속 T워드도 읽도록 만들어진 것이라 신규 포스트에서도 그대로 맞다.
- 필터 라벨(`lathe_tool_name_map_from_rows`)은 파서 결과를 그대로 쓰므로
  3-2 수정만으로 인서트 문구가 자동으로 바르게 들어간다.

→ **선반 전용 파서 한 곳만 고친다.** 밀링 파서(`parse_program`)·뷰어·PDF
레이아웃은 무수정.

---

## 4. 테스트 계획 (`tests/test_nc_tool_list.py`)

기존 선반 테스트(`LATHE_TOOLLIST_SOURCE` 기반 6종)는 **그대로 통과해야
한다** — 예전 포스트 회귀 방어선.

추가할 테스트:

1. `test_lathe_parse_program_skips_tool_no_comment_line_new_post`
   — 신규 포스트 양식 샘플(1111.nc N1 발췌)로
   `HOLDER == 'BMT STRAIGHT'`, `INSERT == '10.0 R0.5 E/M / D-10. / R-0.5'`,
   `NO == 'T0404'` 확인.
2. `test_lathe_parse_program_old_post_unaffected_by_new_rule`
   — 예전 양식에서 홀더가 `T06 - SLEEVE`처럼 문구를 가지면 건너뛰지 않음.
3. `test_lathe_parse_program_ignores_trailing_range_comment`
   — `( MAX : Z100. / MIN : Z0. )` 가 인서트를 덮어쓰지 않음.
4. `test_lathe_tool_name_map_uses_insert_on_new_post`
   — 필터 라벨에 인서트 문구(`10.0 R0.5 E/M ...`)가 들어감.
5. (결정 A 승인 시) `test_lathe_parse_program_falls_back_to_tool_no_comment`
   — 블록 코드에 T워드가 없을 때 `(T0404)`가 TOOL NO가 됨.

전체 회귀: `pytest tests/test_nc_tool_list.py` 통과 확인
(§0 규칙 3 — 밀링 테스트 전수 통과).

---

## 5. 문서 갱신

- `LATHE_MODE_GUIDELINES.md` §7 — 파서 설명에 "신규 포스트는 N 아래 첫
  `(Tnnnn)` 주석을 건너뛰고 그다음 두 주석을 홀더/인서트로 읽는다(v1.7.5,
  예전 포스트와 동시 지원)" 추가, §10 승인 이력에 v1.7.5 한 줄 추가.

---

## 6. 버전 / 릴리스

- `APP_VERSION` `1.7.4` → `1.7.5`, `version_info.txt`, `NC_Tool_List.iss` 동기화.
- 설치본/포터블 빌드는 **별도 승인 후** 진행(§10 규약).

---

## 7. 확인이 필요한 결정 사항

| # | 항목 | 제안 |
|---|---|---|
| **A** | 블록 코드에 T워드가 없을 때 `(Tnnnn)` 주석을 TOOL NO 폴백으로 쓸지 | **쓴다** (기존 결과 무변화, 데이터 유실만 구제) |
| **B** | `(Tnnnn)` 판정에 2자리(`(T04)`)도 포함할지 | **포함** — 건너뛰기 판정에만. TOOL NO 폴백에는 미사용 |
| **C** | `( MAX : Z.. / MIN : Z.. )` 가공범위 주석을 별도 열로 넣을지 | **이번엔 넣지 않음** (요구 범위 밖) |
