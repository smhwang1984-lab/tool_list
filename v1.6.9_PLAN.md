# v1.6.9 업데이트 플랜 (승인 대기)

원본 요구: 사용자 지시 (2026-09-06)

> 1. 선반 항목 수정 — `X200. Z200.`에서 `G0 X100. Z10.`으로 이동할 때 G0 라인이 이전 끝난
>    위치에서 시작함. `T0100 ~ T0100` 사이를 공정으로 잡은 것 때문으로 보임. 선반 공정은
>    **N번호 공정 시작부터 M1 / M0 / M30까지**이고, 공정이란 **수행하는 부분**을 말함.
> 2. `G12.1(G112)` / `G13.1(G113)` 관련 수정 — G17 평면 지령이 없어도 가공이 가능함.
>    (`G28 H0`은 C축 원점 복귀, H는 C의 증분, U는 X의 증분, W는 Z의 증분)
>    `G12.1 ~ G13.1` 사이는 XY 평면 극좌표 가공이고 **C = Y 좌표**로 보면 됨. 모션은 X축이
>    상하로 움직이며 전체를 제어하고, 실제로는 X축과 C축의 움직임으로 XY 평면을 가공함.
>    여기서 **-X 값이어도 X는 실제 +위치에서 움직이고 C축이 회전된 상태**임.
> 3. 툴리스트 선반 MCT(밀링) 부분에서 "MCT" 문구 제거 — **선반 / 밀링**만 표기.

**전제**: `LATHE_MODE_GUIDELINES.md` §0 완전 분리 원칙을 그대로 지킨다. 아래 변경은 전부
`is_lathe` 분기 안에서만 일어나고, 밀링(3/4/5축 MCT) 경로 계산은 한 줄도 건드리지 않는다.

---

## 항목 1. 선반 공정 분리 기준을 `Tnn00` → `N번호 ~ M0/M1/M30`으로

### 현재 상태 (증상의 원인)

`nc_viewer_widget.py:2852-2889` `NCViewerWidget.process_nc_lines()`:

```python
lathe_tool_change_pattern = re.compile(r"T(?!0000)(\d{2})00(?!\d)")   # :2665
...
if tool_changed:                                   # :2876
    process_no += 1
    current_tool = self._make_process_key(process_no, detected_t)
    self.process_first_line[current_tool] = idx
    start_point = lathe_world_point(cz, cx, cc_deg) if is_lathe else [cx, cy, cz]
    self.tool_paths[current_tool].append({"pt": start_point, ...})     # :2883-2888
```

`cx / cz / cc_deg`는 공정이 바뀌어도 **리셋되지 않는다**. 그래서 선반에서 흔한

```
G01 X60. Z-30. F0.2
G00 X200. Z200. T0300     ← 복귀 + 다음 공구 예약이 한 줄
M1
N2
G50 S2500 T0303
G00 X100. Z10.
```

같은 프로그램이면, `T0300`이 있는 **복귀 줄에서 공정 2가 열리고** 그 첫 노드가 직전 절삭
끝점(X60/Z-30)이 된다. 이어서 같은 줄의 모션(X200/Z200)이 공정 2에 붙으므로,
`_render_segments`(`:3366-3391`)가 첫 세그먼트를 `X60,Z-30 → X200,Z200`으로 그린다.
사용자가 본 "G0 라인이 이전 끝난 위치에서 시작함"이 정확히 이것이다.
(같은 이유로 `_compute_machining_times`(`:3192`)도 복귀 거리를 다음 공정 시간에 넣는다.)

v1.6.7의 `active_lathe_tool` 중복 제거는 **같은 공구 번호**만 걸러내므로, 다음 공구를
예약하는 이 형태에는 듣지 않는다.

### 변경 (선반 분기 한정)

`process_nc_lines()`에 **선반 전용 N 블록 사전 스캔**을 추가한다.

1. **시퀀스 구체화** — `line_sequence`(`:2721`, 선반은 `_expand_lathe_subprograms` 제너레이터)를
   선반일 때만 `list(...)`로 받아 사전 스캔과 본 루프가 같은 시퀀스를 보게 한다.
   밀링은 지금처럼 `enumerate(lines)` 제너레이터 그대로 — 동작·메모리 특성 불변.
2. **N 블록 판정** — 원본 라인(주석/공백 제거 전)에 `^\s*N(\d+)\s*$`를 적용한다.
   `line_upper`는 주석·공백이 제거된 형태(`:2723-2724`)라 `N1 (COMMENT)`까지 잡혀
   공구리스트(`NC_Tool_List.py:166 parse_lathe_program`, `:139 LATHE_N_RE`)와 어긋난다.
   **두 곳이 반드시 같은 규약이어야 하므로 원본 라인 기준으로 통일**한다.
   - `NC_Tool_List.py`는 모듈 최상단에서 `nc_viewer_widget`을 import하지 않으므로
     (`:1278`의 지연 import뿐 — Qt 없이도 파서를 쓰기 위한 구조) 상수를 공유할 수 없다.
     `nc_viewer_widget.py`에 `LATHE_N_LINE_RE`를 같은 패턴으로 두고, 주석으로
     `NC_Tool_List.LATHE_N_RE`가 원본임을 밝힌 뒤 **두 패턴이 일치하는지 검사하는
     테스트**를 둬서 드리프트를 막는다.
3. **블록별 공구 번호** — N 라인 다음부터 다음 N 라인 전까지에서 `parse_lathe_program`과
   **같은 우선순위**로 고른다.
   - ① 옵셋이 살아 있는 `Tnnnn`(뒤 두 자리 ≠ `00`, `T0000` 제외) 중 첫 번째 → 앞 두 자리
   - ② 없으면 `Tnn00`(`T0000` 제외) 중 첫 번째
   - 이 우선순위 덕분에 블록 끝 복귀 줄의 **다음 공구 예약 `T0500`은 무시**되고 그 블록이
     실제로 쓴 `T0303`이 잡힌다. 공구리스트와 3D 공정 필터의 공구명이 항상 일치한다.
4. **공정 분할** — 선반 분기의 `tool_changed` 판정을 교체한다.
   ```python
   if is_lathe:
       if n_block_starts:                      # N 라인이 하나라도 있으면 N 기준
           tool_changed = idx in n_block_starts
           if tool_changed:
               detected_t = n_block_tool[idx]
       else:
           ... 기존 Tnn00 + active_lathe_tool 로직 그대로 (폴백) ...
   ```
   **N 라인이 하나도 없는 선반 프로그램은 기존 `Tnn00` 로직으로 자동 폴백**한다
   (2026-09-06 사용자 확정). 기존 선반 테스트(`test_lathe_viewer_splits_processes_on_tnn00`,
   `test_lathe_process_is_listed_once_despite_closing_offset_cancel`,
   `test_same_tool_in_two_processes_still_splits_after_m01`)는 전부 N 라인이 없는
   소스라 그대로 통과한다.
5. **공정 종료(M0/M1/M30)** — `lathe_process_end_pattern`(`:2674`)을 만나면 `process_open`
   플래그를 내린다. 닫혀 있는 동안:
   - 모달 상태(`cx/cz/cc_deg/current_motion/F/S`)와 `line_to_tool_map`은 **계속 갱신**한다
     (커서 동기화·PG 매칭이 깨지지 않게).
   - `tool_paths`에 **경로 노드를 추가하지 않는다** — "공정 = 수행하는 부분"이라는 정의대로,
     M1 이후 다음 N 전까지의 잔여 이동이 공정에 딸려붙지 않는다.
   - 다음 N 라인에서 다시 열린다. 폴백(Tnn00) 경로에서는 기존처럼 `active_lathe_tool`만
     리셋하고 `process_open`은 항상 열어 둬서 v1.6.7 동작을 그대로 유지한다.
   - 덤으로 `polar_interpolation`(항목 2)도 여기서 리셋한다.

### 결과

복귀 줄 `G00 X200. Z200. T0300`이 **공정 1에 남고**, 공정 2는 `N2`에서 열리면서 시작점이
X200/Z200이 된다 → `G0 X100. Z10.`이 올바르게 X200/Z200에서 출발한다.

### 손대지 않는 것

- **'다음공구검색'은 현행 `Tnn00` 유지** (2026-09-06 사용자 확정).
  `NC_Tool_List.py:678 find_next_tool_change_span` / `:131 LATHE_T_SEARCH_RE` 변경 없음 —
  이 버튼의 목적은 "공구 교체 지점 찾기"라서 공정 경계와 별개다.
  → `LATHE_MODE_GUIDELINES.md` §4의 "세 곳이 같은 기준" 문장에 **예외를 명시**한다.
- 밀링 `M6 Tnn` 판정 경로(`:2870-2874`) 전면 유지.

---

## 항목 2. `G12.1`/`G112` ~ `G13.1`/`G113` 극좌표 보간 수정

### 현재 상태

전부 `nc_viewer_widget.py process_nc_lines()` 안에 있다.

| 위치 | 현재 코드 | 문제 |
|---|---|---|
| `:2686-2687` | `re.compile(r"G12\.1\|G112")`, `r"G13\.1\|G113"` | `(?!\d)` 가드 없음 |
| `:2814-2824` | `if not (is_lathe and lathe_milling_active): continue` | **M35 밖이면 G12.1 줄이 통째로 버려짐** (같은 줄 모션 워드 손실) |
| `:2996` | `if is_lathe and lathe_milling_active and polar_interpolation:` | **M35 밖이면 C가 계속 각도로 읽힘** → 경로가 회전으로 그려짐 |
| `:3122` | `if lathe_milling_active and not polar_interpolation and current_plane in ("G17","G19")` | **극좌표를 명시적으로 제외** → 극좌표 원호가 `"LATHE"`(기계 Z × 반경) 평면으로 보간됨. 게다가 선반 세이프티 라인의 `G18` 때문에 `current_plane`이 G18이면 M35 분기 자체가 안 걸림 |
| `:2812` | `cy_lathe = 0.0`은 `M34`에서만 | `G13.1`이 `cy_lathe`를 안 지워 극좌표 종료 후 선삭 경로가 Y로 밀림 |
| `:2681, :2933` | `g28_pattern`은 `G91`과 함께일 때만 처리 | `G28 H0.`(C축 원점복귀) 미처리 |

### 변경 (전부 선반 분기 한정)

1. **정규식 가드** — `r"G12\.1(?!\d)|G112(?!\d)"`, `r"G13\.1(?!\d)|G113(?!\d)"`,
   `g28_pattern`도 `r"G28(?!\d)"`로 좁힌다.
2. **M35 게이팅 제거** (2026-09-06 사용자 확정) — 극좌표는 `M35`와 무관하게 인식한다.
   - `:2819`, `:2823`의 `continue` 조건을 `if not is_lathe:`로 바꿔, 선반에서는 G12.1/G13.1
     줄에 붙은 모션 워드(`G12.1 X0. C0.` 등)를 항상 살린다.
   - `:2996` 조건을 `if is_lathe and polar_interpolation:`으로 바꿔 **C를 항상 Y(mm)로**
     해석한다. `cc_deg`는 G12.1 진입 시점 값으로 동결(현행 유지) — 극좌표 윤곽은 로컬
     프레임에서 그린 뒤 그 각도만큼 통째로 배치 회전된다.
3. **극좌표 원호 평면 강제** — `:3121`의 선반 원호 분기 **맨 앞**에 극좌표 분기를 넣는다.
   ```python
   if polar_interpolation:
       # 극좌표는 평면 지령(G17/G18/G19)과 무관하게 X(반경)–C(Y) 평면이다.
       plane_key = "LATHE_G17"
       local_arc_pts = self._arc_points(line_upper, start_local, target_local,
                                        current_motion, plane_key, ...)
       → lathe_rotate_c(local_pt, cc_deg)로 배치 회전
   elif lathe_milling_active and current_plane in ("G17", "G19"):
       ... 기존 M35 분기 그대로 ...
   else:
       ... 기존 "LATHE" 선삭 분기 그대로 ...
   ```
   `ARC_PLANE_AXES["LATHE_G17"] = (2, 1, 0, "i", "j")`(`:76-94`)가 그대로 맞는다 —
   u = 월드 Z(= X 반경, 오프셋 **I**), v = 월드 Y(= C, 오프셋 **J**), w = 월드 X(= 기계 Z,
   깊이로 선형 보간). 즉 **G17 평면 지령이 없어도, G18이 걸려 있어도** 극좌표 원호가
   XY(=X–C) 평면으로 그려진다 — 요구사항 그대로.
4. **`G13.1`에서 `cy_lathe = 0.0` 리셋** — 극좌표를 빠져나오면 선삭 경로가 다시 Z–X
   평면(Y=0)으로 돌아온다. `M34`의 기존 리셋은 유지.
5. **`G28 H0.` = C축 원점 복귀** — 선반 분기에서 G28 라인에 `H` 워드가 있으면
   `cc_deg = 0.0`, `self.line_to_c_rot[idx] = 0.0`으로 두고 기존 G28 처리와 같은 모양으로
   `G00` 노드를 남긴다.
   - **U / W(X·Z 증분)는 이번 범위에서 제외** — `LATHE_MODE_GUIDELINES.md` §8이
     "U/W 증분 지령, G90/G91 증분 모드"를 별도 승인 단계로 미뤄 뒀고, 현재 `G28 U0. W0.`은
     매칭되는 워드가 없어 **무해한 no-op**(잘못된 선을 그리지 않음)이라 급하지 않다.
6. **음수 X** — `abs()`를 넣지 않는다. 극좌표 구간의 X는 **부호 있는 반경 좌표**이므로,
   `-X`는 파트 XY 평면에서 중심 반대편 점으로 그대로 그리는 것이 실제 형상과 맞는다
   ("X는 실제 +위치에서 움직이고 C축이 180° 회전된 상태" = 같은 점의 기계 축 표현).
   툴패스는 파트 좌표계로 보여야 하므로 프로그램 좌표를 그대로 쓴다.
7. **X는 지름 유지** (2026-09-06 사용자 확정) — `lathe_local_point`의 `X/2` 환산을 그대로
   둔다. 실제 프로그램 `O4006.nc`의 `X-.076 Z-11. C-.067 R.077`이 이 해석에서
   `sqrt((0.076/2)² + 0.067²) = 0.077`로 **R과 정확히 일치**(원점 중심 원호)하는 것이 근거.

---

## 항목 3. 산출 모드 콤보에서 "MCT" 문구 제거

사용자에게 보이는 "MCT"는 **산출 모드 콤보 항목 하나뿐**이다. 표 헤더(`LATHE_COLUMNS`,
`COLUMNS`)·PDF·창 제목에는 없다.

| 파일:줄 | 현재 | 변경 |
|---|---|---|
| `NC_Tool_List.py:2440` | `addItems(['MCT (밀링)', '선반'])` | `addItems(['밀링', '선반'])` |
| `NC_Tool_List.py:2442` | `setCurrentText('선반' if ... else 'MCT (밀링)')` | `'밀링'` |
| `NC_Tool_List.py:2300` | `_sync_tool_mode_combo`의 `'MCT (밀링)'` | `'밀링'` |

- 판정 로직(`:2307 target_lathe = ... == '선반'`)은 `'선반'`만 비교하므로 영향 없다.
- 콤보가 editable이 아니라 **`setCurrentText`에 없는 문자열을 주면 조용히 무시**된다.
  따라서 위 3곳을 **반드시 함께** 고쳐야 밀링 장비로 되돌릴 때 콤보가 '선반'에 붙어 있는
  버그가 안 생긴다.
- **장비 콤보의 `3축 MCT (X Y Z)` / `5축 MCT (A to C)` 등은 건드리지 않는다** — 요구는
  툴리스트 쪽 표기이고, 장비명은 QSettings 마이그레이션(`RENAMED_MACHINE_TYPES`)이
  걸린 별개 사안이다.

---

## 항목 4. 버전 확정 · 문서 · 회귀 방지

### 버전
- `NC_Tool_List.py:29,31` `APP_VERSION = '1.6.9'`, `APP_BUILD_DATE`
- `NC_Tool_List.iss:5` `MyAppVersion "1.6.9"`
- `version_info.txt` `filevers/prodvers = (1, 6, 9, 0)`, `FileVersion`/`ProductVersion` 문자열

### 문서
- `LATHE_MODE_GUIDELINES.md`
  - §4: 3D 공정 분리는 **N 기준**(N 없으면 Tnn00 폴백), 공구리스트·다음공구검색은
    기존 기준 유지 — **예외를 명시**
  - §8: v1.6.9 항목 추가 (공정 경계, 극좌표 M35 비의존 + 평면 강제, G28 H0)
    / U·W 증분은 계속 보류로 남김
  - §9: v1.6.9 플랜 승인 이력
- `ABOUT_RELEASE_INSTRUCTIONS.md`에 v1.6.9 이력 추가

### 테스트 (`tests/test_nc_tool_list.py` 신규)
| # | 검증 |
|---|---|
| 1 | `G00 X200. Z200. T0300` + `M1` + `N2` 프로그램에서 공정이 **2개**, 공정 2의 첫 노드가 **X200/Z200**(직전 절삭 끝점이 아님) |
| 2 | 복귀 이동이 **공정 1**에 속하고, 공정 2의 가공시간에 복귀 거리가 안 들어감 |
| 3 | N 라인 없는 선반 소스는 기존 Tnn00 분리 그대로 (폴백) — 기존 3개 테스트 무수정 통과 |
| 4 | N 블록 공구번호가 복귀 줄의 다음 공구 예약(`T0500`)이 아니라 블록의 `T0303`을 따름 |
| 5 | `nc_viewer_widget.LATHE_N_LINE_RE` 패턴이 `NC_Tool_List.LATHE_N_RE`와 동일 |
| 6 | `M35` 없이 `G12.1`만 있어도 C가 **Y(mm)** 로 해석됨 |
| 7 | `G18`이 선행해도 극좌표 원호가 **X–C 평면**(LATHE_G17)으로 보간됨 |
| 8 | `G12.1` 줄에 붙은 모션 워드(`G12.1 X0. C0.`)가 버려지지 않음 |
| 9 | `G13.1` 뒤 `cy_lathe`가 0으로 리셋됨 |
| 10 | 극좌표 구간의 음수 X가 중심 반대편(월드 Z < 0)으로 그려짐 |
| 11 | `G28 H0.` 뒤 `cc_deg == 0` |
| 12 | 산출 모드 콤보가 `'밀링'`/`'선반'`이고 장비 전환 시 왕복 동기화됨 (기존 `ToolListModeComboTests` 4곳 문자열 갱신) |
| 13 | **밀링 회귀** — `test_lathe_mode_does_not_change_milling_paths` 포함 `pytest tests/` 전체 통과 |

### 빌드
- **설치본/포터블 빌드는 사용자 승인 후 별도 수행** (기존 릴리스 절차와 동일).

---

## 작업 순서

1. 항목 1 공정 분리 (N 사전 스캔 + 폴백 + `process_open`)
2. 항목 2 극좌표 (같은 파싱 루프라 이어서)
3. 항목 3 콤보 표기
4. 테스트 작성 → 전체 통과 → 버전 1.6.9 확정 → 문서 갱신

---

## 확정 사항 (2026-09-06 사용자 승인)

- 극좌표 구간의 X는 **지름** — 현행 `X/2` 환산 유지.
- `G12.1` 극좌표는 **M35와 무관하게** 인식한다.
- 단독 N 라인이 없는 선반 프로그램은 **기존 `Tnn00` 기준으로 자동 폴백**.
- **'다음공구검색'은 `Tnn00` 유지** — 공정 분리 기준만 N으로 바꾼다.
